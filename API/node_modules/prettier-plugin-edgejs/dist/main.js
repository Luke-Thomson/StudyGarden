import Ao from "uglify-js";
var Ui = typeof global == "object" && global && global.Object === Object && global, _o = typeof self == "object" && self && self.Object === Object && self, Oe = Ui || _o || Function("return this")(), ue = Oe.Symbol, Di = Object.prototype, vo = Di.hasOwnProperty, Ro = Di.toString, pt = ue ? ue.toStringTag : void 0;
function Oo(t) {
  var e = vo.call(t, pt), n = t[pt];
  try {
    t[pt] = void 0;
    var r = !0;
  } catch {
  }
  var i = Ro.call(t);
  return r && (e ? t[pt] = n : delete t[pt]), i;
}
var So = Object.prototype, Io = So.toString;
function No(t) {
  return Io.call(t);
}
var Lo = "[object Null]", ko = "[object Undefined]", xr = ue ? ue.toStringTag : void 0;
function Ve(t) {
  return t == null ? t === void 0 ? ko : Lo : xr && xr in Object(t) ? Oo(t) : No(t);
}
function ge(t) {
  return t != null && typeof t == "object";
}
var Co = "[object Symbol]";
function on(t) {
  return typeof t == "symbol" || ge(t) && Ve(t) == Co;
}
function an(t, e) {
  for (var n = -1, r = t == null ? 0 : t.length, i = Array(r); ++n < r; )
    i[n] = e(t[n], n, t);
  return i;
}
var x = Array.isArray, Mr = ue ? ue.prototype : void 0, Pr = Mr ? Mr.toString : void 0;
function $i(t) {
  if (typeof t == "string")
    return t;
  if (x(t))
    return an(t, $i) + "";
  if (on(t))
    return Pr ? Pr.call(t) : "";
  var e = t + "";
  return e == "0" && 1 / t == -1 / 0 ? "-0" : e;
}
var bo = /\s/;
function xo(t) {
  for (var e = t.length; e-- && bo.test(t.charAt(e)); )
    ;
  return e;
}
var Mo = /^\s+/;
function Po(t) {
  return t && t.slice(0, xo(t) + 1).replace(Mo, "");
}
function le(t) {
  var e = typeof t;
  return t != null && (e == "object" || e == "function");
}
var wr = NaN, wo = /^[-+]0x[0-9a-f]+$/i, Uo = /^0b[01]+$/i, Do = /^0o[0-7]+$/i, $o = parseInt;
function Fo(t) {
  if (typeof t == "number")
    return t;
  if (on(t))
    return wr;
  if (le(t)) {
    var e = typeof t.valueOf == "function" ? t.valueOf() : t;
    t = le(e) ? e + "" : e;
  }
  if (typeof t != "string")
    return t === 0 ? t : +t;
  t = Po(t);
  var n = Uo.test(t);
  return n || Do.test(t) ? $o(t.slice(2), n ? 2 : 8) : wo.test(t) ? wr : +t;
}
var jo = 1 / 0, Go = 17976931348623157e292;
function Bo(t) {
  if (!t)
    return t === 0 ? t : 0;
  if (t = Fo(t), t === jo || t === -1 / 0) {
    var e = t < 0 ? -1 : 1;
    return e * Go;
  }
  return t === t ? t : 0;
}
function cn(t) {
  var e = Bo(t), n = e % 1;
  return e === e ? n ? e - n : e : 0;
}
function at(t) {
  return t;
}
var Vo = "[object AsyncFunction]", Ko = "[object Function]", Ho = "[object GeneratorFunction]", Wo = "[object Proxy]";
function De(t) {
  if (!le(t))
    return !1;
  var e = Ve(t);
  return e == Ko || e == Ho || e == Vo || e == Wo;
}
var Sn = Oe["__core-js_shared__"], Ur = function() {
  var t = /[^.]+$/.exec(Sn && Sn.keys && Sn.keys.IE_PROTO || "");
  return t ? "Symbol(src)_1." + t : "";
}();
function zo(t) {
  return !!Ur && Ur in t;
}
var Yo = Function.prototype, Xo = Yo.toString;
function Je(t) {
  if (t != null) {
    try {
      return Xo.call(t);
    } catch {
    }
    try {
      return t + "";
    } catch {
    }
  }
  return "";
}
var qo = /[\\^$.*+?()[\]{}|]/g, Qo = /^\[object .+?Constructor\]$/, Zo = Function.prototype, Jo = Object.prototype, ea = Zo.toString, ta = Jo.hasOwnProperty, na = RegExp(
  "^" + ea.call(ta).replace(qo, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
);
function ra(t) {
  if (!le(t) || zo(t))
    return !1;
  var e = De(t) ? na : Qo;
  return e.test(Je(t));
}
function ia(t, e) {
  return t?.[e];
}
function et(t, e) {
  var n = ia(t, e);
  return ra(n) ? n : void 0;
}
var Dn = et(Oe, "WeakMap"), Dr = Object.create, sa = /* @__PURE__ */ function() {
  function t() {
  }
  return function(e) {
    if (!le(e))
      return {};
    if (Dr)
      return Dr(e);
    t.prototype = e;
    var n = new t();
    return t.prototype = void 0, n;
  };
}();
function oa(t, e, n) {
  switch (n.length) {
    case 0:
      return t.call(e);
    case 1:
      return t.call(e, n[0]);
    case 2:
      return t.call(e, n[0], n[1]);
    case 3:
      return t.call(e, n[0], n[1], n[2]);
  }
  return t.apply(e, n);
}
function K() {
}
function aa(t, e) {
  var n = -1, r = t.length;
  for (e || (e = Array(r)); ++n < r; )
    e[n] = t[n];
  return e;
}
var ca = 800, ua = 16, la = Date.now;
function ha(t) {
  var e = 0, n = 0;
  return function() {
    var r = la(), i = ua - (r - n);
    if (n = r, i > 0) {
      if (++e >= ca)
        return arguments[0];
    } else
      e = 0;
    return t.apply(void 0, arguments);
  };
}
function fa(t) {
  return function() {
    return t;
  };
}
var Yt = function() {
  try {
    var t = et(Object, "defineProperty");
    return t({}, "", {}), t;
  } catch {
  }
}(), da = Yt ? function(t, e) {
  return Yt(t, "toString", {
    configurable: !0,
    enumerable: !1,
    value: fa(e),
    writable: !0
  });
} : at, pa = ha(da);
function Fi(t, e) {
  for (var n = -1, r = t == null ? 0 : t.length; ++n < r && e(t[n], n, t) !== !1; )
    ;
  return t;
}
function ji(t, e, n, r) {
  for (var i = t.length, s = n + -1; ++s < i; )
    if (e(t[s], s, t))
      return s;
  return -1;
}
function ma(t) {
  return t !== t;
}
function ga(t, e, n) {
  for (var r = n - 1, i = t.length; ++r < i; )
    if (t[r] === e)
      return r;
  return -1;
}
function rr(t, e, n) {
  return e === e ? ga(t, e, n) : ji(t, ma, n);
}
function Gi(t, e) {
  var n = t == null ? 0 : t.length;
  return !!n && rr(t, e, 0) > -1;
}
var Ea = 9007199254740991, Ta = /^(?:0|[1-9]\d*)$/;
function un(t, e) {
  var n = typeof t;
  return e = e ?? Ea, !!e && (n == "number" || n != "symbol" && Ta.test(t)) && t > -1 && t % 1 == 0 && t < e;
}
function ir(t, e, n) {
  e == "__proto__" && Yt ? Yt(t, e, {
    configurable: !0,
    enumerable: !0,
    value: n,
    writable: !0
  }) : t[e] = n;
}
function It(t, e) {
  return t === e || t !== t && e !== e;
}
var ya = Object.prototype, Aa = ya.hasOwnProperty;
function ln(t, e, n) {
  var r = t[e];
  (!(Aa.call(t, e) && It(r, n)) || n === void 0 && !(e in t)) && ir(t, e, n);
}
function sr(t, e, n, r) {
  var i = !n;
  n || (n = {});
  for (var s = -1, o = e.length; ++s < o; ) {
    var a = e[s], c = void 0;
    c === void 0 && (c = t[a]), i ? ir(n, a, c) : ln(n, a, c);
  }
  return n;
}
var $r = Math.max;
function _a(t, e, n) {
  return e = $r(e === void 0 ? t.length - 1 : e, 0), function() {
    for (var r = arguments, i = -1, s = $r(r.length - e, 0), o = Array(s); ++i < s; )
      o[i] = r[e + i];
    i = -1;
    for (var a = Array(e + 1); ++i < e; )
      a[i] = r[i];
    return a[e] = n(o), oa(t, this, a);
  };
}
function or(t, e) {
  return pa(_a(t, e, at), t + "");
}
var va = 9007199254740991;
function ar(t) {
  return typeof t == "number" && t > -1 && t % 1 == 0 && t <= va;
}
function Se(t) {
  return t != null && ar(t.length) && !De(t);
}
function Bi(t, e, n) {
  if (!le(n))
    return !1;
  var r = typeof e;
  return (r == "number" ? Se(n) && un(e, n.length) : r == "string" && e in n) ? It(n[e], t) : !1;
}
function Ra(t) {
  return or(function(e, n) {
    var r = -1, i = n.length, s = i > 1 ? n[i - 1] : void 0, o = i > 2 ? n[2] : void 0;
    for (s = t.length > 3 && typeof s == "function" ? (i--, s) : void 0, o && Bi(n[0], n[1], o) && (s = i < 3 ? void 0 : s, i = 1), e = Object(e); ++r < i; ) {
      var a = n[r];
      a && t(e, a, r, s);
    }
    return e;
  });
}
var Oa = Object.prototype;
function Nt(t) {
  var e = t && t.constructor, n = typeof e == "function" && e.prototype || Oa;
  return t === n;
}
function Sa(t, e) {
  for (var n = -1, r = Array(t); ++n < t; )
    r[n] = e(n);
  return r;
}
var Ia = "[object Arguments]";
function Fr(t) {
  return ge(t) && Ve(t) == Ia;
}
var Vi = Object.prototype, Na = Vi.hasOwnProperty, La = Vi.propertyIsEnumerable, hn = Fr(/* @__PURE__ */ function() {
  return arguments;
}()) ? Fr : function(t) {
  return ge(t) && Na.call(t, "callee") && !La.call(t, "callee");
};
function ka() {
  return !1;
}
var Ki = typeof exports == "object" && exports && !exports.nodeType && exports, jr = Ki && typeof module == "object" && module && !module.nodeType && module, Ca = jr && jr.exports === Ki, Gr = Ca ? Oe.Buffer : void 0, ba = Gr ? Gr.isBuffer : void 0, vt = ba || ka, xa = "[object Arguments]", Ma = "[object Array]", Pa = "[object Boolean]", wa = "[object Date]", Ua = "[object Error]", Da = "[object Function]", $a = "[object Map]", Fa = "[object Number]", ja = "[object Object]", Ga = "[object RegExp]", Ba = "[object Set]", Va = "[object String]", Ka = "[object WeakMap]", Ha = "[object ArrayBuffer]", Wa = "[object DataView]", za = "[object Float32Array]", Ya = "[object Float64Array]", Xa = "[object Int8Array]", qa = "[object Int16Array]", Qa = "[object Int32Array]", Za = "[object Uint8Array]", Ja = "[object Uint8ClampedArray]", ec = "[object Uint16Array]", tc = "[object Uint32Array]", $ = {};
$[za] = $[Ya] = $[Xa] = $[qa] = $[Qa] = $[Za] = $[Ja] = $[ec] = $[tc] = !0;
$[xa] = $[Ma] = $[Ha] = $[Pa] = $[Wa] = $[wa] = $[Ua] = $[Da] = $[$a] = $[Fa] = $[ja] = $[Ga] = $[Ba] = $[Va] = $[Ka] = !1;
function nc(t) {
  return ge(t) && ar(t.length) && !!$[Ve(t)];
}
function fn(t) {
  return function(e) {
    return t(e);
  };
}
var Hi = typeof exports == "object" && exports && !exports.nodeType && exports, At = Hi && typeof module == "object" && module && !module.nodeType && module, rc = At && At.exports === Hi, In = rc && Ui.process, Ge = function() {
  try {
    var t = At && At.require && At.require("util").types;
    return t || In && In.binding && In.binding("util");
  } catch {
  }
}(), Br = Ge && Ge.isTypedArray, cr = Br ? fn(Br) : nc, ic = Object.prototype, sc = ic.hasOwnProperty;
function Wi(t, e) {
  var n = x(t), r = !n && hn(t), i = !n && !r && vt(t), s = !n && !r && !i && cr(t), o = n || r || i || s, a = o ? Sa(t.length, String) : [], c = a.length;
  for (var u in t)
    (e || sc.call(t, u)) && !(o && // Safari 9 has enumerable `arguments.length` in strict mode.
    (u == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
    i && (u == "offset" || u == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
    s && (u == "buffer" || u == "byteLength" || u == "byteOffset") || // Skip index properties.
    un(u, c))) && a.push(u);
  return a;
}
function zi(t, e) {
  return function(n) {
    return t(e(n));
  };
}
var oc = zi(Object.keys, Object), ac = Object.prototype, cc = ac.hasOwnProperty;
function Yi(t) {
  if (!Nt(t))
    return oc(t);
  var e = [];
  for (var n in Object(t))
    cc.call(t, n) && n != "constructor" && e.push(n);
  return e;
}
function he(t) {
  return Se(t) ? Wi(t) : Yi(t);
}
var uc = Object.prototype, lc = uc.hasOwnProperty, ne = Ra(function(t, e) {
  if (Nt(e) || Se(e)) {
    sr(e, he(e), t);
    return;
  }
  for (var n in e)
    lc.call(e, n) && ln(t, n, e[n]);
});
function hc(t) {
  var e = [];
  if (t != null)
    for (var n in Object(t))
      e.push(n);
  return e;
}
var fc = Object.prototype, dc = fc.hasOwnProperty;
function pc(t) {
  if (!le(t))
    return hc(t);
  var e = Nt(t), n = [];
  for (var r in t)
    r == "constructor" && (e || !dc.call(t, r)) || n.push(r);
  return n;
}
function Xi(t) {
  return Se(t) ? Wi(t, !0) : pc(t);
}
var mc = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/, gc = /^\w*$/;
function ur(t, e) {
  if (x(t))
    return !1;
  var n = typeof t;
  return n == "number" || n == "symbol" || n == "boolean" || t == null || on(t) ? !0 : gc.test(t) || !mc.test(t) || e != null && t in Object(e);
}
var Rt = et(Object, "create");
function Ec() {
  this.__data__ = Rt ? Rt(null) : {}, this.size = 0;
}
function Tc(t) {
  var e = this.has(t) && delete this.__data__[t];
  return this.size -= e ? 1 : 0, e;
}
var yc = "__lodash_hash_undefined__", Ac = Object.prototype, _c = Ac.hasOwnProperty;
function vc(t) {
  var e = this.__data__;
  if (Rt) {
    var n = e[t];
    return n === yc ? void 0 : n;
  }
  return _c.call(e, t) ? e[t] : void 0;
}
var Rc = Object.prototype, Oc = Rc.hasOwnProperty;
function Sc(t) {
  var e = this.__data__;
  return Rt ? e[t] !== void 0 : Oc.call(e, t);
}
var Ic = "__lodash_hash_undefined__";
function Nc(t, e) {
  var n = this.__data__;
  return this.size += this.has(t) ? 0 : 1, n[t] = Rt && e === void 0 ? Ic : e, this;
}
function qe(t) {
  var e = -1, n = t == null ? 0 : t.length;
  for (this.clear(); ++e < n; ) {
    var r = t[e];
    this.set(r[0], r[1]);
  }
}
qe.prototype.clear = Ec;
qe.prototype.delete = Tc;
qe.prototype.get = vc;
qe.prototype.has = Sc;
qe.prototype.set = Nc;
function Lc() {
  this.__data__ = [], this.size = 0;
}
function dn(t, e) {
  for (var n = t.length; n--; )
    if (It(t[n][0], e))
      return n;
  return -1;
}
var kc = Array.prototype, Cc = kc.splice;
function bc(t) {
  var e = this.__data__, n = dn(e, t);
  if (n < 0)
    return !1;
  var r = e.length - 1;
  return n == r ? e.pop() : Cc.call(e, n, 1), --this.size, !0;
}
function xc(t) {
  var e = this.__data__, n = dn(e, t);
  return n < 0 ? void 0 : e[n][1];
}
function Mc(t) {
  return dn(this.__data__, t) > -1;
}
function Pc(t, e) {
  var n = this.__data__, r = dn(n, t);
  return r < 0 ? (++this.size, n.push([t, e])) : n[r][1] = e, this;
}
function $e(t) {
  var e = -1, n = t == null ? 0 : t.length;
  for (this.clear(); ++e < n; ) {
    var r = t[e];
    this.set(r[0], r[1]);
  }
}
$e.prototype.clear = Lc;
$e.prototype.delete = bc;
$e.prototype.get = xc;
$e.prototype.has = Mc;
$e.prototype.set = Pc;
var Ot = et(Oe, "Map");
function wc() {
  this.size = 0, this.__data__ = {
    hash: new qe(),
    map: new (Ot || $e)(),
    string: new qe()
  };
}
function Uc(t) {
  var e = typeof t;
  return e == "string" || e == "number" || e == "symbol" || e == "boolean" ? t !== "__proto__" : t === null;
}
function pn(t, e) {
  var n = t.__data__;
  return Uc(e) ? n[typeof e == "string" ? "string" : "hash"] : n.map;
}
function Dc(t) {
  var e = pn(this, t).delete(t);
  return this.size -= e ? 1 : 0, e;
}
function $c(t) {
  return pn(this, t).get(t);
}
function Fc(t) {
  return pn(this, t).has(t);
}
function jc(t, e) {
  var n = pn(this, t), r = n.size;
  return n.set(t, e), this.size += n.size == r ? 0 : 1, this;
}
function Fe(t) {
  var e = -1, n = t == null ? 0 : t.length;
  for (this.clear(); ++e < n; ) {
    var r = t[e];
    this.set(r[0], r[1]);
  }
}
Fe.prototype.clear = wc;
Fe.prototype.delete = Dc;
Fe.prototype.get = $c;
Fe.prototype.has = Fc;
Fe.prototype.set = jc;
var Gc = "Expected a function";
function lr(t, e) {
  if (typeof t != "function" || e != null && typeof e != "function")
    throw new TypeError(Gc);
  var n = function() {
    var r = arguments, i = e ? e.apply(this, r) : r[0], s = n.cache;
    if (s.has(i))
      return s.get(i);
    var o = t.apply(this, r);
    return n.cache = s.set(i, o) || s, o;
  };
  return n.cache = new (lr.Cache || Fe)(), n;
}
lr.Cache = Fe;
var Bc = 500;
function Vc(t) {
  var e = lr(t, function(r) {
    return n.size === Bc && n.clear(), r;
  }), n = e.cache;
  return e;
}
var Kc = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g, Hc = /\\(\\)?/g, Wc = Vc(function(t) {
  var e = [];
  return t.charCodeAt(0) === 46 && e.push(""), t.replace(Kc, function(n, r, i, s) {
    e.push(i ? s.replace(Hc, "$1") : r || n);
  }), e;
});
function zc(t) {
  return t == null ? "" : $i(t);
}
function mn(t, e) {
  return x(t) ? t : ur(t, e) ? [t] : Wc(zc(t));
}
function Lt(t) {
  if (typeof t == "string" || on(t))
    return t;
  var e = t + "";
  return e == "0" && 1 / t == -1 / 0 ? "-0" : e;
}
function hr(t, e) {
  e = mn(e, t);
  for (var n = 0, r = e.length; t != null && n < r; )
    t = t[Lt(e[n++])];
  return n && n == r ? t : void 0;
}
function Yc(t, e, n) {
  var r = t == null ? void 0 : hr(t, e);
  return r === void 0 ? n : r;
}
function fr(t, e) {
  for (var n = -1, r = e.length, i = t.length; ++n < r; )
    t[i + n] = e[n];
  return t;
}
var Vr = ue ? ue.isConcatSpreadable : void 0;
function Xc(t) {
  return x(t) || hn(t) || !!(Vr && t && t[Vr]);
}
function dr(t, e, n, r, i) {
  var s = -1, o = t.length;
  for (n || (n = Xc), i || (i = []); ++s < o; ) {
    var a = t[s];
    n(a) ? fr(i, a) : r || (i[i.length] = a);
  }
  return i;
}
function pe(t) {
  var e = t == null ? 0 : t.length;
  return e ? dr(t) : [];
}
var qi = zi(Object.getPrototypeOf, Object);
function Qi(t, e, n) {
  var r = -1, i = t.length;
  e < 0 && (e = -e > i ? 0 : i + e), n = n > i ? i : n, n < 0 && (n += i), i = e > n ? 0 : n - e >>> 0, e >>>= 0;
  for (var s = Array(i); ++r < i; )
    s[r] = t[r + e];
  return s;
}
function qc(t, e, n, r) {
  var i = -1, s = t == null ? 0 : t.length;
  for (r && s && (n = t[++i]); ++i < s; )
    n = e(n, t[i], i, t);
  return n;
}
function Qc() {
  this.__data__ = new $e(), this.size = 0;
}
function Zc(t) {
  var e = this.__data__, n = e.delete(t);
  return this.size = e.size, n;
}
function Jc(t) {
  return this.__data__.get(t);
}
function eu(t) {
  return this.__data__.has(t);
}
var tu = 200;
function nu(t, e) {
  var n = this.__data__;
  if (n instanceof $e) {
    var r = n.__data__;
    if (!Ot || r.length < tu - 1)
      return r.push([t, e]), this.size = ++n.size, this;
    n = this.__data__ = new Fe(r);
  }
  return n.set(t, e), this.size = n.size, this;
}
function Re(t) {
  var e = this.__data__ = new $e(t);
  this.size = e.size;
}
Re.prototype.clear = Qc;
Re.prototype.delete = Zc;
Re.prototype.get = Jc;
Re.prototype.has = eu;
Re.prototype.set = nu;
function ru(t, e) {
  return t && sr(e, he(e), t);
}
var Zi = typeof exports == "object" && exports && !exports.nodeType && exports, Kr = Zi && typeof module == "object" && module && !module.nodeType && module, iu = Kr && Kr.exports === Zi, Hr = iu ? Oe.Buffer : void 0, Wr = Hr ? Hr.allocUnsafe : void 0;
function su(t, e) {
  var n = t.length, r = Wr ? Wr(n) : new t.constructor(n);
  return t.copy(r), r;
}
function pr(t, e) {
  for (var n = -1, r = t == null ? 0 : t.length, i = 0, s = []; ++n < r; ) {
    var o = t[n];
    e(o, n, t) && (s[i++] = o);
  }
  return s;
}
function Ji() {
  return [];
}
var ou = Object.prototype, au = ou.propertyIsEnumerable, zr = Object.getOwnPropertySymbols, mr = zr ? function(t) {
  return t == null ? [] : (t = Object(t), pr(zr(t), function(e) {
    return au.call(t, e);
  }));
} : Ji;
function cu(t, e) {
  return sr(t, mr(t), e);
}
var uu = Object.getOwnPropertySymbols, lu = uu ? function(t) {
  for (var e = []; t; )
    fr(e, mr(t)), t = qi(t);
  return e;
} : Ji;
function es(t, e, n) {
  var r = e(t);
  return x(t) ? r : fr(r, n(t));
}
function $n(t) {
  return es(t, he, mr);
}
function hu(t) {
  return es(t, Xi, lu);
}
var Fn = et(Oe, "DataView"), jn = et(Oe, "Promise"), ot = et(Oe, "Set"), Yr = "[object Map]", fu = "[object Object]", Xr = "[object Promise]", qr = "[object Set]", Qr = "[object WeakMap]", Zr = "[object DataView]", du = Je(Fn), pu = Je(Ot), mu = Je(jn), gu = Je(ot), Eu = Je(Dn), ae = Ve;
(Fn && ae(new Fn(new ArrayBuffer(1))) != Zr || Ot && ae(new Ot()) != Yr || jn && ae(jn.resolve()) != Xr || ot && ae(new ot()) != qr || Dn && ae(new Dn()) != Qr) && (ae = function(t) {
  var e = Ve(t), n = e == fu ? t.constructor : void 0, r = n ? Je(n) : "";
  if (r)
    switch (r) {
      case du:
        return Zr;
      case pu:
        return Yr;
      case mu:
        return Xr;
      case gu:
        return qr;
      case Eu:
        return Qr;
    }
  return e;
});
var Tu = Object.prototype, yu = Tu.hasOwnProperty;
function Au(t) {
  var e = t.length, n = new t.constructor(e);
  return e && typeof t[0] == "string" && yu.call(t, "index") && (n.index = t.index, n.input = t.input), n;
}
var Xt = Oe.Uint8Array;
function _u(t) {
  var e = new t.constructor(t.byteLength);
  return new Xt(e).set(new Xt(t)), e;
}
function vu(t, e) {
  var n = t.buffer;
  return new t.constructor(n, t.byteOffset, t.byteLength);
}
var Ru = /\w*$/;
function Ou(t) {
  var e = new t.constructor(t.source, Ru.exec(t));
  return e.lastIndex = t.lastIndex, e;
}
var Jr = ue ? ue.prototype : void 0, ei = Jr ? Jr.valueOf : void 0;
function Su(t) {
  return ei ? Object(ei.call(t)) : {};
}
function Iu(t, e) {
  var n = t.buffer;
  return new t.constructor(n, t.byteOffset, t.length);
}
var Nu = "[object Boolean]", Lu = "[object Date]", ku = "[object Map]", Cu = "[object Number]", bu = "[object RegExp]", xu = "[object Set]", Mu = "[object String]", Pu = "[object Symbol]", wu = "[object ArrayBuffer]", Uu = "[object DataView]", Du = "[object Float32Array]", $u = "[object Float64Array]", Fu = "[object Int8Array]", ju = "[object Int16Array]", Gu = "[object Int32Array]", Bu = "[object Uint8Array]", Vu = "[object Uint8ClampedArray]", Ku = "[object Uint16Array]", Hu = "[object Uint32Array]";
function Wu(t, e, n) {
  var r = t.constructor;
  switch (e) {
    case wu:
      return _u(t);
    case Nu:
    case Lu:
      return new r(+t);
    case Uu:
      return vu(t);
    case Du:
    case $u:
    case Fu:
    case ju:
    case Gu:
    case Bu:
    case Vu:
    case Ku:
    case Hu:
      return Iu(t);
    case ku:
      return new r();
    case Cu:
    case Mu:
      return new r(t);
    case bu:
      return Ou(t);
    case xu:
      return new r();
    case Pu:
      return Su(t);
  }
}
function zu(t) {
  return typeof t.constructor == "function" && !Nt(t) ? sa(qi(t)) : {};
}
var Yu = "[object Map]";
function Xu(t) {
  return ge(t) && ae(t) == Yu;
}
var ti = Ge && Ge.isMap, qu = ti ? fn(ti) : Xu, Qu = "[object Set]";
function Zu(t) {
  return ge(t) && ae(t) == Qu;
}
var ni = Ge && Ge.isSet, Ju = ni ? fn(ni) : Zu, ts = "[object Arguments]", el = "[object Array]", tl = "[object Boolean]", nl = "[object Date]", rl = "[object Error]", ns = "[object Function]", il = "[object GeneratorFunction]", sl = "[object Map]", ol = "[object Number]", rs = "[object Object]", al = "[object RegExp]", cl = "[object Set]", ul = "[object String]", ll = "[object Symbol]", hl = "[object WeakMap]", fl = "[object ArrayBuffer]", dl = "[object DataView]", pl = "[object Float32Array]", ml = "[object Float64Array]", gl = "[object Int8Array]", El = "[object Int16Array]", Tl = "[object Int32Array]", yl = "[object Uint8Array]", Al = "[object Uint8ClampedArray]", _l = "[object Uint16Array]", vl = "[object Uint32Array]", U = {};
U[ts] = U[el] = U[fl] = U[dl] = U[tl] = U[nl] = U[pl] = U[ml] = U[gl] = U[El] = U[Tl] = U[sl] = U[ol] = U[rs] = U[al] = U[cl] = U[ul] = U[ll] = U[yl] = U[Al] = U[_l] = U[vl] = !0;
U[rl] = U[ns] = U[hl] = !1;
function Gt(t, e, n, r, i, s) {
  var o;
  if (o !== void 0)
    return o;
  if (!le(t))
    return t;
  var a = x(t);
  if (a)
    return o = Au(t), aa(t, o);
  var c = ae(t), u = c == ns || c == il;
  if (vt(t))
    return su(t);
  if (c == rs || c == ts || u && !i)
    return o = u ? {} : zu(t), cu(t, ru(o, t));
  if (!U[c])
    return i ? t : {};
  o = Wu(t, c), s || (s = new Re());
  var l = s.get(t);
  if (l)
    return l;
  s.set(t, o), Ju(t) ? t.forEach(function(A) {
    o.add(Gt(A, e, n, A, t, s));
  }) : qu(t) && t.forEach(function(A, d) {
    o.set(d, Gt(A, e, n, d, t, s));
  });
  var h = $n, f = a ? void 0 : h(t);
  return Fi(f || t, function(A, d) {
    f && (d = A, A = t[d]), ln(o, d, Gt(A, e, n, d, t, s));
  }), o;
}
var Rl = 4;
function Y(t) {
  return Gt(t, Rl);
}
function kt(t) {
  for (var e = -1, n = t == null ? 0 : t.length, r = 0, i = []; ++e < n; ) {
    var s = t[e];
    s && (i[r++] = s);
  }
  return i;
}
var Ol = "__lodash_hash_undefined__";
function Sl(t) {
  return this.__data__.set(t, Ol), this;
}
function Il(t) {
  return this.__data__.has(t);
}
function ct(t) {
  var e = -1, n = t == null ? 0 : t.length;
  for (this.__data__ = new Fe(); ++e < n; )
    this.add(t[e]);
}
ct.prototype.add = ct.prototype.push = Sl;
ct.prototype.has = Il;
function is(t, e) {
  for (var n = -1, r = t == null ? 0 : t.length; ++n < r; )
    if (e(t[n], n, t))
      return !0;
  return !1;
}
function gr(t, e) {
  return t.has(e);
}
var Nl = 1, Ll = 2;
function ss(t, e, n, r, i, s) {
  var o = n & Nl, a = t.length, c = e.length;
  if (a != c && !(o && c > a))
    return !1;
  var u = s.get(t), l = s.get(e);
  if (u && l)
    return u == e && l == t;
  var h = -1, f = !0, A = n & Ll ? new ct() : void 0;
  for (s.set(t, e), s.set(e, t); ++h < a; ) {
    var d = t[h], _ = e[h];
    if (r)
      var R = o ? r(_, d, h, e, t, s) : r(d, _, h, t, e, s);
    if (R !== void 0) {
      if (R)
        continue;
      f = !1;
      break;
    }
    if (A) {
      if (!is(e, function(m, T) {
        if (!gr(A, T) && (d === m || i(d, m, n, r, s)))
          return A.push(T);
      })) {
        f = !1;
        break;
      }
    } else if (!(d === _ || i(d, _, n, r, s))) {
      f = !1;
      break;
    }
  }
  return s.delete(t), s.delete(e), f;
}
function kl(t) {
  var e = -1, n = Array(t.size);
  return t.forEach(function(r, i) {
    n[++e] = [i, r];
  }), n;
}
function Er(t) {
  var e = -1, n = Array(t.size);
  return t.forEach(function(r) {
    n[++e] = r;
  }), n;
}
var Cl = 1, bl = 2, xl = "[object Boolean]", Ml = "[object Date]", Pl = "[object Error]", wl = "[object Map]", Ul = "[object Number]", Dl = "[object RegExp]", $l = "[object Set]", Fl = "[object String]", jl = "[object Symbol]", Gl = "[object ArrayBuffer]", Bl = "[object DataView]", ri = ue ? ue.prototype : void 0, Nn = ri ? ri.valueOf : void 0;
function Vl(t, e, n, r, i, s, o) {
  switch (n) {
    case Bl:
      if (t.byteLength != e.byteLength || t.byteOffset != e.byteOffset)
        return !1;
      t = t.buffer, e = e.buffer;
    case Gl:
      return !(t.byteLength != e.byteLength || !s(new Xt(t), new Xt(e)));
    case xl:
    case Ml:
    case Ul:
      return It(+t, +e);
    case Pl:
      return t.name == e.name && t.message == e.message;
    case Dl:
    case Fl:
      return t == e + "";
    case wl:
      var a = kl;
    case $l:
      var c = r & Cl;
      if (a || (a = Er), t.size != e.size && !c)
        return !1;
      var u = o.get(t);
      if (u)
        return u == e;
      r |= bl, o.set(t, e);
      var l = ss(a(t), a(e), r, i, s, o);
      return o.delete(t), l;
    case jl:
      if (Nn)
        return Nn.call(t) == Nn.call(e);
  }
  return !1;
}
var Kl = 1, Hl = Object.prototype, Wl = Hl.hasOwnProperty;
function zl(t, e, n, r, i, s) {
  var o = n & Kl, a = $n(t), c = a.length, u = $n(e), l = u.length;
  if (c != l && !o)
    return !1;
  for (var h = c; h--; ) {
    var f = a[h];
    if (!(o ? f in e : Wl.call(e, f)))
      return !1;
  }
  var A = s.get(t), d = s.get(e);
  if (A && d)
    return A == e && d == t;
  var _ = !0;
  s.set(t, e), s.set(e, t);
  for (var R = o; ++h < c; ) {
    f = a[h];
    var m = t[f], T = e[f];
    if (r)
      var p = o ? r(T, m, f, e, t, s) : r(m, T, f, t, e, s);
    if (!(p === void 0 ? m === T || i(m, T, n, r, s) : p)) {
      _ = !1;
      break;
    }
    R || (R = f == "constructor");
  }
  if (_ && !R) {
    var y = t.constructor, N = e.constructor;
    y != N && "constructor" in t && "constructor" in e && !(typeof y == "function" && y instanceof y && typeof N == "function" && N instanceof N) && (_ = !1);
  }
  return s.delete(t), s.delete(e), _;
}
var Yl = 1, ii = "[object Arguments]", si = "[object Array]", wt = "[object Object]", Xl = Object.prototype, oi = Xl.hasOwnProperty;
function ql(t, e, n, r, i, s) {
  var o = x(t), a = x(e), c = o ? si : ae(t), u = a ? si : ae(e);
  c = c == ii ? wt : c, u = u == ii ? wt : u;
  var l = c == wt, h = u == wt, f = c == u;
  if (f && vt(t)) {
    if (!vt(e))
      return !1;
    o = !0, l = !1;
  }
  if (f && !l)
    return s || (s = new Re()), o || cr(t) ? ss(t, e, n, r, i, s) : Vl(t, e, c, n, r, i, s);
  if (!(n & Yl)) {
    var A = l && oi.call(t, "__wrapped__"), d = h && oi.call(e, "__wrapped__");
    if (A || d) {
      var _ = A ? t.value() : t, R = d ? e.value() : e;
      return s || (s = new Re()), i(_, R, n, r, s);
    }
  }
  return f ? (s || (s = new Re()), zl(t, e, n, r, i, s)) : !1;
}
function Tr(t, e, n, r, i) {
  return t === e ? !0 : t == null || e == null || !ge(t) && !ge(e) ? t !== t && e !== e : ql(t, e, n, r, Tr, i);
}
var Ql = 1, Zl = 2;
function Jl(t, e, n, r) {
  var i = n.length, s = i;
  if (t == null)
    return !s;
  for (t = Object(t); i--; ) {
    var o = n[i];
    if (o[2] ? o[1] !== t[o[0]] : !(o[0] in t))
      return !1;
  }
  for (; ++i < s; ) {
    o = n[i];
    var a = o[0], c = t[a], u = o[1];
    if (o[2]) {
      if (c === void 0 && !(a in t))
        return !1;
    } else {
      var l = new Re(), h;
      if (!(h === void 0 ? Tr(u, c, Ql | Zl, r, l) : h))
        return !1;
    }
  }
  return !0;
}
function os(t) {
  return t === t && !le(t);
}
function eh(t) {
  for (var e = he(t), n = e.length; n--; ) {
    var r = e[n], i = t[r];
    e[n] = [r, i, os(i)];
  }
  return e;
}
function as(t, e) {
  return function(n) {
    return n == null ? !1 : n[t] === e && (e !== void 0 || t in Object(n));
  };
}
function th(t) {
  var e = eh(t);
  return e.length == 1 && e[0][2] ? as(e[0][0], e[0][1]) : function(n) {
    return n === t || Jl(n, t, e);
  };
}
function nh(t, e) {
  return t != null && e in Object(t);
}
function cs(t, e, n) {
  e = mn(e, t);
  for (var r = -1, i = e.length, s = !1; ++r < i; ) {
    var o = Lt(e[r]);
    if (!(s = t != null && n(t, o)))
      break;
    t = t[o];
  }
  return s || ++r != i ? s : (i = t == null ? 0 : t.length, !!i && ar(i) && un(o, i) && (x(t) || hn(t)));
}
function rh(t, e) {
  return t != null && cs(t, e, nh);
}
var ih = 1, sh = 2;
function oh(t, e) {
  return ur(t) && os(e) ? as(Lt(t), e) : function(n) {
    var r = Yc(n, t);
    return r === void 0 && r === e ? rh(n, t) : Tr(e, r, ih | sh);
  };
}
function ah(t) {
  return function(e) {
    return e?.[t];
  };
}
function ch(t) {
  return function(e) {
    return hr(e, t);
  };
}
function uh(t) {
  return ur(t) ? ah(Lt(t)) : ch(t);
}
function Ie(t) {
  return typeof t == "function" ? t : t == null ? at : typeof t == "object" ? x(t) ? oh(t[0], t[1]) : th(t) : uh(t);
}
function lh(t, e, n, r) {
  for (var i = -1, s = t == null ? 0 : t.length; ++i < s; ) {
    var o = t[i];
    e(r, o, n(o), t);
  }
  return r;
}
function hh(t) {
  return function(e, n, r) {
    for (var i = -1, s = Object(e), o = r(e), a = o.length; a--; ) {
      var c = o[++i];
      if (n(s[c], c, s) === !1)
        break;
    }
    return e;
  };
}
var fh = hh();
function dh(t, e) {
  return t && fh(t, e, he);
}
function ph(t, e) {
  return function(n, r) {
    if (n == null)
      return n;
    if (!Se(n))
      return t(n, r);
    for (var i = n.length, s = -1, o = Object(n); ++s < i && r(o[s], s, o) !== !1; )
      ;
    return n;
  };
}
var tt = ph(dh);
function mh(t, e, n, r) {
  return tt(t, function(i, s, o) {
    e(r, i, n(i), o);
  }), r;
}
function gh(t, e) {
  return function(n, r) {
    var i = x(n) ? lh : mh, s = {};
    return i(n, t, Ie(r), s);
  };
}
var us = Object.prototype, Eh = us.hasOwnProperty, yr = or(function(t, e) {
  t = Object(t);
  var n = -1, r = e.length, i = r > 2 ? e[2] : void 0;
  for (i && Bi(e[0], e[1], i) && (r = 1); ++n < r; )
    for (var s = e[n], o = Xi(s), a = -1, c = o.length; ++a < c; ) {
      var u = o[a], l = t[u];
      (l === void 0 || It(l, us[u]) && !Eh.call(t, u)) && (t[u] = s[u]);
    }
  return t;
});
function ai(t) {
  return ge(t) && Se(t);
}
var Th = 200;
function yh(t, e, n, r) {
  var i = -1, s = Gi, o = !0, a = t.length, c = [], u = e.length;
  if (!a)
    return c;
  e.length >= Th && (s = gr, o = !1, e = new ct(e));
  e:
    for (; ++i < a; ) {
      var l = t[i], h = l;
      if (l = l !== 0 ? l : 0, o && h === h) {
        for (var f = u; f--; )
          if (e[f] === h)
            continue e;
        c.push(l);
      } else s(e, h, r) || c.push(l);
    }
  return c;
}
var gn = or(function(t, e) {
  return ai(t) ? yh(t, dr(e, 1, ai, !0)) : [];
});
function ut(t) {
  var e = t == null ? 0 : t.length;
  return e ? t[e - 1] : void 0;
}
function z(t, e, n) {
  var r = t == null ? 0 : t.length;
  return r ? (e = e === void 0 ? 1 : cn(e), Qi(t, e < 0 ? 0 : e, r)) : [];
}
function St(t, e, n) {
  var r = t == null ? 0 : t.length;
  return r ? (e = e === void 0 ? 1 : cn(e), e = r - e, Qi(t, 0, e < 0 ? 0 : e)) : [];
}
function Ah(t) {
  return typeof t == "function" ? t : at;
}
function S(t, e) {
  var n = x(t) ? Fi : tt;
  return n(t, Ah(e));
}
function _h(t, e) {
  for (var n = -1, r = t == null ? 0 : t.length; ++n < r; )
    if (!e(t[n], n, t))
      return !1;
  return !0;
}
function vh(t, e) {
  var n = !0;
  return tt(t, function(r, i, s) {
    return n = !!e(r, i, s), n;
  }), n;
}
function me(t, e, n) {
  var r = x(t) ? _h : vh;
  return r(t, Ie(e));
}
function ls(t, e) {
  var n = [];
  return tt(t, function(r, i, s) {
    e(r, i, s) && n.push(r);
  }), n;
}
function fe(t, e) {
  var n = x(t) ? pr : ls;
  return n(t, Ie(e));
}
function Rh(t) {
  return function(e, n, r) {
    var i = Object(e);
    if (!Se(e)) {
      var s = Ie(n);
      e = he(e), n = function(a) {
        return s(i[a], a, i);
      };
    }
    var o = t(e, n, r);
    return o > -1 ? i[s ? e[o] : o] : void 0;
  };
}
var Oh = Math.max;
function Sh(t, e, n) {
  var r = t == null ? 0 : t.length;
  if (!r)
    return -1;
  var i = n == null ? 0 : cn(n);
  return i < 0 && (i = Oh(r + i, 0)), ji(t, Ie(e), i);
}
var lt = Rh(Sh);
function Ee(t) {
  return t && t.length ? t[0] : void 0;
}
function Ih(t, e) {
  var n = -1, r = Se(t) ? Array(t.length) : [];
  return tt(t, function(i, s, o) {
    r[++n] = e(i, s, o);
  }), r;
}
function v(t, e) {
  var n = x(t) ? an : Ih;
  return n(t, Ie(e));
}
function ce(t, e) {
  return dr(v(t, e));
}
var Nh = Object.prototype, Lh = Nh.hasOwnProperty, kh = gh(function(t, e, n) {
  Lh.call(t, n) ? t[n].push(e) : ir(t, n, [e]);
}), Ch = Object.prototype, bh = Ch.hasOwnProperty;
function xh(t, e) {
  return t != null && bh.call(t, e);
}
function O(t, e) {
  return t != null && cs(t, e, xh);
}
var Mh = "[object String]";
function ee(t) {
  return typeof t == "string" || !x(t) && ge(t) && Ve(t) == Mh;
}
function Ph(t, e) {
  return an(e, function(n) {
    return t[n];
  });
}
function H(t) {
  return t == null ? [] : Ph(t, he(t));
}
var wh = Math.max;
function Q(t, e, n, r) {
  t = Se(t) ? t : H(t), n = n ? cn(n) : 0;
  var i = t.length;
  return n < 0 && (n = wh(i + n, 0)), ee(t) ? n <= i && t.indexOf(e, n) > -1 : !!i && rr(t, e, n) > -1;
}
function ci(t, e, n) {
  var r = t == null ? 0 : t.length;
  if (!r)
    return -1;
  var i = 0;
  return rr(t, e, i);
}
var Uh = "[object Map]", Dh = "[object Set]", $h = Object.prototype, Fh = $h.hasOwnProperty;
function D(t) {
  if (t == null)
    return !0;
  if (Se(t) && (x(t) || typeof t == "string" || typeof t.splice == "function" || vt(t) || cr(t) || hn(t)))
    return !t.length;
  var e = ae(t);
  if (e == Uh || e == Dh)
    return !t.size;
  if (Nt(t))
    return !Yi(t).length;
  for (var n in t)
    if (Fh.call(t, n))
      return !1;
  return !0;
}
var jh = "[object RegExp]";
function Gh(t) {
  return ge(t) && Ve(t) == jh;
}
var ui = Ge && Ge.isRegExp, Pe = ui ? fn(ui) : Gh;
function we(t) {
  return t === void 0;
}
var Bh = "Expected a function";
function Vh(t) {
  if (typeof t != "function")
    throw new TypeError(Bh);
  return function() {
    var e = arguments;
    switch (e.length) {
      case 0:
        return !t.call(this);
      case 1:
        return !t.call(this, e[0]);
      case 2:
        return !t.call(this, e[0], e[1]);
      case 3:
        return !t.call(this, e[0], e[1], e[2]);
    }
    return !t.apply(this, e);
  };
}
function Kh(t, e, n, r) {
  if (!le(t))
    return t;
  e = mn(e, t);
  for (var i = -1, s = e.length, o = s - 1, a = t; a != null && ++i < s; ) {
    var c = Lt(e[i]), u = n;
    if (c === "__proto__" || c === "constructor" || c === "prototype")
      return t;
    if (i != o) {
      var l = a[c];
      u = void 0, u === void 0 && (u = le(l) ? l : un(e[i + 1]) ? [] : {});
    }
    ln(a, c, u), a = a[c];
  }
  return t;
}
function Hh(t, e, n) {
  for (var r = -1, i = e.length, s = {}; ++r < i; ) {
    var o = e[r], a = hr(t, o);
    n(a, o) && Kh(s, mn(o, t), a);
  }
  return s;
}
function Te(t, e) {
  if (t == null)
    return {};
  var n = an(hu(t), function(r) {
    return [r];
  });
  return e = Ie(e), Hh(t, n, function(r, i) {
    return e(r, i[0]);
  });
}
function Wh(t, e, n, r, i) {
  return i(t, function(s, o, a) {
    n = r ? (r = !1, s) : e(n, s, o, a);
  }), n;
}
function re(t, e, n) {
  var r = x(t) ? qc : Wh, i = arguments.length < 3;
  return r(t, Ie(e), n, i, tt);
}
function En(t, e) {
  var n = x(t) ? pr : ls;
  return n(t, Vh(Ie(e)));
}
function zh(t, e) {
  var n;
  return tt(t, function(r, i, s) {
    return n = e(r, i, s), !n;
  }), !!n;
}
function hs(t, e, n) {
  var r = x(t) ? is : zh;
  return r(t, Ie(e));
}
var Yh = 1 / 0, Xh = ot && 1 / Er(new ot([, -0]))[1] == Yh ? function(t) {
  return new ot(t);
} : K, qh = 200;
function Qh(t, e, n) {
  var r = -1, i = Gi, s = t.length, o = !0, a = [], c = a;
  if (s >= qh) {
    var u = Xh(t);
    if (u)
      return Er(u);
    o = !1, i = gr, c = new ct();
  } else
    c = a;
  e:
    for (; ++r < s; ) {
      var l = t[r], h = l;
      if (l = l !== 0 ? l : 0, o && h === h) {
        for (var f = c.length; f--; )
          if (c[f] === h)
            continue e;
        a.push(l);
      } else i(c, h, n) || (c !== a && c.push(h), a.push(l));
    }
  return a;
}
function Ar(t) {
  return t && t.length ? Qh(t) : [];
}
function Gn(t) {
  console && console.error && console.error(`Error: ${t}`);
}
function fs(t) {
  console && console.warn && console.warn(`Warning: ${t}`);
}
function ds(t) {
  const e = (/* @__PURE__ */ new Date()).getTime(), n = t();
  return { time: (/* @__PURE__ */ new Date()).getTime() - e, value: n };
}
function ps(t) {
  function e() {
  }
  e.prototype = t;
  const n = new e();
  function r() {
    return typeof n.bar;
  }
  return r(), r(), t;
}
function Zh(t) {
  return Jh(t) ? t.LABEL : t.name;
}
function Jh(t) {
  return ee(t.LABEL) && t.LABEL !== "";
}
class Ne {
  get definition() {
    return this._definition;
  }
  set definition(e) {
    this._definition = e;
  }
  constructor(e) {
    this._definition = e;
  }
  accept(e) {
    e.visit(this), S(this.definition, (n) => {
      n.accept(e);
    });
  }
}
class ie extends Ne {
  constructor(e) {
    super([]), this.idx = 1, ne(this, Te(e, (n) => n !== void 0));
  }
  set definition(e) {
  }
  get definition() {
    return this.referencedRule !== void 0 ? this.referencedRule.definition : [];
  }
  accept(e) {
    e.visit(this);
  }
}
class ht extends Ne {
  constructor(e) {
    super(e.definition), this.orgText = "", ne(this, Te(e, (n) => n !== void 0));
  }
}
class te extends Ne {
  constructor(e) {
    super(e.definition), this.ignoreAmbiguities = !1, ne(this, Te(e, (n) => n !== void 0));
  }
}
class J extends Ne {
  constructor(e) {
    super(e.definition), this.idx = 1, ne(this, Te(e, (n) => n !== void 0));
  }
}
class Le extends Ne {
  constructor(e) {
    super(e.definition), this.idx = 1, ne(this, Te(e, (n) => n !== void 0));
  }
}
class ke extends Ne {
  constructor(e) {
    super(e.definition), this.idx = 1, ne(this, Te(e, (n) => n !== void 0));
  }
}
class W extends Ne {
  constructor(e) {
    super(e.definition), this.idx = 1, ne(this, Te(e, (n) => n !== void 0));
  }
}
class ye extends Ne {
  constructor(e) {
    super(e.definition), this.idx = 1, ne(this, Te(e, (n) => n !== void 0));
  }
}
class Ae extends Ne {
  get definition() {
    return this._definition;
  }
  set definition(e) {
    this._definition = e;
  }
  constructor(e) {
    super(e.definition), this.idx = 1, this.ignoreAmbiguities = !1, this.hasPredicates = !1, ne(this, Te(e, (n) => n !== void 0));
  }
}
class j {
  constructor(e) {
    this.idx = 1, ne(this, Te(e, (n) => n !== void 0));
  }
  accept(e) {
    e.visit(this);
  }
}
function ef(t) {
  return v(t, Bt);
}
function Bt(t) {
  function e(n) {
    return v(n, Bt);
  }
  if (t instanceof ie) {
    const n = {
      type: "NonTerminal",
      name: t.nonTerminalName,
      idx: t.idx
    };
    return ee(t.label) && (n.label = t.label), n;
  } else {
    if (t instanceof te)
      return {
        type: "Alternative",
        definition: e(t.definition)
      };
    if (t instanceof J)
      return {
        type: "Option",
        idx: t.idx,
        definition: e(t.definition)
      };
    if (t instanceof Le)
      return {
        type: "RepetitionMandatory",
        idx: t.idx,
        definition: e(t.definition)
      };
    if (t instanceof ke)
      return {
        type: "RepetitionMandatoryWithSeparator",
        idx: t.idx,
        separator: Bt(new j({ terminalType: t.separator })),
        definition: e(t.definition)
      };
    if (t instanceof ye)
      return {
        type: "RepetitionWithSeparator",
        idx: t.idx,
        separator: Bt(new j({ terminalType: t.separator })),
        definition: e(t.definition)
      };
    if (t instanceof W)
      return {
        type: "Repetition",
        idx: t.idx,
        definition: e(t.definition)
      };
    if (t instanceof Ae)
      return {
        type: "Alternation",
        idx: t.idx,
        definition: e(t.definition)
      };
    if (t instanceof j) {
      const n = {
        type: "Terminal",
        name: t.terminalType.name,
        label: Zh(t.terminalType),
        idx: t.idx
      };
      ee(t.label) && (n.terminalLabel = t.label);
      const r = t.terminalType.PATTERN;
      return t.terminalType.PATTERN && (n.pattern = Pe(r) ? r.source : r), n;
    } else {
      if (t instanceof ht)
        return {
          type: "Rule",
          name: t.name,
          orgText: t.orgText,
          definition: e(t.definition)
        };
      throw Error("non exhaustive match");
    }
  }
}
class ft {
  visit(e) {
    const n = e;
    switch (n.constructor) {
      case ie:
        return this.visitNonTerminal(n);
      case te:
        return this.visitAlternative(n);
      case J:
        return this.visitOption(n);
      case Le:
        return this.visitRepetitionMandatory(n);
      case ke:
        return this.visitRepetitionMandatoryWithSeparator(n);
      case ye:
        return this.visitRepetitionWithSeparator(n);
      case W:
        return this.visitRepetition(n);
      case Ae:
        return this.visitAlternation(n);
      case j:
        return this.visitTerminal(n);
      case ht:
        return this.visitRule(n);
      /* c8 ignore next 2 */
      default:
        throw Error("non exhaustive match");
    }
  }
  /* c8 ignore next */
  visitNonTerminal(e) {
  }
  /* c8 ignore next */
  visitAlternative(e) {
  }
  /* c8 ignore next */
  visitOption(e) {
  }
  /* c8 ignore next */
  visitRepetition(e) {
  }
  /* c8 ignore next */
  visitRepetitionMandatory(e) {
  }
  /* c8 ignore next 3 */
  visitRepetitionMandatoryWithSeparator(e) {
  }
  /* c8 ignore next */
  visitRepetitionWithSeparator(e) {
  }
  /* c8 ignore next */
  visitAlternation(e) {
  }
  /* c8 ignore next */
  visitTerminal(e) {
  }
  /* c8 ignore next */
  visitRule(e) {
  }
}
function tf(t) {
  return t instanceof te || t instanceof J || t instanceof W || t instanceof Le || t instanceof ke || t instanceof ye || t instanceof j || t instanceof ht;
}
function qt(t, e = []) {
  return t instanceof J || t instanceof W || t instanceof ye ? !0 : t instanceof Ae ? hs(t.definition, (n) => qt(n, e)) : t instanceof ie && Q(e, t) ? !1 : t instanceof Ne ? (t instanceof ie && e.push(t), me(t.definition, (n) => qt(n, e))) : !1;
}
function nf(t) {
  return t instanceof Ae;
}
function ve(t) {
  if (t instanceof ie)
    return "SUBRULE";
  if (t instanceof J)
    return "OPTION";
  if (t instanceof Ae)
    return "OR";
  if (t instanceof Le)
    return "AT_LEAST_ONE";
  if (t instanceof ke)
    return "AT_LEAST_ONE_SEP";
  if (t instanceof ye)
    return "MANY_SEP";
  if (t instanceof W)
    return "MANY";
  if (t instanceof j)
    return "CONSUME";
  throw Error("non exhaustive match");
}
class Tn {
  walk(e, n = []) {
    S(e.definition, (r, i) => {
      const s = z(e.definition, i + 1);
      if (r instanceof ie)
        this.walkProdRef(r, s, n);
      else if (r instanceof j)
        this.walkTerminal(r, s, n);
      else if (r instanceof te)
        this.walkFlat(r, s, n);
      else if (r instanceof J)
        this.walkOption(r, s, n);
      else if (r instanceof Le)
        this.walkAtLeastOne(r, s, n);
      else if (r instanceof ke)
        this.walkAtLeastOneSep(r, s, n);
      else if (r instanceof ye)
        this.walkManySep(r, s, n);
      else if (r instanceof W)
        this.walkMany(r, s, n);
      else if (r instanceof Ae)
        this.walkOr(r, s, n);
      else
        throw Error("non exhaustive match");
    });
  }
  walkTerminal(e, n, r) {
  }
  walkProdRef(e, n, r) {
  }
  walkFlat(e, n, r) {
    const i = n.concat(r);
    this.walk(e, i);
  }
  walkOption(e, n, r) {
    const i = n.concat(r);
    this.walk(e, i);
  }
  walkAtLeastOne(e, n, r) {
    const i = [
      new J({ definition: e.definition })
    ].concat(n, r);
    this.walk(e, i);
  }
  walkAtLeastOneSep(e, n, r) {
    const i = li(e, n, r);
    this.walk(e, i);
  }
  walkMany(e, n, r) {
    const i = [
      new J({ definition: e.definition })
    ].concat(n, r);
    this.walk(e, i);
  }
  walkManySep(e, n, r) {
    const i = li(e, n, r);
    this.walk(e, i);
  }
  walkOr(e, n, r) {
    const i = n.concat(r);
    S(e.definition, (s) => {
      const o = new te({ definition: [s] });
      this.walk(o, i);
    });
  }
}
function li(t, e, n) {
  return [
    new J({
      definition: [
        new j({ terminalType: t.separator })
      ].concat(t.definition)
    })
  ].concat(e, n);
}
function Ct(t) {
  if (t instanceof ie)
    return Ct(t.referencedRule);
  if (t instanceof j)
    return of(t);
  if (tf(t))
    return rf(t);
  if (nf(t))
    return sf(t);
  throw Error("non exhaustive match");
}
function rf(t) {
  let e = [];
  const n = t.definition;
  let r = 0, i = n.length > r, s, o = !0;
  for (; i && o; )
    s = n[r], o = qt(s), e = e.concat(Ct(s)), r = r + 1, i = n.length > r;
  return Ar(e);
}
function sf(t) {
  const e = v(t.definition, (n) => Ct(n));
  return Ar(pe(e));
}
function of(t) {
  return [t.terminalType];
}
const ms = "_~IN~_";
class af extends Tn {
  constructor(e) {
    super(), this.topProd = e, this.follows = {};
  }
  startWalking() {
    return this.walk(this.topProd), this.follows;
  }
  walkTerminal(e, n, r) {
  }
  walkProdRef(e, n, r) {
    const i = uf(e.referencedRule, e.idx) + this.topProd.name, s = n.concat(r), o = new te({ definition: s }), a = Ct(o);
    this.follows[i] = a;
  }
}
function cf(t) {
  const e = {};
  return S(t, (n) => {
    const r = new af(n).startWalking();
    ne(e, r);
  }), e;
}
function uf(t, e) {
  return t.name + e + ms;
}
function I(t) {
  return t.charCodeAt(0);
}
function Ln(t, e) {
  Array.isArray(t) ? t.forEach(function(n) {
    e.push(n);
  }) : e.push(t);
}
function mt(t, e) {
  if (t[e] === !0)
    throw "duplicate flag " + e;
  t[e], t[e] = !0;
}
function st(t) {
  if (t === void 0)
    throw Error("Internal Error - Should never get here!");
  return !0;
}
function lf() {
  throw Error("Internal Error - Should never get here!");
}
function hi(t) {
  return t.type === "Character";
}
const Qt = [];
for (let t = I("0"); t <= I("9"); t++)
  Qt.push(t);
const Zt = [I("_")].concat(Qt);
for (let t = I("a"); t <= I("z"); t++)
  Zt.push(t);
for (let t = I("A"); t <= I("Z"); t++)
  Zt.push(t);
const fi = [
  I(" "),
  I("\f"),
  I(`
`),
  I("\r"),
  I("	"),
  I("\v"),
  I("	"),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I(" "),
  I("\u2028"),
  I("\u2029"),
  I(" "),
  I(" "),
  I("　"),
  I("\uFEFF")
], hf = /[0-9a-fA-F]/, Ut = /[0-9]/, ff = /[1-9]/;
class df {
  constructor() {
    this.idx = 0, this.input = "", this.groupIdx = 0;
  }
  saveState() {
    return {
      idx: this.idx,
      input: this.input,
      groupIdx: this.groupIdx
    };
  }
  restoreState(e) {
    this.idx = e.idx, this.input = e.input, this.groupIdx = e.groupIdx;
  }
  pattern(e) {
    this.idx = 0, this.input = e, this.groupIdx = 0, this.consumeChar("/");
    const n = this.disjunction();
    this.consumeChar("/");
    const r = {
      type: "Flags",
      loc: { begin: this.idx, end: e.length },
      global: !1,
      ignoreCase: !1,
      multiLine: !1,
      unicode: !1,
      sticky: !1
    };
    for (; this.isRegExpFlag(); )
      switch (this.popChar()) {
        case "g":
          mt(r, "global");
          break;
        case "i":
          mt(r, "ignoreCase");
          break;
        case "m":
          mt(r, "multiLine");
          break;
        case "u":
          mt(r, "unicode");
          break;
        case "y":
          mt(r, "sticky");
          break;
      }
    if (this.idx !== this.input.length)
      throw Error("Redundant input: " + this.input.substring(this.idx));
    return {
      type: "Pattern",
      flags: r,
      value: n,
      loc: this.loc(0)
    };
  }
  disjunction() {
    const e = [], n = this.idx;
    for (e.push(this.alternative()); this.peekChar() === "|"; )
      this.consumeChar("|"), e.push(this.alternative());
    return { type: "Disjunction", value: e, loc: this.loc(n) };
  }
  alternative() {
    const e = [], n = this.idx;
    for (; this.isTerm(); )
      e.push(this.term());
    return { type: "Alternative", value: e, loc: this.loc(n) };
  }
  term() {
    return this.isAssertion() ? this.assertion() : this.atom();
  }
  assertion() {
    const e = this.idx;
    switch (this.popChar()) {
      case "^":
        return {
          type: "StartAnchor",
          loc: this.loc(e)
        };
      case "$":
        return { type: "EndAnchor", loc: this.loc(e) };
      // '\b' or '\B'
      case "\\":
        switch (this.popChar()) {
          case "b":
            return {
              type: "WordBoundary",
              loc: this.loc(e)
            };
          case "B":
            return {
              type: "NonWordBoundary",
              loc: this.loc(e)
            };
        }
        throw Error("Invalid Assertion Escape");
      // '(?=' or '(?!'
      case "(":
        this.consumeChar("?");
        let n;
        switch (this.popChar()) {
          case "=":
            n = "Lookahead";
            break;
          case "!":
            n = "NegativeLookahead";
            break;
        }
        st(n);
        const r = this.disjunction();
        return this.consumeChar(")"), {
          type: n,
          value: r,
          loc: this.loc(e)
        };
    }
    return lf();
  }
  quantifier(e = !1) {
    let n;
    const r = this.idx;
    switch (this.popChar()) {
      case "*":
        n = {
          atLeast: 0,
          atMost: 1 / 0
        };
        break;
      case "+":
        n = {
          atLeast: 1,
          atMost: 1 / 0
        };
        break;
      case "?":
        n = {
          atLeast: 0,
          atMost: 1
        };
        break;
      case "{":
        const i = this.integerIncludingZero();
        switch (this.popChar()) {
          case "}":
            n = {
              atLeast: i,
              atMost: i
            };
            break;
          case ",":
            let s;
            this.isDigit() ? (s = this.integerIncludingZero(), n = {
              atLeast: i,
              atMost: s
            }) : n = {
              atLeast: i,
              atMost: 1 / 0
            }, this.consumeChar("}");
            break;
        }
        if (e === !0 && n === void 0)
          return;
        st(n);
        break;
    }
    if (!(e === !0 && n === void 0) && st(n))
      return this.peekChar(0) === "?" ? (this.consumeChar("?"), n.greedy = !1) : n.greedy = !0, n.type = "Quantifier", n.loc = this.loc(r), n;
  }
  atom() {
    let e;
    const n = this.idx;
    switch (this.peekChar()) {
      case ".":
        e = this.dotAll();
        break;
      case "\\":
        e = this.atomEscape();
        break;
      case "[":
        e = this.characterClass();
        break;
      case "(":
        e = this.group();
        break;
    }
    if (e === void 0 && this.isPatternCharacter() && (e = this.patternCharacter()), st(e))
      return e.loc = this.loc(n), this.isQuantifier() && (e.quantifier = this.quantifier()), e;
  }
  dotAll() {
    return this.consumeChar("."), {
      type: "Set",
      complement: !0,
      value: [I(`
`), I("\r"), I("\u2028"), I("\u2029")]
    };
  }
  atomEscape() {
    switch (this.consumeChar("\\"), this.peekChar()) {
      case "1":
      case "2":
      case "3":
      case "4":
      case "5":
      case "6":
      case "7":
      case "8":
      case "9":
        return this.decimalEscapeAtom();
      case "d":
      case "D":
      case "s":
      case "S":
      case "w":
      case "W":
        return this.characterClassEscape();
      case "f":
      case "n":
      case "r":
      case "t":
      case "v":
        return this.controlEscapeAtom();
      case "c":
        return this.controlLetterEscapeAtom();
      case "0":
        return this.nulCharacterAtom();
      case "x":
        return this.hexEscapeSequenceAtom();
      case "u":
        return this.regExpUnicodeEscapeSequenceAtom();
      default:
        return this.identityEscapeAtom();
    }
  }
  decimalEscapeAtom() {
    return { type: "GroupBackReference", value: this.positiveInteger() };
  }
  characterClassEscape() {
    let e, n = !1;
    switch (this.popChar()) {
      case "d":
        e = Qt;
        break;
      case "D":
        e = Qt, n = !0;
        break;
      case "s":
        e = fi;
        break;
      case "S":
        e = fi, n = !0;
        break;
      case "w":
        e = Zt;
        break;
      case "W":
        e = Zt, n = !0;
        break;
    }
    if (st(e))
      return { type: "Set", value: e, complement: n };
  }
  controlEscapeAtom() {
    let e;
    switch (this.popChar()) {
      case "f":
        e = I("\f");
        break;
      case "n":
        e = I(`
`);
        break;
      case "r":
        e = I("\r");
        break;
      case "t":
        e = I("	");
        break;
      case "v":
        e = I("\v");
        break;
    }
    if (st(e))
      return { type: "Character", value: e };
  }
  controlLetterEscapeAtom() {
    this.consumeChar("c");
    const e = this.popChar();
    if (/[a-zA-Z]/.test(e) === !1)
      throw Error("Invalid ");
    return { type: "Character", value: e.toUpperCase().charCodeAt(0) - 64 };
  }
  nulCharacterAtom() {
    return this.consumeChar("0"), { type: "Character", value: I("\0") };
  }
  hexEscapeSequenceAtom() {
    return this.consumeChar("x"), this.parseHexDigits(2);
  }
  regExpUnicodeEscapeSequenceAtom() {
    return this.consumeChar("u"), this.parseHexDigits(4);
  }
  identityEscapeAtom() {
    const e = this.popChar();
    return { type: "Character", value: I(e) };
  }
  classPatternCharacterAtom() {
    switch (this.peekChar()) {
      // istanbul ignore next
      case `
`:
      // istanbul ignore next
      case "\r":
      // istanbul ignore next
      case "\u2028":
      // istanbul ignore next
      case "\u2029":
      // istanbul ignore next
      case "\\":
      // istanbul ignore next
      case "]":
        throw Error("TBD");
      default:
        const e = this.popChar();
        return { type: "Character", value: I(e) };
    }
  }
  characterClass() {
    const e = [];
    let n = !1;
    for (this.consumeChar("["), this.peekChar(0) === "^" && (this.consumeChar("^"), n = !0); this.isClassAtom(); ) {
      const r = this.classAtom();
      if (r.type, hi(r) && this.isRangeDash()) {
        this.consumeChar("-");
        const i = this.classAtom();
        if (i.type, hi(i)) {
          if (i.value < r.value)
            throw Error("Range out of order in character class");
          e.push({ from: r.value, to: i.value });
        } else
          Ln(r.value, e), e.push(I("-")), Ln(i.value, e);
      } else
        Ln(r.value, e);
    }
    return this.consumeChar("]"), { type: "Set", complement: n, value: e };
  }
  classAtom() {
    switch (this.peekChar()) {
      // istanbul ignore next
      case "]":
      // istanbul ignore next
      case `
`:
      // istanbul ignore next
      case "\r":
      // istanbul ignore next
      case "\u2028":
      // istanbul ignore next
      case "\u2029":
        throw Error("TBD");
      case "\\":
        return this.classEscape();
      default:
        return this.classPatternCharacterAtom();
    }
  }
  classEscape() {
    switch (this.consumeChar("\\"), this.peekChar()) {
      // Matches a backspace.
      // (Not to be confused with \b word boundary outside characterClass)
      case "b":
        return this.consumeChar("b"), { type: "Character", value: I("\b") };
      case "d":
      case "D":
      case "s":
      case "S":
      case "w":
      case "W":
        return this.characterClassEscape();
      case "f":
      case "n":
      case "r":
      case "t":
      case "v":
        return this.controlEscapeAtom();
      case "c":
        return this.controlLetterEscapeAtom();
      case "0":
        return this.nulCharacterAtom();
      case "x":
        return this.hexEscapeSequenceAtom();
      case "u":
        return this.regExpUnicodeEscapeSequenceAtom();
      default:
        return this.identityEscapeAtom();
    }
  }
  group() {
    let e = !0;
    switch (this.consumeChar("("), this.peekChar(0)) {
      case "?":
        this.consumeChar("?"), this.consumeChar(":"), e = !1;
        break;
      default:
        this.groupIdx++;
        break;
    }
    const n = this.disjunction();
    this.consumeChar(")");
    const r = {
      type: "Group",
      capturing: e,
      value: n
    };
    return e && (r.idx = this.groupIdx), r;
  }
  positiveInteger() {
    let e = this.popChar();
    if (ff.test(e) === !1)
      throw Error("Expecting a positive integer");
    for (; Ut.test(this.peekChar(0)); )
      e += this.popChar();
    return parseInt(e, 10);
  }
  integerIncludingZero() {
    let e = this.popChar();
    if (Ut.test(e) === !1)
      throw Error("Expecting an integer");
    for (; Ut.test(this.peekChar(0)); )
      e += this.popChar();
    return parseInt(e, 10);
  }
  patternCharacter() {
    const e = this.popChar();
    switch (e) {
      // istanbul ignore next
      case `
`:
      // istanbul ignore next
      case "\r":
      // istanbul ignore next
      case "\u2028":
      // istanbul ignore next
      case "\u2029":
      // istanbul ignore next
      case "^":
      // istanbul ignore next
      case "$":
      // istanbul ignore next
      case "\\":
      // istanbul ignore next
      case ".":
      // istanbul ignore next
      case "*":
      // istanbul ignore next
      case "+":
      // istanbul ignore next
      case "?":
      // istanbul ignore next
      case "(":
      // istanbul ignore next
      case ")":
      // istanbul ignore next
      case "[":
      // istanbul ignore next
      case "|":
        throw Error("TBD");
      default:
        return { type: "Character", value: I(e) };
    }
  }
  isRegExpFlag() {
    switch (this.peekChar(0)) {
      case "g":
      case "i":
      case "m":
      case "u":
      case "y":
        return !0;
      default:
        return !1;
    }
  }
  isRangeDash() {
    return this.peekChar() === "-" && this.isClassAtom(1);
  }
  isDigit() {
    return Ut.test(this.peekChar(0));
  }
  isClassAtom(e = 0) {
    switch (this.peekChar(e)) {
      case "]":
      case `
`:
      case "\r":
      case "\u2028":
      case "\u2029":
        return !1;
      default:
        return !0;
    }
  }
  isTerm() {
    return this.isAtom() || this.isAssertion();
  }
  isAtom() {
    if (this.isPatternCharacter())
      return !0;
    switch (this.peekChar(0)) {
      case ".":
      case "\\":
      // atomEscape
      case "[":
      // characterClass
      // TODO: isAtom must be called before isAssertion - disambiguate
      case "(":
        return !0;
      default:
        return !1;
    }
  }
  isAssertion() {
    switch (this.peekChar(0)) {
      case "^":
      case "$":
        return !0;
      // '\b' or '\B'
      case "\\":
        switch (this.peekChar(1)) {
          case "b":
          case "B":
            return !0;
          default:
            return !1;
        }
      // '(?=' or '(?!'
      case "(":
        return this.peekChar(1) === "?" && (this.peekChar(2) === "=" || this.peekChar(2) === "!");
      default:
        return !1;
    }
  }
  isQuantifier() {
    const e = this.saveState();
    try {
      return this.quantifier(!0) !== void 0;
    } catch {
      return !1;
    } finally {
      this.restoreState(e);
    }
  }
  isPatternCharacter() {
    switch (this.peekChar()) {
      case "^":
      case "$":
      case "\\":
      case ".":
      case "*":
      case "+":
      case "?":
      case "(":
      case ")":
      case "[":
      case "|":
      case "/":
      case `
`:
      case "\r":
      case "\u2028":
      case "\u2029":
        return !1;
      default:
        return !0;
    }
  }
  parseHexDigits(e) {
    let n = "";
    for (let r = 0; r < e; r++) {
      const i = this.popChar();
      if (hf.test(i) === !1)
        throw Error("Expecting a HexDecimal digits");
      n += i;
    }
    return { type: "Character", value: parseInt(n, 16) };
  }
  peekChar(e = 0) {
    return this.input[this.idx + e];
  }
  popChar() {
    const e = this.peekChar(0);
    return this.consumeChar(void 0), e;
  }
  consumeChar(e) {
    if (e !== void 0 && this.input[this.idx] !== e)
      throw Error("Expected: '" + e + "' but found: '" + this.input[this.idx] + "' at offset: " + this.idx);
    if (this.idx >= this.input.length)
      throw Error("Unexpected end of input");
    this.idx++;
  }
  loc(e) {
    return { begin: e, end: this.idx };
  }
}
class _r {
  visitChildren(e) {
    for (const n in e) {
      const r = e[n];
      e.hasOwnProperty(n) && (r.type !== void 0 ? this.visit(r) : Array.isArray(r) && r.forEach((i) => {
        this.visit(i);
      }, this));
    }
  }
  visit(e) {
    switch (e.type) {
      case "Pattern":
        this.visitPattern(e);
        break;
      case "Flags":
        this.visitFlags(e);
        break;
      case "Disjunction":
        this.visitDisjunction(e);
        break;
      case "Alternative":
        this.visitAlternative(e);
        break;
      case "StartAnchor":
        this.visitStartAnchor(e);
        break;
      case "EndAnchor":
        this.visitEndAnchor(e);
        break;
      case "WordBoundary":
        this.visitWordBoundary(e);
        break;
      case "NonWordBoundary":
        this.visitNonWordBoundary(e);
        break;
      case "Lookahead":
        this.visitLookahead(e);
        break;
      case "NegativeLookahead":
        this.visitNegativeLookahead(e);
        break;
      case "Character":
        this.visitCharacter(e);
        break;
      case "Set":
        this.visitSet(e);
        break;
      case "Group":
        this.visitGroup(e);
        break;
      case "GroupBackReference":
        this.visitGroupBackReference(e);
        break;
      case "Quantifier":
        this.visitQuantifier(e);
        break;
    }
    this.visitChildren(e);
  }
  visitPattern(e) {
  }
  visitFlags(e) {
  }
  visitDisjunction(e) {
  }
  visitAlternative(e) {
  }
  // Assertion
  visitStartAnchor(e) {
  }
  visitEndAnchor(e) {
  }
  visitWordBoundary(e) {
  }
  visitNonWordBoundary(e) {
  }
  visitLookahead(e) {
  }
  visitNegativeLookahead(e) {
  }
  // atoms
  visitCharacter(e) {
  }
  visitSet(e) {
  }
  visitGroup(e) {
  }
  visitGroupBackReference(e) {
  }
  visitQuantifier(e) {
  }
}
let Vt = {};
const pf = new df();
function yn(t) {
  const e = t.toString();
  if (Vt.hasOwnProperty(e))
    return Vt[e];
  {
    const n = pf.pattern(e);
    return Vt[e] = n, n;
  }
}
function mf() {
  Vt = {};
}
const gs = "Complement Sets are not supported for first char optimization", Jt = `Unable to use "first char" lexer optimizations:
`;
function gf(t, e = !1) {
  try {
    const n = yn(t);
    return Bn(n.value, {}, n.flags.ignoreCase);
  } catch (n) {
    if (n.message === gs)
      e && fs(`${Jt}	Unable to optimize: < ${t.toString()} >
	Complement Sets cannot be automatically optimized.
	This will disable the lexer's first char optimizations.
	See: https://chevrotain.io/docs/guide/resolving_lexer_errors.html#COMPLEMENT for details.`);
    else {
      let r = "";
      e && (r = `
	This will disable the lexer's first char optimizations.
	See: https://chevrotain.io/docs/guide/resolving_lexer_errors.html#REGEXP_PARSING for details.`), Gn(`${Jt}
	Failed parsing: < ${t.toString()} >
	Using the @chevrotain/regexp-to-ast library
	Please open an issue at: https://github.com/chevrotain/chevrotain/issues` + r);
    }
  }
  return [];
}
function Bn(t, e, n) {
  switch (t.type) {
    case "Disjunction":
      for (let i = 0; i < t.value.length; i++)
        Bn(t.value[i], e, n);
      break;
    case "Alternative":
      const r = t.value;
      for (let i = 0; i < r.length; i++) {
        const s = r[i];
        switch (s.type) {
          case "EndAnchor":
          // A group back reference cannot affect potential starting char.
          // because if a back reference is the first production than automatically
          // the group being referenced has had to come BEFORE so its codes have already been added
          case "GroupBackReference":
          // assertions do not affect potential starting codes
          case "Lookahead":
          case "NegativeLookahead":
          case "StartAnchor":
          case "WordBoundary":
          case "NonWordBoundary":
            continue;
        }
        const o = s;
        switch (o.type) {
          case "Character":
            Dt(o.value, e, n);
            break;
          case "Set":
            if (o.complement === !0)
              throw Error(gs);
            S(o.value, (c) => {
              if (typeof c == "number")
                Dt(c, e, n);
              else {
                const u = c;
                if (n === !0)
                  for (let l = u.from; l <= u.to; l++)
                    Dt(l, e, n);
                else {
                  for (let l = u.from; l <= u.to && l < Tt; l++)
                    Dt(l, e, n);
                  if (u.to >= Tt) {
                    const l = u.from >= Tt ? u.from : Tt, h = u.to, f = Be(l), A = Be(h);
                    for (let d = f; d <= A; d++)
                      e[d] = d;
                  }
                }
              }
            });
            break;
          case "Group":
            Bn(o.value, e, n);
            break;
          /* istanbul ignore next */
          default:
            throw Error("Non Exhaustive Match");
        }
        const a = o.quantifier !== void 0 && o.quantifier.atLeast === 0;
        if (
          // A group may be optional due to empty contents /(?:)/
          // or if everything inside it is optional /((a)?)/
          o.type === "Group" && Vn(o) === !1 || // If this term is not a group it may only be optional if it has an optional quantifier
          o.type !== "Group" && a === !1
        )
          break;
      }
      break;
    /* istanbul ignore next */
    default:
      throw Error("non exhaustive match!");
  }
  return H(e);
}
function Dt(t, e, n) {
  const r = Be(t);
  e[r] = r, n === !0 && Ef(t, e);
}
function Ef(t, e) {
  const n = String.fromCharCode(t), r = n.toUpperCase();
  if (r !== n) {
    const i = Be(r.charCodeAt(0));
    e[i] = i;
  } else {
    const i = n.toLowerCase();
    if (i !== n) {
      const s = Be(i.charCodeAt(0));
      e[s] = s;
    }
  }
}
function di(t, e) {
  return lt(t.value, (n) => {
    if (typeof n == "number")
      return Q(e, n);
    {
      const r = n;
      return lt(e, (i) => r.from <= i && i <= r.to) !== void 0;
    }
  });
}
function Vn(t) {
  const e = t.quantifier;
  return e && e.atLeast === 0 ? !0 : t.value ? x(t.value) ? me(t.value, Vn) : Vn(t.value) : !1;
}
class Tf extends _r {
  constructor(e) {
    super(), this.targetCharCodes = e, this.found = !1;
  }
  visitChildren(e) {
    if (this.found !== !0) {
      switch (e.type) {
        case "Lookahead":
          this.visitLookahead(e);
          return;
        case "NegativeLookahead":
          this.visitNegativeLookahead(e);
          return;
      }
      super.visitChildren(e);
    }
  }
  visitCharacter(e) {
    Q(this.targetCharCodes, e.value) && (this.found = !0);
  }
  visitSet(e) {
    e.complement ? di(e, this.targetCharCodes) === void 0 && (this.found = !0) : di(e, this.targetCharCodes) !== void 0 && (this.found = !0);
  }
}
function vr(t, e) {
  if (e instanceof RegExp) {
    const n = yn(e), r = new Tf(t);
    return r.visit(n), r.found;
  } else
    return lt(e, (n) => Q(t, n.charCodeAt(0))) !== void 0;
}
const Qe = "PATTERN", Et = "defaultMode", $t = "modes";
let Es = typeof new RegExp("(?:)").sticky == "boolean";
function yf(t, e) {
  e = yr(e, {
    useSticky: Es,
    debug: !1,
    safeMode: !1,
    positionTracking: "full",
    lineTerminatorCharacters: ["\r", `
`],
    tracer: (T, p) => p()
  });
  const n = e.tracer;
  n("initCharCodeToOptimizedIndexMap", () => {
    Bf();
  });
  let r;
  n("Reject Lexer.NA", () => {
    r = En(t, (T) => T[Qe] === X.NA);
  });
  let i = !1, s;
  n("Transform Patterns", () => {
    i = !1, s = v(r, (T) => {
      const p = T[Qe];
      if (Pe(p)) {
        const y = p.source;
        return y.length === 1 && // only these regExp meta characters which can appear in a length one regExp
        y !== "^" && y !== "$" && y !== "." && !p.ignoreCase ? y : y.length === 2 && y[0] === "\\" && // not a meta character
        !Q([
          "d",
          "D",
          "s",
          "S",
          "t",
          "r",
          "n",
          "t",
          "0",
          "c",
          "b",
          "B",
          "f",
          "v",
          "w",
          "W"
        ], y[1]) ? y[1] : e.useSticky ? mi(p) : pi(p);
      } else {
        if (De(p))
          return i = !0, { exec: p };
        if (typeof p == "object")
          return i = !0, p;
        if (typeof p == "string") {
          if (p.length === 1)
            return p;
          {
            const y = p.replace(/[\\^$.*+?()[\]{}|]/g, "\\$&"), N = new RegExp(y);
            return e.useSticky ? mi(N) : pi(N);
          }
        } else
          throw Error("non exhaustive match");
      }
    });
  });
  let o, a, c, u, l;
  n("misc mapping", () => {
    o = v(r, (T) => T.tokenTypeIdx), a = v(r, (T) => {
      const p = T.GROUP;
      if (p !== X.SKIPPED) {
        if (ee(p))
          return p;
        if (we(p))
          return !1;
        throw Error("non exhaustive match");
      }
    }), c = v(r, (T) => {
      const p = T.LONGER_ALT;
      if (p)
        return x(p) ? v(p, (y) => ci(r, y)) : [ci(r, p)];
    }), u = v(r, (T) => T.PUSH_MODE), l = v(r, (T) => O(T, "POP_MODE"));
  });
  let h;
  n("Line Terminator Handling", () => {
    const T = As(e.lineTerminatorCharacters);
    h = v(r, (p) => !1), e.positionTracking !== "onlyOffset" && (h = v(r, (p) => O(p, "LINE_BREAKS") ? !!p.LINE_BREAKS : ys(p, T) === !1 && vr(T, p.PATTERN)));
  });
  let f, A, d, _;
  n("Misc Mapping #2", () => {
    f = v(r, Ts), A = v(s, Ff), d = re(r, (T, p) => {
      const y = p.GROUP;
      return ee(y) && y !== X.SKIPPED && (T[y] = []), T;
    }, {}), _ = v(s, (T, p) => ({
      pattern: s[p],
      longerAlt: c[p],
      canLineTerminator: h[p],
      isCustom: f[p],
      short: A[p],
      group: a[p],
      push: u[p],
      pop: l[p],
      tokenTypeIdx: o[p],
      tokenType: r[p]
    }));
  });
  let R = !0, m = [];
  return e.safeMode || n("First Char Optimization", () => {
    m = re(r, (T, p, y) => {
      if (typeof p.PATTERN == "string") {
        const N = p.PATTERN.charCodeAt(0), L = Be(N);
        kn(T, L, _[y]);
      } else if (x(p.START_CHARS_HINT)) {
        let N;
        S(p.START_CHARS_HINT, (L) => {
          const P = typeof L == "string" ? L.charCodeAt(0) : L, se = Be(P);
          N !== se && (N = se, kn(T, se, _[y]));
        });
      } else if (Pe(p.PATTERN))
        if (p.PATTERN.unicode)
          R = !1, e.ensureOptimizations && Gn(`${Jt}	Unable to analyze < ${p.PATTERN.toString()} > pattern.
	The regexp unicode flag is not currently supported by the regexp-to-ast library.
	This will disable the lexer's first char optimizations.
	For details See: https://chevrotain.io/docs/guide/resolving_lexer_errors.html#UNICODE_OPTIMIZE`);
        else {
          const N = gf(p.PATTERN, e.ensureOptimizations);
          D(N) && (R = !1), S(N, (L) => {
            kn(T, L, _[y]);
          });
        }
      else
        e.ensureOptimizations && Gn(`${Jt}	TokenType: <${p.name}> is using a custom token pattern without providing <start_chars_hint> parameter.
	This will disable the lexer's first char optimizations.
	For details See: https://chevrotain.io/docs/guide/resolving_lexer_errors.html#CUSTOM_OPTIMIZE`), R = !1;
      return T;
    }, []);
  }), {
    emptyGroups: d,
    patternIdxToConfig: _,
    charCodeToPatternIdxToConfig: m,
    hasCustom: i,
    canBeOptimized: R
  };
}
function Af(t, e) {
  let n = [];
  const r = vf(t);
  n = n.concat(r.errors);
  const i = Rf(r.valid), s = i.valid;
  return n = n.concat(i.errors), n = n.concat(_f(s)), n = n.concat(bf(s)), n = n.concat(xf(s, e)), n = n.concat(Mf(s)), n;
}
function _f(t) {
  let e = [];
  const n = fe(t, (r) => Pe(r[Qe]));
  return e = e.concat(Sf(n)), e = e.concat(Lf(n)), e = e.concat(kf(n)), e = e.concat(Cf(n)), e = e.concat(If(n)), e;
}
function vf(t) {
  const e = fe(t, (i) => !O(i, Qe)), n = v(e, (i) => ({
    message: "Token Type: ->" + i.name + "<- missing static 'PATTERN' property",
    type: B.MISSING_PATTERN,
    tokenTypes: [i]
  })), r = gn(t, e);
  return { errors: n, valid: r };
}
function Rf(t) {
  const e = fe(t, (i) => {
    const s = i[Qe];
    return !Pe(s) && !De(s) && !O(s, "exec") && !ee(s);
  }), n = v(e, (i) => ({
    message: "Token Type: ->" + i.name + "<- static 'PATTERN' can only be a RegExp, a Function matching the {CustomPatternMatcherFunc} type or an Object matching the {ICustomPattern} interface.",
    type: B.INVALID_PATTERN,
    tokenTypes: [i]
  })), r = gn(t, e);
  return { errors: n, valid: r };
}
const Of = /[^\\][$]/;
function Sf(t) {
  class e extends _r {
    constructor() {
      super(...arguments), this.found = !1;
    }
    visitEndAnchor(i) {
      this.found = !0;
    }
  }
  const n = fe(t, (r) => {
    const i = r.PATTERN;
    try {
      const s = yn(i), o = new e();
      return o.visit(s), o.found;
    } catch {
      return Of.test(i.source);
    }
  });
  return v(n, (r) => ({
    message: `Unexpected RegExp Anchor Error:
	Token Type: ->` + r.name + `<- static 'PATTERN' cannot contain end of input anchor '$'
	See chevrotain.io/docs/guide/resolving_lexer_errors.html#ANCHORS	for details.`,
    type: B.EOI_ANCHOR_FOUND,
    tokenTypes: [r]
  }));
}
function If(t) {
  const e = fe(t, (n) => n.PATTERN.test(""));
  return v(e, (n) => ({
    message: "Token Type: ->" + n.name + "<- static 'PATTERN' must not match an empty string",
    type: B.EMPTY_MATCH_PATTERN,
    tokenTypes: [n]
  }));
}
const Nf = /[^\\[][\^]|^\^/;
function Lf(t) {
  class e extends _r {
    constructor() {
      super(...arguments), this.found = !1;
    }
    visitStartAnchor(i) {
      this.found = !0;
    }
  }
  const n = fe(t, (r) => {
    const i = r.PATTERN;
    try {
      const s = yn(i), o = new e();
      return o.visit(s), o.found;
    } catch {
      return Nf.test(i.source);
    }
  });
  return v(n, (r) => ({
    message: `Unexpected RegExp Anchor Error:
	Token Type: ->` + r.name + `<- static 'PATTERN' cannot contain start of input anchor '^'
	See https://chevrotain.io/docs/guide/resolving_lexer_errors.html#ANCHORS	for details.`,
    type: B.SOI_ANCHOR_FOUND,
    tokenTypes: [r]
  }));
}
function kf(t) {
  const e = fe(t, (n) => {
    const r = n[Qe];
    return r instanceof RegExp && (r.multiline || r.global);
  });
  return v(e, (n) => ({
    message: "Token Type: ->" + n.name + "<- static 'PATTERN' may NOT contain global('g') or multiline('m')",
    type: B.UNSUPPORTED_FLAGS_FOUND,
    tokenTypes: [n]
  }));
}
function Cf(t) {
  const e = [];
  let n = v(t, (i) => re(t, (s, o) => (i.PATTERN.source === o.PATTERN.source && !Q(e, o) && o.PATTERN !== X.NA && (e.push(o), s.push(o)), s), []));
  n = kt(n);
  const r = fe(n, (i) => i.length > 1);
  return v(r, (i) => {
    const s = v(i, (o) => o.name);
    return {
      message: `The same RegExp pattern ->${Ee(i).PATTERN}<-has been used in all of the following Token Types: ${s.join(", ")} <-`,
      type: B.DUPLICATE_PATTERNS_FOUND,
      tokenTypes: i
    };
  });
}
function bf(t) {
  const e = fe(t, (n) => {
    if (!O(n, "GROUP"))
      return !1;
    const r = n.GROUP;
    return r !== X.SKIPPED && r !== X.NA && !ee(r);
  });
  return v(e, (n) => ({
    message: "Token Type: ->" + n.name + "<- static 'GROUP' can only be Lexer.SKIPPED/Lexer.NA/A String",
    type: B.INVALID_GROUP_TYPE_FOUND,
    tokenTypes: [n]
  }));
}
function xf(t, e) {
  const n = fe(t, (r) => r.PUSH_MODE !== void 0 && !Q(e, r.PUSH_MODE));
  return v(n, (r) => ({
    message: `Token Type: ->${r.name}<- static 'PUSH_MODE' value cannot refer to a Lexer Mode ->${r.PUSH_MODE}<-which does not exist`,
    type: B.PUSH_MODE_DOES_NOT_EXIST,
    tokenTypes: [r]
  }));
}
function Mf(t) {
  const e = [], n = re(t, (r, i, s) => {
    const o = i.PATTERN;
    return o === X.NA || (ee(o) ? r.push({ str: o, idx: s, tokenType: i }) : Pe(o) && wf(o) && r.push({ str: o.source, idx: s, tokenType: i })), r;
  }, []);
  return S(t, (r, i) => {
    S(n, ({ str: s, idx: o, tokenType: a }) => {
      if (i < o && Pf(s, r.PATTERN)) {
        const c = `Token: ->${a.name}<- can never be matched.
Because it appears AFTER the Token Type ->${r.name}<-in the lexer's definition.
See https://chevrotain.io/docs/guide/resolving_lexer_errors.html#UNREACHABLE`;
        e.push({
          message: c,
          type: B.UNREACHABLE_PATTERN,
          tokenTypes: [r, a]
        });
      }
    });
  }), e;
}
function Pf(t, e) {
  if (Pe(e)) {
    const n = e.exec(t);
    return n !== null && n.index === 0;
  } else {
    if (De(e))
      return e(t, 0, [], {});
    if (O(e, "exec"))
      return e.exec(t, 0, [], {});
    if (typeof e == "string")
      return e === t;
    throw Error("non exhaustive match");
  }
}
function wf(t) {
  return lt([
    ".",
    "\\",
    "[",
    "]",
    "|",
    "^",
    "$",
    "(",
    ")",
    "?",
    "*",
    "+",
    "{"
  ], (e) => t.source.indexOf(e) !== -1) === void 0;
}
function pi(t) {
  const e = t.ignoreCase ? "i" : "";
  return new RegExp(`^(?:${t.source})`, e);
}
function mi(t) {
  const e = t.ignoreCase ? "iy" : "y";
  return new RegExp(`${t.source}`, e);
}
function Uf(t, e, n) {
  const r = [];
  return O(t, Et) || r.push({
    message: "A MultiMode Lexer cannot be initialized without a <" + Et + `> property in its definition
`,
    type: B.MULTI_MODE_LEXER_WITHOUT_DEFAULT_MODE
  }), O(t, $t) || r.push({
    message: "A MultiMode Lexer cannot be initialized without a <" + $t + `> property in its definition
`,
    type: B.MULTI_MODE_LEXER_WITHOUT_MODES_PROPERTY
  }), O(t, $t) && O(t, Et) && !O(t.modes, t.defaultMode) && r.push({
    message: `A MultiMode Lexer cannot be initialized with a ${Et}: <${t.defaultMode}>which does not exist
`,
    type: B.MULTI_MODE_LEXER_DEFAULT_MODE_VALUE_DOES_NOT_EXIST
  }), O(t, $t) && S(t.modes, (i, s) => {
    S(i, (o, a) => {
      if (we(o))
        r.push({
          message: `A Lexer cannot be initialized using an undefined Token Type. Mode:<${s}> at index: <${a}>
`,
          type: B.LEXER_DEFINITION_CANNOT_CONTAIN_UNDEFINED
        });
      else if (O(o, "LONGER_ALT")) {
        const c = x(o.LONGER_ALT) ? o.LONGER_ALT : [o.LONGER_ALT];
        S(c, (u) => {
          !we(u) && !Q(i, u) && r.push({
            message: `A MultiMode Lexer cannot be initialized with a longer_alt <${u.name}> on token <${o.name}> outside of mode <${s}>
`,
            type: B.MULTI_MODE_LEXER_LONGER_ALT_NOT_IN_CURRENT_MODE
          });
        });
      }
    });
  }), r;
}
function Df(t, e, n) {
  const r = [];
  let i = !1;
  const s = kt(pe(H(t.modes))), o = En(s, (c) => c[Qe] === X.NA), a = As(n);
  return e && S(o, (c) => {
    const u = ys(c, a);
    if (u !== !1) {
      const l = {
        message: Gf(c, u),
        type: u.issue,
        tokenType: c
      };
      r.push(l);
    } else
      O(c, "LINE_BREAKS") ? c.LINE_BREAKS === !0 && (i = !0) : vr(a, c.PATTERN) && (i = !0);
  }), e && !i && r.push({
    message: `Warning: No LINE_BREAKS Found.
	This Lexer has been defined to track line and column information,
	But none of the Token Types can be identified as matching a line terminator.
	See https://chevrotain.io/docs/guide/resolving_lexer_errors.html#LINE_BREAKS 
	for details.`,
    type: B.NO_LINE_BREAKS_FLAGS
  }), r;
}
function $f(t) {
  const e = {}, n = he(t);
  return S(n, (r) => {
    const i = t[r];
    if (x(i))
      e[r] = [];
    else
      throw Error("non exhaustive match");
  }), e;
}
function Ts(t) {
  const e = t.PATTERN;
  if (Pe(e))
    return !1;
  if (De(e) || O(e, "exec"))
    return !0;
  if (ee(e))
    return !1;
  throw Error("non exhaustive match");
}
function Ff(t) {
  return ee(t) && t.length === 1 ? t.charCodeAt(0) : !1;
}
const jf = {
  // implements /\n|\r\n?/g.test
  test: function(t) {
    const e = t.length;
    for (let n = this.lastIndex; n < e; n++) {
      const r = t.charCodeAt(n);
      if (r === 10)
        return this.lastIndex = n + 1, !0;
      if (r === 13)
        return t.charCodeAt(n + 1) === 10 ? this.lastIndex = n + 2 : this.lastIndex = n + 1, !0;
    }
    return !1;
  },
  lastIndex: 0
};
function ys(t, e) {
  if (O(t, "LINE_BREAKS"))
    return !1;
  if (Pe(t.PATTERN)) {
    try {
      vr(e, t.PATTERN);
    } catch (n) {
      return {
        issue: B.IDENTIFY_TERMINATOR,
        errMsg: n.message
      };
    }
    return !1;
  } else {
    if (ee(t.PATTERN))
      return !1;
    if (Ts(t))
      return { issue: B.CUSTOM_LINE_BREAK };
    throw Error("non exhaustive match");
  }
}
function Gf(t, e) {
  if (e.issue === B.IDENTIFY_TERMINATOR)
    return `Warning: unable to identify line terminator usage in pattern.
	The problem is in the <${t.name}> Token Type
	 Root cause: ${e.errMsg}.
	For details See: https://chevrotain.io/docs/guide/resolving_lexer_errors.html#IDENTIFY_TERMINATOR`;
  if (e.issue === B.CUSTOM_LINE_BREAK)
    return `Warning: A Custom Token Pattern should specify the <line_breaks> option.
	The problem is in the <${t.name}> Token Type
	For details See: https://chevrotain.io/docs/guide/resolving_lexer_errors.html#CUSTOM_LINE_BREAK`;
  throw Error("non exhaustive match");
}
function As(t) {
  return v(t, (e) => ee(e) ? e.charCodeAt(0) : e);
}
function kn(t, e, n) {
  t[e] === void 0 ? t[e] = [n] : t[e].push(n);
}
const Tt = 256;
let Kt = [];
function Be(t) {
  return t < Tt ? t : Kt[t];
}
function Bf() {
  if (D(Kt)) {
    Kt = new Array(65536);
    for (let t = 0; t < 65536; t++)
      Kt[t] = t > 255 ? 255 + ~~(t / 255) : t;
  }
}
function bt(t, e) {
  const n = t.tokenTypeIdx;
  return n === e.tokenTypeIdx ? !0 : e.isParent === !0 && e.categoryMatchesMap[n] === !0;
}
function en(t, e) {
  return t.tokenTypeIdx === e.tokenTypeIdx;
}
let gi = 1;
const _s = {};
function xt(t) {
  const e = Vf(t);
  Kf(e), Wf(e), Hf(e), S(e, (n) => {
    n.isParent = n.categoryMatches.length > 0;
  });
}
function Vf(t) {
  let e = Y(t), n = t, r = !0;
  for (; r; ) {
    n = kt(pe(v(n, (s) => s.CATEGORIES)));
    const i = gn(n, e);
    e = e.concat(i), D(i) ? r = !1 : n = i;
  }
  return e;
}
function Kf(t) {
  S(t, (e) => {
    Rs(e) || (_s[gi] = e, e.tokenTypeIdx = gi++), Ei(e) && !x(e.CATEGORIES) && (e.CATEGORIES = [e.CATEGORIES]), Ei(e) || (e.CATEGORIES = []), zf(e) || (e.categoryMatches = []), Yf(e) || (e.categoryMatchesMap = {});
  });
}
function Hf(t) {
  S(t, (e) => {
    e.categoryMatches = [], S(e.categoryMatchesMap, (n, r) => {
      e.categoryMatches.push(_s[r].tokenTypeIdx);
    });
  });
}
function Wf(t) {
  S(t, (e) => {
    vs([], e);
  });
}
function vs(t, e) {
  S(t, (n) => {
    e.categoryMatchesMap[n.tokenTypeIdx] = !0;
  }), S(e.CATEGORIES, (n) => {
    const r = t.concat(e);
    Q(r, n) || vs(r, n);
  });
}
function Rs(t) {
  return O(t, "tokenTypeIdx");
}
function Ei(t) {
  return O(t, "CATEGORIES");
}
function zf(t) {
  return O(t, "categoryMatches");
}
function Yf(t) {
  return O(t, "categoryMatchesMap");
}
function Xf(t) {
  return O(t, "tokenTypeIdx");
}
const qf = {
  buildUnableToPopLexerModeMessage(t) {
    return `Unable to pop Lexer Mode after encountering Token ->${t.image}<- The Mode Stack is empty`;
  },
  buildUnexpectedCharactersMessage(t, e, n, r, i) {
    return `unexpected character: ->${t.charAt(e)}<- at offset: ${e}, skipped ${n} characters.`;
  }
};
var B;
(function(t) {
  t[t.MISSING_PATTERN = 0] = "MISSING_PATTERN", t[t.INVALID_PATTERN = 1] = "INVALID_PATTERN", t[t.EOI_ANCHOR_FOUND = 2] = "EOI_ANCHOR_FOUND", t[t.UNSUPPORTED_FLAGS_FOUND = 3] = "UNSUPPORTED_FLAGS_FOUND", t[t.DUPLICATE_PATTERNS_FOUND = 4] = "DUPLICATE_PATTERNS_FOUND", t[t.INVALID_GROUP_TYPE_FOUND = 5] = "INVALID_GROUP_TYPE_FOUND", t[t.PUSH_MODE_DOES_NOT_EXIST = 6] = "PUSH_MODE_DOES_NOT_EXIST", t[t.MULTI_MODE_LEXER_WITHOUT_DEFAULT_MODE = 7] = "MULTI_MODE_LEXER_WITHOUT_DEFAULT_MODE", t[t.MULTI_MODE_LEXER_WITHOUT_MODES_PROPERTY = 8] = "MULTI_MODE_LEXER_WITHOUT_MODES_PROPERTY", t[t.MULTI_MODE_LEXER_DEFAULT_MODE_VALUE_DOES_NOT_EXIST = 9] = "MULTI_MODE_LEXER_DEFAULT_MODE_VALUE_DOES_NOT_EXIST", t[t.LEXER_DEFINITION_CANNOT_CONTAIN_UNDEFINED = 10] = "LEXER_DEFINITION_CANNOT_CONTAIN_UNDEFINED", t[t.SOI_ANCHOR_FOUND = 11] = "SOI_ANCHOR_FOUND", t[t.EMPTY_MATCH_PATTERN = 12] = "EMPTY_MATCH_PATTERN", t[t.NO_LINE_BREAKS_FLAGS = 13] = "NO_LINE_BREAKS_FLAGS", t[t.UNREACHABLE_PATTERN = 14] = "UNREACHABLE_PATTERN", t[t.IDENTIFY_TERMINATOR = 15] = "IDENTIFY_TERMINATOR", t[t.CUSTOM_LINE_BREAK = 16] = "CUSTOM_LINE_BREAK", t[t.MULTI_MODE_LEXER_LONGER_ALT_NOT_IN_CURRENT_MODE = 17] = "MULTI_MODE_LEXER_LONGER_ALT_NOT_IN_CURRENT_MODE";
})(B || (B = {}));
const yt = {
  deferDefinitionErrorsHandling: !1,
  positionTracking: "full",
  lineTerminatorsPattern: /\n|\r\n?/g,
  lineTerminatorCharacters: [`
`, "\r"],
  ensureOptimizations: !1,
  safeMode: !1,
  errorMessageProvider: qf,
  traceInitPerf: !1,
  skipValidations: !1,
  recoveryEnabled: !0
};
Object.freeze(yt);
class X {
  constructor(e, n = yt) {
    if (this.lexerDefinition = e, this.lexerDefinitionErrors = [], this.lexerDefinitionWarning = [], this.patternIdxToConfig = {}, this.charCodeToPatternIdxToConfig = {}, this.modes = [], this.emptyGroups = {}, this.trackStartLines = !0, this.trackEndLines = !0, this.hasCustom = !1, this.canModeBeOptimized = {}, this.TRACE_INIT = (i, s) => {
      if (this.traceInitPerf === !0) {
        this.traceInitIndent++;
        const o = new Array(this.traceInitIndent + 1).join("	");
        this.traceInitIndent < this.traceInitMaxIdent && console.log(`${o}--> <${i}>`);
        const { time: a, value: c } = ds(s), u = a > 10 ? console.warn : console.log;
        return this.traceInitIndent < this.traceInitMaxIdent && u(`${o}<-- <${i}> time: ${a}ms`), this.traceInitIndent--, c;
      } else
        return s();
    }, typeof n == "boolean")
      throw Error(`The second argument to the Lexer constructor is now an ILexerConfig Object.
a boolean 2nd argument is no longer supported`);
    this.config = ne({}, yt, n);
    const r = this.config.traceInitPerf;
    r === !0 ? (this.traceInitMaxIdent = 1 / 0, this.traceInitPerf = !0) : typeof r == "number" && (this.traceInitMaxIdent = r, this.traceInitPerf = !0), this.traceInitIndent = -1, this.TRACE_INIT("Lexer Constructor", () => {
      let i, s = !0;
      this.TRACE_INIT("Lexer Config handling", () => {
        if (this.config.lineTerminatorsPattern === yt.lineTerminatorsPattern)
          this.config.lineTerminatorsPattern = jf;
        else if (this.config.lineTerminatorCharacters === yt.lineTerminatorCharacters)
          throw Error(`Error: Missing <lineTerminatorCharacters> property on the Lexer config.
	For details See: https://chevrotain.io/docs/guide/resolving_lexer_errors.html#MISSING_LINE_TERM_CHARS`);
        if (n.safeMode && n.ensureOptimizations)
          throw Error('"safeMode" and "ensureOptimizations" flags are mutually exclusive.');
        this.trackStartLines = /full|onlyStart/i.test(this.config.positionTracking), this.trackEndLines = /full/i.test(this.config.positionTracking), x(e) ? i = {
          modes: { defaultMode: Y(e) },
          defaultMode: Et
        } : (s = !1, i = Y(e));
      }), this.config.skipValidations === !1 && (this.TRACE_INIT("performRuntimeChecks", () => {
        this.lexerDefinitionErrors = this.lexerDefinitionErrors.concat(Uf(i, this.trackStartLines, this.config.lineTerminatorCharacters));
      }), this.TRACE_INIT("performWarningRuntimeChecks", () => {
        this.lexerDefinitionWarning = this.lexerDefinitionWarning.concat(Df(i, this.trackStartLines, this.config.lineTerminatorCharacters));
      })), i.modes = i.modes ? i.modes : {}, S(i.modes, (a, c) => {
        i.modes[c] = En(a, (u) => we(u));
      });
      const o = he(i.modes);
      if (S(i.modes, (a, c) => {
        this.TRACE_INIT(`Mode: <${c}> processing`, () => {
          if (this.modes.push(c), this.config.skipValidations === !1 && this.TRACE_INIT("validatePatterns", () => {
            this.lexerDefinitionErrors = this.lexerDefinitionErrors.concat(Af(a, o));
          }), D(this.lexerDefinitionErrors)) {
            xt(a);
            let u;
            this.TRACE_INIT("analyzeTokenTypes", () => {
              u = yf(a, {
                lineTerminatorCharacters: this.config.lineTerminatorCharacters,
                positionTracking: n.positionTracking,
                ensureOptimizations: n.ensureOptimizations,
                safeMode: n.safeMode,
                tracer: this.TRACE_INIT
              });
            }), this.patternIdxToConfig[c] = u.patternIdxToConfig, this.charCodeToPatternIdxToConfig[c] = u.charCodeToPatternIdxToConfig, this.emptyGroups = ne({}, this.emptyGroups, u.emptyGroups), this.hasCustom = u.hasCustom || this.hasCustom, this.canModeBeOptimized[c] = u.canBeOptimized;
          }
        });
      }), this.defaultMode = i.defaultMode, !D(this.lexerDefinitionErrors) && !this.config.deferDefinitionErrorsHandling) {
        const a = v(this.lexerDefinitionErrors, (c) => c.message).join(`-----------------------
`);
        throw new Error(`Errors detected in definition of Lexer:
` + a);
      }
      S(this.lexerDefinitionWarning, (a) => {
        fs(a.message);
      }), this.TRACE_INIT("Choosing sub-methods implementations", () => {
        if (Es ? (this.chopInput = at, this.match = this.matchWithTest) : (this.updateLastIndex = K, this.match = this.matchWithExec), s && (this.handleModes = K), this.trackStartLines === !1 && (this.computeNewColumn = at), this.trackEndLines === !1 && (this.updateTokenEndLineColumnLocation = K), /full/i.test(this.config.positionTracking))
          this.createTokenInstance = this.createFullToken;
        else if (/onlyStart/i.test(this.config.positionTracking))
          this.createTokenInstance = this.createStartOnlyToken;
        else if (/onlyOffset/i.test(this.config.positionTracking))
          this.createTokenInstance = this.createOffsetOnlyToken;
        else
          throw Error(`Invalid <positionTracking> config option: "${this.config.positionTracking}"`);
        this.hasCustom ? (this.addToken = this.addTokenUsingPush, this.handlePayload = this.handlePayloadWithCustom) : (this.addToken = this.addTokenUsingMemberAccess, this.handlePayload = this.handlePayloadNoCustom);
      }), this.TRACE_INIT("Failed Optimization Warnings", () => {
        const a = re(this.canModeBeOptimized, (c, u, l) => (u === !1 && c.push(l), c), []);
        if (n.ensureOptimizations && !D(a))
          throw Error(`Lexer Modes: < ${a.join(", ")} > cannot be optimized.
	 Disable the "ensureOptimizations" lexer config flag to silently ignore this and run the lexer in an un-optimized mode.
	 Or inspect the console log for details on how to resolve these issues.`);
      }), this.TRACE_INIT("clearRegExpParserCache", () => {
        mf();
      }), this.TRACE_INIT("toFastProperties", () => {
        ps(this);
      });
    });
  }
  tokenize(e, n = this.defaultMode) {
    if (!D(this.lexerDefinitionErrors)) {
      const r = v(this.lexerDefinitionErrors, (i) => i.message).join(`-----------------------
`);
      throw new Error(`Unable to Tokenize because Errors detected in definition of Lexer:
` + r);
    }
    return this.tokenizeInternal(e, n);
  }
  // There is quite a bit of duplication between this and "tokenizeInternalLazy"
  // This is intentional due to performance considerations.
  // this method also used quite a bit of `!` none null assertions because it is too optimized
  // for `tsc` to always understand it is "safe"
  tokenizeInternal(e, n) {
    let r, i, s, o, a, c, u, l, h, f, A, d, _, R, m;
    const T = e, p = T.length;
    let y = 0, N = 0;
    const L = this.hasCustom ? 0 : Math.floor(e.length / 10), P = new Array(L), se = [];
    let Ce = this.trackStartLines ? 1 : void 0, de = this.trackStartLines ? 1 : void 0;
    const He = $f(this.emptyGroups), Rn = this.trackStartLines, dt = this.config.lineTerminatorsPattern;
    let nt = 0, _e = [], We = [];
    const rt = [], it = [];
    Object.freeze(it);
    let ze;
    function Pt() {
      return _e;
    }
    function g(w) {
      const V = Be(w), be = We[V];
      return be === void 0 ? it : be;
    }
    const E = (w) => {
      if (rt.length === 1 && // if we have both a POP_MODE and a PUSH_MODE this is in-fact a "transition"
      // So no error should occur.
      w.tokenType.PUSH_MODE === void 0) {
        const V = this.config.errorMessageProvider.buildUnableToPopLexerModeMessage(w);
        se.push({
          offset: w.startOffset,
          line: w.startLine,
          column: w.startColumn,
          length: w.image.length,
          message: V
        });
      } else {
        rt.pop();
        const V = ut(rt);
        _e = this.patternIdxToConfig[V], We = this.charCodeToPatternIdxToConfig[V], nt = _e.length;
        const be = this.canModeBeOptimized[V] && this.config.safeMode === !1;
        We && be ? ze = g : ze = Pt;
      }
    };
    function k(w) {
      rt.push(w), We = this.charCodeToPatternIdxToConfig[w], _e = this.patternIdxToConfig[w], nt = _e.length, nt = _e.length;
      const V = this.canModeBeOptimized[w] && this.config.safeMode === !1;
      We && V ? ze = g : ze = Pt;
    }
    k.call(this, n);
    let C;
    const F = this.config.recoveryEnabled;
    for (; y < p; ) {
      c = null;
      const w = T.charCodeAt(y), V = ze(w), be = V.length;
      for (r = 0; r < be; r++) {
        C = V[r];
        const Z = C.pattern;
        u = null;
        const xe = C.short;
        if (xe !== !1 ? w === xe && (c = Z) : C.isCustom === !0 ? (m = Z.exec(T, y, P, He), m !== null ? (c = m[0], m.payload !== void 0 && (u = m.payload)) : c = null) : (this.updateLastIndex(Z, y), c = this.match(Z, e, y)), c !== null) {
          if (a = C.longerAlt, a !== void 0) {
            const je = a.length;
            for (s = 0; s < je; s++) {
              const Me = _e[a[s]], Ye = Me.pattern;
              if (l = null, Me.isCustom === !0 ? (m = Ye.exec(T, y, P, He), m !== null ? (o = m[0], m.payload !== void 0 && (l = m.payload)) : o = null) : (this.updateLastIndex(Ye, y), o = this.match(Ye, e, y)), o && o.length > c.length) {
                c = o, u = l, C = Me;
                break;
              }
            }
          }
          break;
        }
      }
      if (c !== null) {
        if (h = c.length, f = C.group, f !== void 0 && (A = C.tokenTypeIdx, d = this.createTokenInstance(c, y, A, C.tokenType, Ce, de, h), this.handlePayload(d, u), f === !1 ? N = this.addToken(P, N, d) : He[f].push(d)), e = this.chopInput(e, h), y = y + h, de = this.computeNewColumn(de, h), Rn === !0 && C.canLineTerminator === !0) {
          let Z = 0, xe, je;
          dt.lastIndex = 0;
          do
            xe = dt.test(c), xe === !0 && (je = dt.lastIndex - 1, Z++);
          while (xe === !0);
          Z !== 0 && (Ce = Ce + Z, de = h - je, this.updateTokenEndLineColumnLocation(d, f, je, Z, Ce, de, h));
        }
        this.handleModes(C, E, k, d);
      } else {
        const Z = y, xe = Ce, je = de;
        let Me = F === !1;
        for (; Me === !1 && y < p; )
          for (e = this.chopInput(e, 1), y++, i = 0; i < nt; i++) {
            const Ye = _e[i], On = Ye.pattern, br = Ye.short;
            if (br !== !1 ? T.charCodeAt(y) === br && (Me = !0) : Ye.isCustom === !0 ? Me = On.exec(T, y, P, He) !== null : (this.updateLastIndex(On, y), Me = On.exec(e) !== null), Me === !0)
              break;
          }
        if (_ = y - Z, de = this.computeNewColumn(de, _), R = this.config.errorMessageProvider.buildUnexpectedCharactersMessage(T, Z, _, xe, je), se.push({
          offset: Z,
          line: xe,
          column: je,
          length: _,
          message: R
        }), F === !1)
          break;
      }
    }
    return this.hasCustom || (P.length = N), {
      tokens: P,
      groups: He,
      errors: se
    };
  }
  handleModes(e, n, r, i) {
    if (e.pop === !0) {
      const s = e.push;
      n(i), s !== void 0 && r.call(this, s);
    } else e.push !== void 0 && r.call(this, e.push);
  }
  chopInput(e, n) {
    return e.substring(n);
  }
  updateLastIndex(e, n) {
    e.lastIndex = n;
  }
  // TODO: decrease this under 600 characters? inspect stripping comments option in TSC compiler
  updateTokenEndLineColumnLocation(e, n, r, i, s, o, a) {
    let c, u;
    n !== void 0 && (c = r === a - 1, u = c ? -1 : 0, i === 1 && c === !0 || (e.endLine = s + u, e.endColumn = o - 1 + -u));
  }
  computeNewColumn(e, n) {
    return e + n;
  }
  createOffsetOnlyToken(e, n, r, i) {
    return {
      image: e,
      startOffset: n,
      tokenTypeIdx: r,
      tokenType: i
    };
  }
  createStartOnlyToken(e, n, r, i, s, o) {
    return {
      image: e,
      startOffset: n,
      startLine: s,
      startColumn: o,
      tokenTypeIdx: r,
      tokenType: i
    };
  }
  createFullToken(e, n, r, i, s, o, a) {
    return {
      image: e,
      startOffset: n,
      endOffset: n + a - 1,
      startLine: s,
      endLine: s,
      startColumn: o,
      endColumn: o + a - 1,
      tokenTypeIdx: r,
      tokenType: i
    };
  }
  addTokenUsingPush(e, n, r) {
    return e.push(r), n;
  }
  addTokenUsingMemberAccess(e, n, r) {
    return e[n] = r, n++, n;
  }
  handlePayloadNoCustom(e, n) {
  }
  handlePayloadWithCustom(e, n) {
    n !== null && (e.payload = n);
  }
  matchWithTest(e, n, r) {
    return e.test(n) === !0 ? n.substring(r, e.lastIndex) : null;
  }
  matchWithExec(e, n) {
    const r = e.exec(n);
    return r !== null ? r[0] : null;
  }
}
X.SKIPPED = "This marks a skipped Token pattern, this means each token identified by it willbe consumed and then thrown into oblivion, this can be used to for example to completely ignore whitespace.";
X.NA = /NOT_APPLICABLE/;
function _t(t) {
  return Os(t) ? t.LABEL : t.name;
}
function Os(t) {
  return ee(t.LABEL) && t.LABEL !== "";
}
const Qf = "parent", Ti = "categories", yi = "label", Ai = "group", _i = "push_mode", vi = "pop_mode", Ri = "longer_alt", Oi = "line_breaks", Si = "start_chars_hint";
function M(t) {
  return Zf(t);
}
function Zf(t) {
  const e = t.pattern, n = {};
  if (n.name = t.name, we(e) || (n.PATTERN = e), O(t, Qf))
    throw `The parent property is no longer supported.
See: https://github.com/chevrotain/chevrotain/issues/564#issuecomment-349062346 for details.`;
  return O(t, Ti) && (n.CATEGORIES = t[Ti]), xt([n]), O(t, yi) && (n.LABEL = t[yi]), O(t, Ai) && (n.GROUP = t[Ai]), O(t, vi) && (n.POP_MODE = t[vi]), O(t, _i) && (n.PUSH_MODE = t[_i]), O(t, Ri) && (n.LONGER_ALT = t[Ri]), O(t, Oi) && (n.LINE_BREAKS = t[Oi]), O(t, Si) && (n.START_CHARS_HINT = t[Si]), n;
}
const Ze = M({ name: "EOF", pattern: X.NA });
xt([Ze]);
function Rr(t, e, n, r, i, s, o, a) {
  return {
    image: e,
    startOffset: n,
    endOffset: r,
    startLine: i,
    endLine: s,
    startColumn: o,
    endColumn: a,
    tokenTypeIdx: t.tokenTypeIdx,
    tokenType: t
  };
}
function Jf(t, e) {
  return bt(t, e);
}
const Ss = {
  buildMismatchTokenMessage({ expected: t, actual: e, previous: n, ruleName: r }) {
    return `Expecting ${Os(t) ? `--> ${_t(t)} <--` : `token of type --> ${t.name} <--`} but found --> '${e.image}' <--`;
  },
  buildNotAllInputParsedMessage({ firstRedundant: t, ruleName: e }) {
    return "Redundant input, expecting EOF but found: " + t.image;
  },
  buildNoViableAltMessage({ expectedPathsPerAlt: t, actual: e, previous: n, customUserDescription: r, ruleName: i }) {
    const s = "Expecting: ", o = `
but found: '` + Ee(e).image + "'";
    if (r)
      return s + r + o;
    {
      const a = re(t, (l, h) => l.concat(h), []), c = v(a, (l) => `[${v(l, (h) => _t(h)).join(", ")}]`), u = `one of these possible Token sequences:
${v(c, (l, h) => `  ${h + 1}. ${l}`).join(`
`)}`;
      return s + u + o;
    }
  },
  buildEarlyExitMessage({ expectedIterationPaths: t, actual: e, customUserDescription: n, ruleName: r }) {
    const i = "Expecting: ", s = `
but found: '` + Ee(e).image + "'";
    if (n)
      return i + n + s;
    {
      const o = `expecting at least one iteration which starts with one of these possible Token sequences::
  <${v(t, (a) => `[${v(a, (c) => _t(c)).join(",")}]`).join(" ,")}>`;
      return i + o + s;
    }
  }
};
Object.freeze(Ss);
const ed = {
  buildRuleNotFoundError(t, e) {
    return "Invalid grammar, reference to a rule which is not defined: ->" + e.nonTerminalName + `<-
inside top level rule: ->` + t.name + "<-";
  }
}, Xe = {
  buildDuplicateFoundError(t, e) {
    function n(l) {
      return l instanceof j ? l.terminalType.name : l instanceof ie ? l.nonTerminalName : "";
    }
    const r = t.name, i = Ee(e), s = i.idx, o = ve(i), a = n(i), c = s > 0;
    let u = `->${o}${c ? s : ""}<- ${a ? `with argument: ->${a}<-` : ""}
                  appears more than once (${e.length} times) in the top level rule: ->${r}<-.                  
                  For further details see: https://chevrotain.io/docs/FAQ.html#NUMERICAL_SUFFIXES 
                  `;
    return u = u.replace(/[ \t]+/g, " "), u = u.replace(/\s\s+/g, `
`), u;
  },
  buildNamespaceConflictError(t) {
    return `Namespace conflict found in grammar.
The grammar has both a Terminal(Token) and a Non-Terminal(Rule) named: <${t.name}>.
To resolve this make sure each Terminal and Non-Terminal names are unique
This is easy to accomplish by using the convention that Terminal names start with an uppercase letter
and Non-Terminal names start with a lower case letter.`;
  },
  buildAlternationPrefixAmbiguityError(t) {
    const e = v(t.prefixPath, (r) => _t(r)).join(", "), n = t.alternation.idx === 0 ? "" : t.alternation.idx;
    return `Ambiguous alternatives: <${t.ambiguityIndices.join(" ,")}> due to common lookahead prefix
in <OR${n}> inside <${t.topLevelRule.name}> Rule,
<${e}> may appears as a prefix path in all these alternatives.
See: https://chevrotain.io/docs/guide/resolving_grammar_errors.html#COMMON_PREFIX
For Further details.`;
  },
  buildAlternationAmbiguityError(t) {
    const e = v(t.prefixPath, (i) => _t(i)).join(", "), n = t.alternation.idx === 0 ? "" : t.alternation.idx;
    let r = `Ambiguous Alternatives Detected: <${t.ambiguityIndices.join(" ,")}> in <OR${n}> inside <${t.topLevelRule.name}> Rule,
<${e}> may appears as a prefix path in all these alternatives.
`;
    return r = r + `See: https://chevrotain.io/docs/guide/resolving_grammar_errors.html#AMBIGUOUS_ALTERNATIVES
For Further details.`, r;
  },
  buildEmptyRepetitionError(t) {
    let e = ve(t.repetition);
    return t.repetition.idx !== 0 && (e += t.repetition.idx), `The repetition <${e}> within Rule <${t.topLevelRule.name}> can never consume any tokens.
This could lead to an infinite loop.`;
  },
  // TODO: remove - `errors_public` from nyc.config.js exclude
  //       once this method is fully removed from this file
  buildTokenNameError(t) {
    return "deprecated";
  },
  buildEmptyAlternationError(t) {
    return `Ambiguous empty alternative: <${t.emptyChoiceIdx + 1}> in <OR${t.alternation.idx}> inside <${t.topLevelRule.name}> Rule.
Only the last alternative may be an empty alternative.`;
  },
  buildTooManyAlternativesError(t) {
    return `An Alternation cannot have more than 256 alternatives:
<OR${t.alternation.idx}> inside <${t.topLevelRule.name}> Rule.
 has ${t.alternation.definition.length + 1} alternatives.`;
  },
  buildLeftRecursionError(t) {
    const e = t.topLevelRule.name, n = v(t.leftRecursionPath, (i) => i.name), r = `${e} --> ${n.concat([e]).join(" --> ")}`;
    return `Left Recursion found in grammar.
rule: <${e}> can be invoked from itself (directly or indirectly)
without consuming any Tokens. The grammar path that causes this is: 
 ${r}
 To fix this refactor your grammar to remove the left recursion.
see: https://en.wikipedia.org/wiki/LL_parser#Left_factoring.`;
  },
  // TODO: remove - `errors_public` from nyc.config.js exclude
  //       once this method is fully removed from this file
  buildInvalidRuleNameError(t) {
    return "deprecated";
  },
  buildDuplicateRuleNameError(t) {
    let e;
    return t.topLevelRule instanceof ht ? e = t.topLevelRule.name : e = t.topLevelRule, `Duplicate definition, rule: ->${e}<- is already defined in the grammar: ->${t.grammarName}<-`;
  }
};
function td(t, e) {
  const n = new nd(t, e);
  return n.resolveRefs(), n.errors;
}
class nd extends ft {
  constructor(e, n) {
    super(), this.nameToTopRule = e, this.errMsgProvider = n, this.errors = [];
  }
  resolveRefs() {
    S(H(this.nameToTopRule), (e) => {
      this.currTopLevel = e, e.accept(this);
    });
  }
  visitNonTerminal(e) {
    const n = this.nameToTopRule[e.nonTerminalName];
    if (n)
      e.referencedRule = n;
    else {
      const r = this.errMsgProvider.buildRuleNotFoundError(this.currTopLevel, e);
      this.errors.push({
        message: r,
        type: q.UNRESOLVED_SUBRULE_REF,
        ruleName: this.currTopLevel.name,
        unresolvedRefName: e.nonTerminalName
      });
    }
  }
}
class rd extends Tn {
  constructor(e, n) {
    super(), this.topProd = e, this.path = n, this.possibleTokTypes = [], this.nextProductionName = "", this.nextProductionOccurrence = 0, this.found = !1, this.isAtEndOfPath = !1;
  }
  startWalking() {
    if (this.found = !1, this.path.ruleStack[0] !== this.topProd.name)
      throw Error("The path does not start with the walker's top Rule!");
    return this.ruleStack = Y(this.path.ruleStack).reverse(), this.occurrenceStack = Y(this.path.occurrenceStack).reverse(), this.ruleStack.pop(), this.occurrenceStack.pop(), this.updateExpectedNext(), this.walk(this.topProd), this.possibleTokTypes;
  }
  walk(e, n = []) {
    this.found || super.walk(e, n);
  }
  walkProdRef(e, n, r) {
    if (e.referencedRule.name === this.nextProductionName && e.idx === this.nextProductionOccurrence) {
      const i = n.concat(r);
      this.updateExpectedNext(), this.walk(e.referencedRule, i);
    }
  }
  updateExpectedNext() {
    D(this.ruleStack) ? (this.nextProductionName = "", this.nextProductionOccurrence = 0, this.isAtEndOfPath = !0) : (this.nextProductionName = this.ruleStack.pop(), this.nextProductionOccurrence = this.occurrenceStack.pop());
  }
}
class id extends rd {
  constructor(e, n) {
    super(e, n), this.path = n, this.nextTerminalName = "", this.nextTerminalOccurrence = 0, this.nextTerminalName = this.path.lastTok.name, this.nextTerminalOccurrence = this.path.lastTokOccurrence;
  }
  walkTerminal(e, n, r) {
    if (this.isAtEndOfPath && e.terminalType.name === this.nextTerminalName && e.idx === this.nextTerminalOccurrence && !this.found) {
      const i = n.concat(r), s = new te({ definition: i });
      this.possibleTokTypes = Ct(s), this.found = !0;
    }
  }
}
class An extends Tn {
  constructor(e, n) {
    super(), this.topRule = e, this.occurrence = n, this.result = {
      token: void 0,
      occurrence: void 0,
      isEndOfRule: void 0
    };
  }
  startWalking() {
    return this.walk(this.topRule), this.result;
  }
}
class sd extends An {
  walkMany(e, n, r) {
    if (e.idx === this.occurrence) {
      const i = Ee(n.concat(r));
      this.result.isEndOfRule = i === void 0, i instanceof j && (this.result.token = i.terminalType, this.result.occurrence = i.idx);
    } else
      super.walkMany(e, n, r);
  }
}
class Ii extends An {
  walkManySep(e, n, r) {
    if (e.idx === this.occurrence) {
      const i = Ee(n.concat(r));
      this.result.isEndOfRule = i === void 0, i instanceof j && (this.result.token = i.terminalType, this.result.occurrence = i.idx);
    } else
      super.walkManySep(e, n, r);
  }
}
class od extends An {
  walkAtLeastOne(e, n, r) {
    if (e.idx === this.occurrence) {
      const i = Ee(n.concat(r));
      this.result.isEndOfRule = i === void 0, i instanceof j && (this.result.token = i.terminalType, this.result.occurrence = i.idx);
    } else
      super.walkAtLeastOne(e, n, r);
  }
}
class Ni extends An {
  walkAtLeastOneSep(e, n, r) {
    if (e.idx === this.occurrence) {
      const i = Ee(n.concat(r));
      this.result.isEndOfRule = i === void 0, i instanceof j && (this.result.token = i.terminalType, this.result.occurrence = i.idx);
    } else
      super.walkAtLeastOneSep(e, n, r);
  }
}
function Kn(t, e, n = []) {
  n = Y(n);
  let r = [], i = 0;
  function s(a) {
    return a.concat(z(t, i + 1));
  }
  function o(a) {
    const c = Kn(s(a), e, n);
    return r.concat(c);
  }
  for (; n.length < e && i < t.length; ) {
    const a = t[i];
    if (a instanceof te || a instanceof ie)
      return o(a.definition);
    if (a instanceof J)
      r = o(a.definition);
    else if (a instanceof Le) {
      const c = a.definition.concat([
        new W({
          definition: a.definition
        })
      ]);
      return o(c);
    } else if (a instanceof ke) {
      const c = [
        new te({ definition: a.definition }),
        new W({
          definition: [new j({ terminalType: a.separator })].concat(a.definition)
        })
      ];
      return o(c);
    } else if (a instanceof ye) {
      const c = a.definition.concat([
        new W({
          definition: [new j({ terminalType: a.separator })].concat(a.definition)
        })
      ]);
      r = o(c);
    } else if (a instanceof W) {
      const c = a.definition.concat([
        new W({
          definition: a.definition
        })
      ]);
      r = o(c);
    } else {
      if (a instanceof Ae)
        return S(a.definition, (c) => {
          D(c.definition) === !1 && (r = o(c.definition));
        }), r;
      if (a instanceof j)
        n.push(a.terminalType);
      else
        throw Error("non exhaustive match");
    }
    i++;
  }
  return r.push({
    partialPath: n,
    suffixDef: z(t, i)
  }), r;
}
function Is(t, e, n, r) {
  const i = "EXIT_NONE_TERMINAL", s = [i], o = "EXIT_ALTERNATIVE";
  let a = !1;
  const c = e.length, u = c - r - 1, l = [], h = [];
  for (h.push({
    idx: -1,
    def: t,
    ruleStack: [],
    occurrenceStack: []
  }); !D(h); ) {
    const f = h.pop();
    if (f === o) {
      a && ut(h).idx <= u && h.pop();
      continue;
    }
    const A = f.def, d = f.idx, _ = f.ruleStack, R = f.occurrenceStack;
    if (D(A))
      continue;
    const m = A[0];
    if (m === i) {
      const T = {
        idx: d,
        def: z(A),
        ruleStack: St(_),
        occurrenceStack: St(R)
      };
      h.push(T);
    } else if (m instanceof j)
      if (d < c - 1) {
        const T = d + 1, p = e[T];
        if (n(p, m.terminalType)) {
          const y = {
            idx: T,
            def: z(A),
            ruleStack: _,
            occurrenceStack: R
          };
          h.push(y);
        }
      } else if (d === c - 1)
        l.push({
          nextTokenType: m.terminalType,
          nextTokenOccurrence: m.idx,
          ruleStack: _,
          occurrenceStack: R
        }), a = !0;
      else
        throw Error("non exhaustive match");
    else if (m instanceof ie) {
      const T = Y(_);
      T.push(m.nonTerminalName);
      const p = Y(R);
      p.push(m.idx);
      const y = {
        idx: d,
        def: m.definition.concat(s, z(A)),
        ruleStack: T,
        occurrenceStack: p
      };
      h.push(y);
    } else if (m instanceof J) {
      const T = {
        idx: d,
        def: z(A),
        ruleStack: _,
        occurrenceStack: R
      };
      h.push(T), h.push(o);
      const p = {
        idx: d,
        def: m.definition.concat(z(A)),
        ruleStack: _,
        occurrenceStack: R
      };
      h.push(p);
    } else if (m instanceof Le) {
      const T = new W({
        definition: m.definition,
        idx: m.idx
      }), p = m.definition.concat([T], z(A)), y = {
        idx: d,
        def: p,
        ruleStack: _,
        occurrenceStack: R
      };
      h.push(y);
    } else if (m instanceof ke) {
      const T = new j({
        terminalType: m.separator
      }), p = new W({
        definition: [T].concat(m.definition),
        idx: m.idx
      }), y = m.definition.concat([p], z(A)), N = {
        idx: d,
        def: y,
        ruleStack: _,
        occurrenceStack: R
      };
      h.push(N);
    } else if (m instanceof ye) {
      const T = {
        idx: d,
        def: z(A),
        ruleStack: _,
        occurrenceStack: R
      };
      h.push(T), h.push(o);
      const p = new j({
        terminalType: m.separator
      }), y = new W({
        definition: [p].concat(m.definition),
        idx: m.idx
      }), N = m.definition.concat([y], z(A)), L = {
        idx: d,
        def: N,
        ruleStack: _,
        occurrenceStack: R
      };
      h.push(L);
    } else if (m instanceof W) {
      const T = {
        idx: d,
        def: z(A),
        ruleStack: _,
        occurrenceStack: R
      };
      h.push(T), h.push(o);
      const p = new W({
        definition: m.definition,
        idx: m.idx
      }), y = m.definition.concat([p], z(A)), N = {
        idx: d,
        def: y,
        ruleStack: _,
        occurrenceStack: R
      };
      h.push(N);
    } else if (m instanceof Ae)
      for (let T = m.definition.length - 1; T >= 0; T--) {
        const p = m.definition[T], y = {
          idx: d,
          def: p.definition.concat(z(A)),
          ruleStack: _,
          occurrenceStack: R
        };
        h.push(y), h.push(o);
      }
    else if (m instanceof te)
      h.push({
        idx: d,
        def: m.definition.concat(z(A)),
        ruleStack: _,
        occurrenceStack: R
      });
    else if (m instanceof ht)
      h.push(ad(m, d, _, R));
    else
      throw Error("non exhaustive match");
  }
  return l;
}
function ad(t, e, n, r) {
  const i = Y(n);
  i.push(t.name);
  const s = Y(r);
  return s.push(1), {
    idx: e,
    def: t.definition,
    ruleStack: i,
    occurrenceStack: s
  };
}
var G;
(function(t) {
  t[t.OPTION = 0] = "OPTION", t[t.REPETITION = 1] = "REPETITION", t[t.REPETITION_MANDATORY = 2] = "REPETITION_MANDATORY", t[t.REPETITION_MANDATORY_WITH_SEPARATOR = 3] = "REPETITION_MANDATORY_WITH_SEPARATOR", t[t.REPETITION_WITH_SEPARATOR = 4] = "REPETITION_WITH_SEPARATOR", t[t.ALTERNATION = 5] = "ALTERNATION";
})(G || (G = {}));
function Ns(t) {
  if (t instanceof J || t === "Option")
    return G.OPTION;
  if (t instanceof W || t === "Repetition")
    return G.REPETITION;
  if (t instanceof Le || t === "RepetitionMandatory")
    return G.REPETITION_MANDATORY;
  if (t instanceof ke || t === "RepetitionMandatoryWithSeparator")
    return G.REPETITION_MANDATORY_WITH_SEPARATOR;
  if (t instanceof ye || t === "RepetitionWithSeparator")
    return G.REPETITION_WITH_SEPARATOR;
  if (t instanceof Ae || t === "Alternation")
    return G.ALTERNATION;
  throw Error("non exhaustive match");
}
function cd(t, e, n, r, i, s) {
  const o = Or(t, e, n), a = Cs(o) ? en : bt;
  return s(o, r, a, i);
}
function ud(t, e, n, r, i, s) {
  const o = Sr(t, e, i, n), a = Cs(o) ? en : bt;
  return s(o[0], a, r);
}
function ld(t, e, n, r) {
  const i = t.length, s = me(t, (o) => me(o, (a) => a.length === 1));
  if (e)
    return function(o) {
      const a = v(o, (c) => c.GATE);
      for (let c = 0; c < i; c++) {
        const u = t[c], l = u.length, h = a[c];
        if (!(h !== void 0 && h.call(this) === !1))
          e: for (let f = 0; f < l; f++) {
            const A = u[f], d = A.length;
            for (let _ = 0; _ < d; _++) {
              const R = this.LA(_ + 1);
              if (n(R, A[_]) === !1)
                continue e;
            }
            return c;
          }
      }
    };
  if (s && !r) {
    const o = v(t, (c) => pe(c)), a = re(o, (c, u, l) => (S(u, (h) => {
      O(c, h.tokenTypeIdx) || (c[h.tokenTypeIdx] = l), S(h.categoryMatches, (f) => {
        O(c, f) || (c[f] = l);
      });
    }), c), {});
    return function() {
      const c = this.LA(1);
      return a[c.tokenTypeIdx];
    };
  } else
    return function() {
      for (let o = 0; o < i; o++) {
        const a = t[o], c = a.length;
        e: for (let u = 0; u < c; u++) {
          const l = a[u], h = l.length;
          for (let f = 0; f < h; f++) {
            const A = this.LA(f + 1);
            if (n(A, l[f]) === !1)
              continue e;
          }
          return o;
        }
      }
    };
}
function hd(t, e, n) {
  const r = me(t, (s) => s.length === 1), i = t.length;
  if (r && !n) {
    const s = pe(t);
    if (s.length === 1 && D(s[0].categoryMatches)) {
      const o = s[0].tokenTypeIdx;
      return function() {
        return this.LA(1).tokenTypeIdx === o;
      };
    } else {
      const o = re(s, (a, c, u) => (a[c.tokenTypeIdx] = !0, S(c.categoryMatches, (l) => {
        a[l] = !0;
      }), a), []);
      return function() {
        const a = this.LA(1);
        return o[a.tokenTypeIdx] === !0;
      };
    }
  } else
    return function() {
      e: for (let s = 0; s < i; s++) {
        const o = t[s], a = o.length;
        for (let c = 0; c < a; c++) {
          const u = this.LA(c + 1);
          if (e(u, o[c]) === !1)
            continue e;
        }
        return !0;
      }
      return !1;
    };
}
class fd extends Tn {
  constructor(e, n, r) {
    super(), this.topProd = e, this.targetOccurrence = n, this.targetProdType = r;
  }
  startWalking() {
    return this.walk(this.topProd), this.restDef;
  }
  checkIsTarget(e, n, r, i) {
    return e.idx === this.targetOccurrence && this.targetProdType === n ? (this.restDef = r.concat(i), !0) : !1;
  }
  walkOption(e, n, r) {
    this.checkIsTarget(e, G.OPTION, n, r) || super.walkOption(e, n, r);
  }
  walkAtLeastOne(e, n, r) {
    this.checkIsTarget(e, G.REPETITION_MANDATORY, n, r) || super.walkOption(e, n, r);
  }
  walkAtLeastOneSep(e, n, r) {
    this.checkIsTarget(e, G.REPETITION_MANDATORY_WITH_SEPARATOR, n, r) || super.walkOption(e, n, r);
  }
  walkMany(e, n, r) {
    this.checkIsTarget(e, G.REPETITION, n, r) || super.walkOption(e, n, r);
  }
  walkManySep(e, n, r) {
    this.checkIsTarget(e, G.REPETITION_WITH_SEPARATOR, n, r) || super.walkOption(e, n, r);
  }
}
class Ls extends ft {
  constructor(e, n, r) {
    super(), this.targetOccurrence = e, this.targetProdType = n, this.targetRef = r, this.result = [];
  }
  checkIsTarget(e, n) {
    e.idx === this.targetOccurrence && this.targetProdType === n && (this.targetRef === void 0 || e === this.targetRef) && (this.result = e.definition);
  }
  visitOption(e) {
    this.checkIsTarget(e, G.OPTION);
  }
  visitRepetition(e) {
    this.checkIsTarget(e, G.REPETITION);
  }
  visitRepetitionMandatory(e) {
    this.checkIsTarget(e, G.REPETITION_MANDATORY);
  }
  visitRepetitionMandatoryWithSeparator(e) {
    this.checkIsTarget(e, G.REPETITION_MANDATORY_WITH_SEPARATOR);
  }
  visitRepetitionWithSeparator(e) {
    this.checkIsTarget(e, G.REPETITION_WITH_SEPARATOR);
  }
  visitAlternation(e) {
    this.checkIsTarget(e, G.ALTERNATION);
  }
}
function Li(t) {
  const e = new Array(t);
  for (let n = 0; n < t; n++)
    e[n] = [];
  return e;
}
function Cn(t) {
  let e = [""];
  for (let n = 0; n < t.length; n++) {
    const r = t[n], i = [];
    for (let s = 0; s < e.length; s++) {
      const o = e[s];
      i.push(o + "_" + r.tokenTypeIdx);
      for (let a = 0; a < r.categoryMatches.length; a++) {
        const c = "_" + r.categoryMatches[a];
        i.push(o + c);
      }
    }
    e = i;
  }
  return e;
}
function dd(t, e, n) {
  for (let r = 0; r < t.length; r++) {
    if (r === n)
      continue;
    const i = t[r];
    for (let s = 0; s < e.length; s++) {
      const o = e[s];
      if (i[o] === !0)
        return !1;
    }
  }
  return !0;
}
function ks(t, e) {
  const n = v(t, (o) => Kn([o], 1)), r = Li(n.length), i = v(n, (o) => {
    const a = {};
    return S(o, (c) => {
      const u = Cn(c.partialPath);
      S(u, (l) => {
        a[l] = !0;
      });
    }), a;
  });
  let s = n;
  for (let o = 1; o <= e; o++) {
    const a = s;
    s = Li(a.length);
    for (let c = 0; c < a.length; c++) {
      const u = a[c];
      for (let l = 0; l < u.length; l++) {
        const h = u[l].partialPath, f = u[l].suffixDef, A = Cn(h);
        if (dd(i, A, c) || D(f) || h.length === e) {
          const d = r[c];
          if (Hn(d, h) === !1) {
            d.push(h);
            for (let _ = 0; _ < A.length; _++) {
              const R = A[_];
              i[c][R] = !0;
            }
          }
        } else {
          const d = Kn(f, o + 1, h);
          s[c] = s[c].concat(d), S(d, (_) => {
            const R = Cn(_.partialPath);
            S(R, (m) => {
              i[c][m] = !0;
            });
          });
        }
      }
    }
  }
  return r;
}
function Or(t, e, n, r) {
  const i = new Ls(t, G.ALTERNATION, r);
  return e.accept(i), ks(i.result, n);
}
function Sr(t, e, n, r) {
  const i = new Ls(t, n);
  e.accept(i);
  const s = i.result, o = new fd(e, t, n).startWalking(), a = new te({ definition: s }), c = new te({ definition: o });
  return ks([a, c], r);
}
function Hn(t, e) {
  e: for (let n = 0; n < t.length; n++) {
    const r = t[n];
    if (r.length === e.length) {
      for (let i = 0; i < r.length; i++) {
        const s = e[i], o = r[i];
        if (!(s === o || o.categoryMatchesMap[s.tokenTypeIdx] !== void 0))
          continue e;
      }
      return !0;
    }
  }
  return !1;
}
function pd(t, e) {
  return t.length < e.length && me(t, (n, r) => {
    const i = e[r];
    return n === i || i.categoryMatchesMap[n.tokenTypeIdx];
  });
}
function Cs(t) {
  return me(t, (e) => me(e, (n) => me(n, (r) => D(r.categoryMatches))));
}
function md(t) {
  const e = t.lookaheadStrategy.validate({
    rules: t.rules,
    tokenTypes: t.tokenTypes,
    grammarName: t.grammarName
  });
  return v(e, (n) => Object.assign({ type: q.CUSTOM_LOOKAHEAD_VALIDATION }, n));
}
function gd(t, e, n, r) {
  const i = ce(t, (c) => Ed(c, n)), s = kd(t, e, n), o = ce(t, (c) => Sd(c, n)), a = ce(t, (c) => Ad(c, t, r, n));
  return i.concat(s, o, a);
}
function Ed(t, e) {
  const n = new yd();
  t.accept(n);
  const r = n.allProductions, i = kh(r, Td), s = Te(i, (o) => o.length > 1);
  return v(H(s), (o) => {
    const a = Ee(o), c = e.buildDuplicateFoundError(t, o), u = ve(a), l = {
      message: c,
      type: q.DUPLICATE_PRODUCTIONS,
      ruleName: t.name,
      dslName: u,
      occurrence: a.idx
    }, h = bs(a);
    return h && (l.parameter = h), l;
  });
}
function Td(t) {
  return `${ve(t)}_#_${t.idx}_#_${bs(t)}`;
}
function bs(t) {
  return t instanceof j ? t.terminalType.name : t instanceof ie ? t.nonTerminalName : "";
}
class yd extends ft {
  constructor() {
    super(...arguments), this.allProductions = [];
  }
  visitNonTerminal(e) {
    this.allProductions.push(e);
  }
  visitOption(e) {
    this.allProductions.push(e);
  }
  visitRepetitionWithSeparator(e) {
    this.allProductions.push(e);
  }
  visitRepetitionMandatory(e) {
    this.allProductions.push(e);
  }
  visitRepetitionMandatoryWithSeparator(e) {
    this.allProductions.push(e);
  }
  visitRepetition(e) {
    this.allProductions.push(e);
  }
  visitAlternation(e) {
    this.allProductions.push(e);
  }
  visitTerminal(e) {
    this.allProductions.push(e);
  }
}
function Ad(t, e, n, r) {
  const i = [];
  if (re(e, (s, o) => o.name === t.name ? s + 1 : s, 0) > 1) {
    const s = r.buildDuplicateRuleNameError({
      topLevelRule: t,
      grammarName: n
    });
    i.push({
      message: s,
      type: q.DUPLICATE_RULE_NAME,
      ruleName: t.name
    });
  }
  return i;
}
function _d(t, e, n) {
  const r = [];
  let i;
  return Q(e, t) || (i = `Invalid rule override, rule: ->${t}<- cannot be overridden in the grammar: ->${n}<-as it is not defined in any of the super grammars `, r.push({
    message: i,
    type: q.INVALID_RULE_OVERRIDE,
    ruleName: t
  })), r;
}
function xs(t, e, n, r = []) {
  const i = [], s = Ht(e.definition);
  if (D(s))
    return [];
  {
    const o = t.name;
    Q(s, t) && i.push({
      message: n.buildLeftRecursionError({
        topLevelRule: t,
        leftRecursionPath: r
      }),
      type: q.LEFT_RECURSION,
      ruleName: o
    });
    const a = gn(s, r.concat([t])), c = ce(a, (u) => {
      const l = Y(r);
      return l.push(u), xs(t, u, n, l);
    });
    return i.concat(c);
  }
}
function Ht(t) {
  let e = [];
  if (D(t))
    return e;
  const n = Ee(t);
  if (n instanceof ie)
    e.push(n.referencedRule);
  else if (n instanceof te || n instanceof J || n instanceof Le || n instanceof ke || n instanceof ye || n instanceof W)
    e = e.concat(Ht(n.definition));
  else if (n instanceof Ae)
    e = pe(v(n.definition, (s) => Ht(s.definition)));
  else if (!(n instanceof j)) throw Error("non exhaustive match");
  const r = qt(n), i = t.length > 1;
  if (r && i) {
    const s = z(t);
    return e.concat(Ht(s));
  } else
    return e;
}
class Ir extends ft {
  constructor() {
    super(...arguments), this.alternations = [];
  }
  visitAlternation(e) {
    this.alternations.push(e);
  }
}
function vd(t, e) {
  const n = new Ir();
  t.accept(n);
  const r = n.alternations;
  return ce(r, (i) => {
    const s = St(i.definition);
    return ce(s, (o, a) => {
      const c = Is([o], [], bt, 1);
      return D(c) ? [
        {
          message: e.buildEmptyAlternationError({
            topLevelRule: t,
            alternation: i,
            emptyChoiceIdx: a
          }),
          type: q.NONE_LAST_EMPTY_ALT,
          ruleName: t.name,
          occurrence: i.idx,
          alternative: a + 1
        }
      ] : [];
    });
  });
}
function Rd(t, e, n) {
  const r = new Ir();
  t.accept(r);
  let i = r.alternations;
  return i = En(i, (s) => s.ignoreAmbiguities === !0), ce(i, (s) => {
    const o = s.idx, a = s.maxLookahead || e, c = Or(o, t, a, s), u = Nd(c, s, t, n), l = Ld(c, s, t, n);
    return u.concat(l);
  });
}
class Od extends ft {
  constructor() {
    super(...arguments), this.allProductions = [];
  }
  visitRepetitionWithSeparator(e) {
    this.allProductions.push(e);
  }
  visitRepetitionMandatory(e) {
    this.allProductions.push(e);
  }
  visitRepetitionMandatoryWithSeparator(e) {
    this.allProductions.push(e);
  }
  visitRepetition(e) {
    this.allProductions.push(e);
  }
}
function Sd(t, e) {
  const n = new Ir();
  t.accept(n);
  const r = n.alternations;
  return ce(r, (i) => i.definition.length > 255 ? [
    {
      message: e.buildTooManyAlternativesError({
        topLevelRule: t,
        alternation: i
      }),
      type: q.TOO_MANY_ALTS,
      ruleName: t.name,
      occurrence: i.idx
    }
  ] : []);
}
function Id(t, e, n) {
  const r = [];
  return S(t, (i) => {
    const s = new Od();
    i.accept(s);
    const o = s.allProductions;
    S(o, (a) => {
      const c = Ns(a), u = a.maxLookahead || e, l = a.idx, h = Sr(l, i, c, u)[0];
      if (D(pe(h))) {
        const f = n.buildEmptyRepetitionError({
          topLevelRule: i,
          repetition: a
        });
        r.push({
          message: f,
          type: q.NO_NON_EMPTY_LOOKAHEAD,
          ruleName: i.name
        });
      }
    });
  }), r;
}
function Nd(t, e, n, r) {
  const i = [], s = re(t, (o, a, c) => (e.definition[c].ignoreAmbiguities === !0 || S(a, (u) => {
    const l = [c];
    S(t, (h, f) => {
      c !== f && Hn(h, u) && // ignore (skip) ambiguities with this "other" alternative
      e.definition[f].ignoreAmbiguities !== !0 && l.push(f);
    }), l.length > 1 && !Hn(i, u) && (i.push(u), o.push({
      alts: l,
      path: u
    }));
  }), o), []);
  return v(s, (o) => {
    const a = v(o.alts, (c) => c + 1);
    return {
      message: r.buildAlternationAmbiguityError({
        topLevelRule: n,
        alternation: e,
        ambiguityIndices: a,
        prefixPath: o.path
      }),
      type: q.AMBIGUOUS_ALTS,
      ruleName: n.name,
      occurrence: e.idx,
      alternatives: o.alts
    };
  });
}
function Ld(t, e, n, r) {
  const i = re(t, (s, o, a) => {
    const c = v(o, (u) => ({ idx: a, path: u }));
    return s.concat(c);
  }, []);
  return kt(ce(i, (s) => {
    if (e.definition[s.idx].ignoreAmbiguities === !0)
      return [];
    const o = s.idx, a = s.path, c = fe(i, (u) => (
      // ignore (skip) ambiguities with this "other" alternative
      e.definition[u.idx].ignoreAmbiguities !== !0 && u.idx < o && // checking for strict prefix because identical lookaheads
      // will be be detected using a different validation.
      pd(u.path, a)
    ));
    return v(c, (u) => {
      const l = [u.idx + 1, o + 1], h = e.idx === 0 ? "" : e.idx;
      return {
        message: r.buildAlternationPrefixAmbiguityError({
          topLevelRule: n,
          alternation: e,
          ambiguityIndices: l,
          prefixPath: u.path
        }),
        type: q.AMBIGUOUS_PREFIX_ALTS,
        ruleName: n.name,
        occurrence: h,
        alternatives: l
      };
    });
  }));
}
function kd(t, e, n) {
  const r = [], i = v(e, (s) => s.name);
  return S(t, (s) => {
    const o = s.name;
    if (Q(i, o)) {
      const a = n.buildNamespaceConflictError(s);
      r.push({
        message: a,
        type: q.CONFLICT_TOKENS_RULES_NAMESPACE,
        ruleName: o
      });
    }
  }), r;
}
function Cd(t) {
  const e = yr(t, {
    errMsgProvider: ed
  }), n = {};
  return S(t.rules, (r) => {
    n[r.name] = r;
  }), td(n, e.errMsgProvider);
}
function bd(t) {
  return t = yr(t, {
    errMsgProvider: Xe
  }), gd(t.rules, t.tokenTypes, t.errMsgProvider, t.grammarName);
}
const Ms = "MismatchedTokenException", Ps = "NoViableAltException", ws = "EarlyExitException", Us = "NotAllInputParsedException", Ds = [
  Ms,
  Ps,
  ws,
  Us
];
Object.freeze(Ds);
function tn(t) {
  return Q(Ds, t.name);
}
class _n extends Error {
  constructor(e, n) {
    super(e), this.token = n, this.resyncedTokens = [], Object.setPrototypeOf(this, new.target.prototype), Error.captureStackTrace && Error.captureStackTrace(this, this.constructor);
  }
}
class $s extends _n {
  constructor(e, n, r) {
    super(e, n), this.previousToken = r, this.name = Ms;
  }
}
class xd extends _n {
  constructor(e, n, r) {
    super(e, n), this.previousToken = r, this.name = Ps;
  }
}
class Md extends _n {
  constructor(e, n) {
    super(e, n), this.name = Us;
  }
}
class Pd extends _n {
  constructor(e, n, r) {
    super(e, n), this.previousToken = r, this.name = ws;
  }
}
const bn = {}, Fs = "InRuleRecoveryException";
class wd extends Error {
  constructor(e) {
    super(e), this.name = Fs;
  }
}
class Ud {
  initRecoverable(e) {
    this.firstAfterRepMap = {}, this.resyncFollows = {}, this.recoveryEnabled = O(e, "recoveryEnabled") ? e.recoveryEnabled : Ue.recoveryEnabled, this.recoveryEnabled && (this.attemptInRepetitionRecovery = Dd);
  }
  getTokenToInsert(e) {
    const n = Rr(e, "", NaN, NaN, NaN, NaN, NaN, NaN);
    return n.isInsertedInRecovery = !0, n;
  }
  canTokenTypeBeInsertedInRecovery(e) {
    return !0;
  }
  canTokenTypeBeDeletedInRecovery(e) {
    return !0;
  }
  tryInRepetitionRecovery(e, n, r, i) {
    const s = this.findReSyncTokenType(), o = this.exportLexerState(), a = [];
    let c = !1;
    const u = this.LA(1);
    let l = this.LA(1);
    const h = () => {
      const f = this.LA(0), A = this.errorMessageProvider.buildMismatchTokenMessage({
        expected: i,
        actual: u,
        previous: f,
        ruleName: this.getCurrRuleFullName()
      }), d = new $s(A, u, this.LA(0));
      d.resyncedTokens = St(a), this.SAVE_ERROR(d);
    };
    for (; !c; )
      if (this.tokenMatcher(l, i)) {
        h();
        return;
      } else if (r.call(this)) {
        h(), e.apply(this, n);
        return;
      } else this.tokenMatcher(l, s) ? c = !0 : (l = this.SKIP_TOKEN(), this.addToResyncTokens(l, a));
    this.importLexerState(o);
  }
  shouldInRepetitionRecoveryBeTried(e, n, r) {
    return !(r === !1 || this.tokenMatcher(this.LA(1), e) || this.isBackTracking() || this.canPerformInRuleRecovery(e, this.getFollowsForInRuleRecovery(e, n)));
  }
  // Error Recovery functionality
  getFollowsForInRuleRecovery(e, n) {
    const r = this.getCurrentGrammarPath(e, n);
    return this.getNextPossibleTokenTypes(r);
  }
  tryInRuleRecovery(e, n) {
    if (this.canRecoverWithSingleTokenInsertion(e, n))
      return this.getTokenToInsert(e);
    if (this.canRecoverWithSingleTokenDeletion(e)) {
      const r = this.SKIP_TOKEN();
      return this.consumeToken(), r;
    }
    throw new wd("sad sad panda");
  }
  canPerformInRuleRecovery(e, n) {
    return this.canRecoverWithSingleTokenInsertion(e, n) || this.canRecoverWithSingleTokenDeletion(e);
  }
  canRecoverWithSingleTokenInsertion(e, n) {
    if (!this.canTokenTypeBeInsertedInRecovery(e) || D(n))
      return !1;
    const r = this.LA(1);
    return lt(n, (i) => this.tokenMatcher(r, i)) !== void 0;
  }
  canRecoverWithSingleTokenDeletion(e) {
    return this.canTokenTypeBeDeletedInRecovery(e) ? this.tokenMatcher(this.LA(2), e) : !1;
  }
  isInCurrentRuleReSyncSet(e) {
    const n = this.getCurrFollowKey(), r = this.getFollowSetFromFollowKey(n);
    return Q(r, e);
  }
  findReSyncTokenType() {
    const e = this.flattenFollowSet();
    let n = this.LA(1), r = 2;
    for (; ; ) {
      const i = lt(e, (s) => Jf(n, s));
      if (i !== void 0)
        return i;
      n = this.LA(r), r++;
    }
  }
  getCurrFollowKey() {
    if (this.RULE_STACK.length === 1)
      return bn;
    const e = this.getLastExplicitRuleShortName(), n = this.getLastExplicitRuleOccurrenceIndex(), r = this.getPreviousExplicitRuleShortName();
    return {
      ruleName: this.shortRuleNameToFullName(e),
      idxInCallingRule: n,
      inRule: this.shortRuleNameToFullName(r)
    };
  }
  buildFullFollowKeyStack() {
    const e = this.RULE_STACK, n = this.RULE_OCCURRENCE_STACK;
    return v(e, (r, i) => i === 0 ? bn : {
      ruleName: this.shortRuleNameToFullName(r),
      idxInCallingRule: n[i],
      inRule: this.shortRuleNameToFullName(e[i - 1])
    });
  }
  flattenFollowSet() {
    const e = v(this.buildFullFollowKeyStack(), (n) => this.getFollowSetFromFollowKey(n));
    return pe(e);
  }
  getFollowSetFromFollowKey(e) {
    if (e === bn)
      return [Ze];
    const n = e.ruleName + e.idxInCallingRule + ms + e.inRule;
    return this.resyncFollows[n];
  }
  // It does not make any sense to include a virtual EOF token in the list of resynced tokens
  // as EOF does not really exist and thus does not contain any useful information (line/column numbers)
  addToResyncTokens(e, n) {
    return this.tokenMatcher(e, Ze) || n.push(e), n;
  }
  reSyncTo(e) {
    const n = [];
    let r = this.LA(1);
    for (; this.tokenMatcher(r, e) === !1; )
      r = this.SKIP_TOKEN(), this.addToResyncTokens(r, n);
    return St(n);
  }
  attemptInRepetitionRecovery(e, n, r, i, s, o, a) {
  }
  getCurrentGrammarPath(e, n) {
    const r = this.getHumanReadableRuleStack(), i = Y(this.RULE_OCCURRENCE_STACK);
    return {
      ruleStack: r,
      occurrenceStack: i,
      lastTok: e,
      lastTokOccurrence: n
    };
  }
  getHumanReadableRuleStack() {
    return v(this.RULE_STACK, (e) => this.shortRuleNameToFullName(e));
  }
}
function Dd(t, e, n, r, i, s, o) {
  const a = this.getKeyForAutomaticLookahead(r, i);
  let c = this.firstAfterRepMap[a];
  if (c === void 0) {
    const f = this.getCurrRuleFullName(), A = this.getGAstProductions()[f];
    c = new s(A, i).startWalking(), this.firstAfterRepMap[a] = c;
  }
  let u = c.token, l = c.occurrence;
  const h = c.isEndOfRule;
  this.RULE_STACK.length === 1 && h && u === void 0 && (u = Ze, l = 1), !(u === void 0 || l === void 0) && this.shouldInRepetitionRecoveryBeTried(u, l, o) && this.tryInRepetitionRecovery(t, e, n, u);
}
const $d = 4, Ke = 8, js = 1 << Ke, Gs = 2 << Ke, Wn = 3 << Ke, zn = 4 << Ke, Yn = 5 << Ke, Wt = 6 << Ke;
function xn(t, e, n) {
  return n | e | t;
}
class Fd {
  constructor(e) {
    var n;
    this.maxLookahead = (n = e?.maxLookahead) !== null && n !== void 0 ? n : Ue.maxLookahead;
  }
  validate(e) {
    const n = this.validateNoLeftRecursion(e.rules);
    if (D(n)) {
      const r = this.validateEmptyOrAlternatives(e.rules), i = this.validateAmbiguousAlternationAlternatives(e.rules, this.maxLookahead), s = this.validateSomeNonEmptyLookaheadPath(e.rules, this.maxLookahead);
      return [
        ...n,
        ...r,
        ...i,
        ...s
      ];
    }
    return n;
  }
  validateNoLeftRecursion(e) {
    return ce(e, (n) => xs(n, n, Xe));
  }
  validateEmptyOrAlternatives(e) {
    return ce(e, (n) => vd(n, Xe));
  }
  validateAmbiguousAlternationAlternatives(e, n) {
    return ce(e, (r) => Rd(r, n, Xe));
  }
  validateSomeNonEmptyLookaheadPath(e, n) {
    return Id(e, n, Xe);
  }
  buildLookaheadForAlternation(e) {
    return cd(e.prodOccurrence, e.rule, e.maxLookahead, e.hasPredicates, e.dynamicTokensEnabled, ld);
  }
  buildLookaheadForOptional(e) {
    return ud(e.prodOccurrence, e.rule, e.maxLookahead, e.dynamicTokensEnabled, Ns(e.prodType), hd);
  }
}
class jd {
  initLooksAhead(e) {
    this.dynamicTokensEnabled = O(e, "dynamicTokensEnabled") ? e.dynamicTokensEnabled : Ue.dynamicTokensEnabled, this.maxLookahead = O(e, "maxLookahead") ? e.maxLookahead : Ue.maxLookahead, this.lookaheadStrategy = O(e, "lookaheadStrategy") ? e.lookaheadStrategy : new Fd({ maxLookahead: this.maxLookahead }), this.lookAheadFuncsCache = /* @__PURE__ */ new Map();
  }
  preComputeLookaheadFunctions(e) {
    S(e, (n) => {
      this.TRACE_INIT(`${n.name} Rule Lookahead`, () => {
        const { alternation: r, repetition: i, option: s, repetitionMandatory: o, repetitionMandatoryWithSeparator: a, repetitionWithSeparator: c } = Bd(n);
        S(r, (u) => {
          const l = u.idx === 0 ? "" : u.idx;
          this.TRACE_INIT(`${ve(u)}${l}`, () => {
            const h = this.lookaheadStrategy.buildLookaheadForAlternation({
              prodOccurrence: u.idx,
              rule: n,
              maxLookahead: u.maxLookahead || this.maxLookahead,
              hasPredicates: u.hasPredicates,
              dynamicTokensEnabled: this.dynamicTokensEnabled
            }), f = xn(this.fullRuleNameToShort[n.name], js, u.idx);
            this.setLaFuncCache(f, h);
          });
        }), S(i, (u) => {
          this.computeLookaheadFunc(n, u.idx, Wn, "Repetition", u.maxLookahead, ve(u));
        }), S(s, (u) => {
          this.computeLookaheadFunc(n, u.idx, Gs, "Option", u.maxLookahead, ve(u));
        }), S(o, (u) => {
          this.computeLookaheadFunc(n, u.idx, zn, "RepetitionMandatory", u.maxLookahead, ve(u));
        }), S(a, (u) => {
          this.computeLookaheadFunc(n, u.idx, Wt, "RepetitionMandatoryWithSeparator", u.maxLookahead, ve(u));
        }), S(c, (u) => {
          this.computeLookaheadFunc(n, u.idx, Yn, "RepetitionWithSeparator", u.maxLookahead, ve(u));
        });
      });
    });
  }
  computeLookaheadFunc(e, n, r, i, s, o) {
    this.TRACE_INIT(`${o}${n === 0 ? "" : n}`, () => {
      const a = this.lookaheadStrategy.buildLookaheadForOptional({
        prodOccurrence: n,
        rule: e,
        maxLookahead: s || this.maxLookahead,
        dynamicTokensEnabled: this.dynamicTokensEnabled,
        prodType: i
      }), c = xn(this.fullRuleNameToShort[e.name], r, n);
      this.setLaFuncCache(c, a);
    });
  }
  // this actually returns a number, but it is always used as a string (object prop key)
  getKeyForAutomaticLookahead(e, n) {
    const r = this.getLastExplicitRuleShortName();
    return xn(r, e, n);
  }
  getLaFuncFromCache(e) {
    return this.lookAheadFuncsCache.get(e);
  }
  /* istanbul ignore next */
  setLaFuncCache(e, n) {
    this.lookAheadFuncsCache.set(e, n);
  }
}
class Gd extends ft {
  constructor() {
    super(...arguments), this.dslMethods = {
      option: [],
      alternation: [],
      repetition: [],
      repetitionWithSeparator: [],
      repetitionMandatory: [],
      repetitionMandatoryWithSeparator: []
    };
  }
  reset() {
    this.dslMethods = {
      option: [],
      alternation: [],
      repetition: [],
      repetitionWithSeparator: [],
      repetitionMandatory: [],
      repetitionMandatoryWithSeparator: []
    };
  }
  visitOption(e) {
    this.dslMethods.option.push(e);
  }
  visitRepetitionWithSeparator(e) {
    this.dslMethods.repetitionWithSeparator.push(e);
  }
  visitRepetitionMandatory(e) {
    this.dslMethods.repetitionMandatory.push(e);
  }
  visitRepetitionMandatoryWithSeparator(e) {
    this.dslMethods.repetitionMandatoryWithSeparator.push(e);
  }
  visitRepetition(e) {
    this.dslMethods.repetition.push(e);
  }
  visitAlternation(e) {
    this.dslMethods.alternation.push(e);
  }
}
const Ft = new Gd();
function Bd(t) {
  Ft.reset(), t.accept(Ft);
  const e = Ft.dslMethods;
  return Ft.reset(), e;
}
function ki(t, e) {
  isNaN(t.startOffset) === !0 ? (t.startOffset = e.startOffset, t.endOffset = e.endOffset) : t.endOffset < e.endOffset && (t.endOffset = e.endOffset);
}
function Ci(t, e) {
  isNaN(t.startOffset) === !0 ? (t.startOffset = e.startOffset, t.startColumn = e.startColumn, t.startLine = e.startLine, t.endOffset = e.endOffset, t.endColumn = e.endColumn, t.endLine = e.endLine) : t.endOffset < e.endOffset && (t.endOffset = e.endOffset, t.endColumn = e.endColumn, t.endLine = e.endLine);
}
function Vd(t, e, n) {
  t.children[n] === void 0 ? t.children[n] = [e] : t.children[n].push(e);
}
function Kd(t, e, n) {
  t.children[e] === void 0 ? t.children[e] = [n] : t.children[e].push(n);
}
const Hd = "name";
function Bs(t, e) {
  Object.defineProperty(t, Hd, {
    enumerable: !1,
    configurable: !0,
    writable: !1,
    value: e
  });
}
function Wd(t, e) {
  const n = he(t), r = n.length;
  for (let i = 0; i < r; i++) {
    const s = n[i], o = t[s], a = o.length;
    for (let c = 0; c < a; c++) {
      const u = o[c];
      u.tokenTypeIdx === void 0 && this[u.name](u.children, e);
    }
  }
}
function zd(t, e) {
  const n = function() {
  };
  Bs(n, t + "BaseSemantics");
  const r = {
    visit: function(i, s) {
      if (x(i) && (i = i[0]), !we(i))
        return this[i.name](i.children, s);
    },
    validateVisitor: function() {
      const i = Xd(this, e);
      if (!D(i)) {
        const s = v(i, (o) => o.msg);
        throw Error(`Errors Detected in CST Visitor <${this.constructor.name}>:
	${s.join(`

`).replace(/\n/g, `
	`)}`);
      }
    }
  };
  return n.prototype = r, n.prototype.constructor = n, n._RULE_NAMES = e, n;
}
function Yd(t, e, n) {
  const r = function() {
  };
  Bs(r, t + "BaseSemanticsWithDefaults");
  const i = Object.create(n.prototype);
  return S(e, (s) => {
    i[s] = Wd;
  }), r.prototype = i, r.prototype.constructor = r, r;
}
var Xn;
(function(t) {
  t[t.REDUNDANT_METHOD = 0] = "REDUNDANT_METHOD", t[t.MISSING_METHOD = 1] = "MISSING_METHOD";
})(Xn || (Xn = {}));
function Xd(t, e) {
  return qd(t, e);
}
function qd(t, e) {
  const n = fe(e, (i) => De(t[i]) === !1), r = v(n, (i) => ({
    msg: `Missing visitor method: <${i}> on ${t.constructor.name} CST Visitor.`,
    type: Xn.MISSING_METHOD,
    methodName: i
  }));
  return kt(r);
}
class Qd {
  initTreeBuilder(e) {
    if (this.CST_STACK = [], this.outputCst = e.outputCst, this.nodeLocationTracking = O(e, "nodeLocationTracking") ? e.nodeLocationTracking : Ue.nodeLocationTracking, !this.outputCst)
      this.cstInvocationStateUpdate = K, this.cstFinallyStateUpdate = K, this.cstPostTerminal = K, this.cstPostNonTerminal = K, this.cstPostRule = K;
    else if (/full/i.test(this.nodeLocationTracking))
      this.recoveryEnabled ? (this.setNodeLocationFromToken = Ci, this.setNodeLocationFromNode = Ci, this.cstPostRule = K, this.setInitialNodeLocation = this.setInitialNodeLocationFullRecovery) : (this.setNodeLocationFromToken = K, this.setNodeLocationFromNode = K, this.cstPostRule = this.cstPostRuleFull, this.setInitialNodeLocation = this.setInitialNodeLocationFullRegular);
    else if (/onlyOffset/i.test(this.nodeLocationTracking))
      this.recoveryEnabled ? (this.setNodeLocationFromToken = ki, this.setNodeLocationFromNode = ki, this.cstPostRule = K, this.setInitialNodeLocation = this.setInitialNodeLocationOnlyOffsetRecovery) : (this.setNodeLocationFromToken = K, this.setNodeLocationFromNode = K, this.cstPostRule = this.cstPostRuleOnlyOffset, this.setInitialNodeLocation = this.setInitialNodeLocationOnlyOffsetRegular);
    else if (/none/i.test(this.nodeLocationTracking))
      this.setNodeLocationFromToken = K, this.setNodeLocationFromNode = K, this.cstPostRule = K, this.setInitialNodeLocation = K;
    else
      throw Error(`Invalid <nodeLocationTracking> config option: "${e.nodeLocationTracking}"`);
  }
  setInitialNodeLocationOnlyOffsetRecovery(e) {
    e.location = {
      startOffset: NaN,
      endOffset: NaN
    };
  }
  setInitialNodeLocationOnlyOffsetRegular(e) {
    e.location = {
      // without error recovery the starting Location of a new CstNode is guaranteed
      // To be the next Token's startOffset (for valid inputs).
      // For invalid inputs there won't be any CSTOutput so this potential
      // inaccuracy does not matter
      startOffset: this.LA(1).startOffset,
      endOffset: NaN
    };
  }
  setInitialNodeLocationFullRecovery(e) {
    e.location = {
      startOffset: NaN,
      startLine: NaN,
      startColumn: NaN,
      endOffset: NaN,
      endLine: NaN,
      endColumn: NaN
    };
  }
  /**
       *  @see setInitialNodeLocationOnlyOffsetRegular for explanation why this work
  
       * @param cstNode
       */
  setInitialNodeLocationFullRegular(e) {
    const n = this.LA(1);
    e.location = {
      startOffset: n.startOffset,
      startLine: n.startLine,
      startColumn: n.startColumn,
      endOffset: NaN,
      endLine: NaN,
      endColumn: NaN
    };
  }
  cstInvocationStateUpdate(e) {
    const n = {
      name: e,
      children: /* @__PURE__ */ Object.create(null)
    };
    this.setInitialNodeLocation(n), this.CST_STACK.push(n);
  }
  cstFinallyStateUpdate() {
    this.CST_STACK.pop();
  }
  cstPostRuleFull(e) {
    const n = this.LA(0), r = e.location;
    r.startOffset <= n.startOffset ? (r.endOffset = n.endOffset, r.endLine = n.endLine, r.endColumn = n.endColumn) : (r.startOffset = NaN, r.startLine = NaN, r.startColumn = NaN);
  }
  cstPostRuleOnlyOffset(e) {
    const n = this.LA(0), r = e.location;
    r.startOffset <= n.startOffset ? r.endOffset = n.endOffset : r.startOffset = NaN;
  }
  cstPostTerminal(e, n) {
    const r = this.CST_STACK[this.CST_STACK.length - 1];
    Vd(r, n, e), this.setNodeLocationFromToken(r.location, n);
  }
  cstPostNonTerminal(e, n) {
    const r = this.CST_STACK[this.CST_STACK.length - 1];
    Kd(r, n, e), this.setNodeLocationFromNode(r.location, e.location);
  }
  getBaseCstVisitorConstructor() {
    if (we(this.baseCstVisitorConstructor)) {
      const e = zd(this.className, he(this.gastProductionsCache));
      return this.baseCstVisitorConstructor = e, e;
    }
    return this.baseCstVisitorConstructor;
  }
  getBaseCstVisitorConstructorWithDefaults() {
    if (we(this.baseCstVisitorWithDefaultsConstructor)) {
      const e = Yd(this.className, he(this.gastProductionsCache), this.getBaseCstVisitorConstructor());
      return this.baseCstVisitorWithDefaultsConstructor = e, e;
    }
    return this.baseCstVisitorWithDefaultsConstructor;
  }
  getLastExplicitRuleShortName() {
    const e = this.RULE_STACK;
    return e[e.length - 1];
  }
  getPreviousExplicitRuleShortName() {
    const e = this.RULE_STACK;
    return e[e.length - 2];
  }
  getLastExplicitRuleOccurrenceIndex() {
    const e = this.RULE_OCCURRENCE_STACK;
    return e[e.length - 1];
  }
}
class Zd {
  initLexerAdapter() {
    this.tokVector = [], this.tokVectorLength = 0, this.currIdx = -1;
  }
  set input(e) {
    if (this.selfAnalysisDone !== !0)
      throw Error("Missing <performSelfAnalysis> invocation at the end of the Parser's constructor.");
    this.reset(), this.tokVector = e, this.tokVectorLength = e.length;
  }
  get input() {
    return this.tokVector;
  }
  // skips a token and returns the next token
  SKIP_TOKEN() {
    return this.currIdx <= this.tokVector.length - 2 ? (this.consumeToken(), this.LA(1)) : rn;
  }
  // Lexer (accessing Token vector) related methods which can be overridden to implement lazy lexers
  // or lexers dependent on parser context.
  LA(e) {
    const n = this.currIdx + e;
    return n < 0 || this.tokVectorLength <= n ? rn : this.tokVector[n];
  }
  consumeToken() {
    this.currIdx++;
  }
  exportLexerState() {
    return this.currIdx;
  }
  importLexerState(e) {
    this.currIdx = e;
  }
  resetLexerState() {
    this.currIdx = -1;
  }
  moveToTerminatedState() {
    this.currIdx = this.tokVector.length - 1;
  }
  getLexerPosition() {
    return this.exportLexerState();
  }
}
class Jd {
  ACTION(e) {
    return e.call(this);
  }
  consume(e, n, r) {
    return this.consumeInternal(n, e, r);
  }
  subrule(e, n, r) {
    return this.subruleInternal(n, e, r);
  }
  option(e, n) {
    return this.optionInternal(n, e);
  }
  or(e, n) {
    return this.orInternal(n, e);
  }
  many(e, n) {
    return this.manyInternal(e, n);
  }
  atLeastOne(e, n) {
    return this.atLeastOneInternal(e, n);
  }
  CONSUME(e, n) {
    return this.consumeInternal(e, 0, n);
  }
  CONSUME1(e, n) {
    return this.consumeInternal(e, 1, n);
  }
  CONSUME2(e, n) {
    return this.consumeInternal(e, 2, n);
  }
  CONSUME3(e, n) {
    return this.consumeInternal(e, 3, n);
  }
  CONSUME4(e, n) {
    return this.consumeInternal(e, 4, n);
  }
  CONSUME5(e, n) {
    return this.consumeInternal(e, 5, n);
  }
  CONSUME6(e, n) {
    return this.consumeInternal(e, 6, n);
  }
  CONSUME7(e, n) {
    return this.consumeInternal(e, 7, n);
  }
  CONSUME8(e, n) {
    return this.consumeInternal(e, 8, n);
  }
  CONSUME9(e, n) {
    return this.consumeInternal(e, 9, n);
  }
  SUBRULE(e, n) {
    return this.subruleInternal(e, 0, n);
  }
  SUBRULE1(e, n) {
    return this.subruleInternal(e, 1, n);
  }
  SUBRULE2(e, n) {
    return this.subruleInternal(e, 2, n);
  }
  SUBRULE3(e, n) {
    return this.subruleInternal(e, 3, n);
  }
  SUBRULE4(e, n) {
    return this.subruleInternal(e, 4, n);
  }
  SUBRULE5(e, n) {
    return this.subruleInternal(e, 5, n);
  }
  SUBRULE6(e, n) {
    return this.subruleInternal(e, 6, n);
  }
  SUBRULE7(e, n) {
    return this.subruleInternal(e, 7, n);
  }
  SUBRULE8(e, n) {
    return this.subruleInternal(e, 8, n);
  }
  SUBRULE9(e, n) {
    return this.subruleInternal(e, 9, n);
  }
  OPTION(e) {
    return this.optionInternal(e, 0);
  }
  OPTION1(e) {
    return this.optionInternal(e, 1);
  }
  OPTION2(e) {
    return this.optionInternal(e, 2);
  }
  OPTION3(e) {
    return this.optionInternal(e, 3);
  }
  OPTION4(e) {
    return this.optionInternal(e, 4);
  }
  OPTION5(e) {
    return this.optionInternal(e, 5);
  }
  OPTION6(e) {
    return this.optionInternal(e, 6);
  }
  OPTION7(e) {
    return this.optionInternal(e, 7);
  }
  OPTION8(e) {
    return this.optionInternal(e, 8);
  }
  OPTION9(e) {
    return this.optionInternal(e, 9);
  }
  OR(e) {
    return this.orInternal(e, 0);
  }
  OR1(e) {
    return this.orInternal(e, 1);
  }
  OR2(e) {
    return this.orInternal(e, 2);
  }
  OR3(e) {
    return this.orInternal(e, 3);
  }
  OR4(e) {
    return this.orInternal(e, 4);
  }
  OR5(e) {
    return this.orInternal(e, 5);
  }
  OR6(e) {
    return this.orInternal(e, 6);
  }
  OR7(e) {
    return this.orInternal(e, 7);
  }
  OR8(e) {
    return this.orInternal(e, 8);
  }
  OR9(e) {
    return this.orInternal(e, 9);
  }
  MANY(e) {
    this.manyInternal(0, e);
  }
  MANY1(e) {
    this.manyInternal(1, e);
  }
  MANY2(e) {
    this.manyInternal(2, e);
  }
  MANY3(e) {
    this.manyInternal(3, e);
  }
  MANY4(e) {
    this.manyInternal(4, e);
  }
  MANY5(e) {
    this.manyInternal(5, e);
  }
  MANY6(e) {
    this.manyInternal(6, e);
  }
  MANY7(e) {
    this.manyInternal(7, e);
  }
  MANY8(e) {
    this.manyInternal(8, e);
  }
  MANY9(e) {
    this.manyInternal(9, e);
  }
  MANY_SEP(e) {
    this.manySepFirstInternal(0, e);
  }
  MANY_SEP1(e) {
    this.manySepFirstInternal(1, e);
  }
  MANY_SEP2(e) {
    this.manySepFirstInternal(2, e);
  }
  MANY_SEP3(e) {
    this.manySepFirstInternal(3, e);
  }
  MANY_SEP4(e) {
    this.manySepFirstInternal(4, e);
  }
  MANY_SEP5(e) {
    this.manySepFirstInternal(5, e);
  }
  MANY_SEP6(e) {
    this.manySepFirstInternal(6, e);
  }
  MANY_SEP7(e) {
    this.manySepFirstInternal(7, e);
  }
  MANY_SEP8(e) {
    this.manySepFirstInternal(8, e);
  }
  MANY_SEP9(e) {
    this.manySepFirstInternal(9, e);
  }
  AT_LEAST_ONE(e) {
    this.atLeastOneInternal(0, e);
  }
  AT_LEAST_ONE1(e) {
    return this.atLeastOneInternal(1, e);
  }
  AT_LEAST_ONE2(e) {
    this.atLeastOneInternal(2, e);
  }
  AT_LEAST_ONE3(e) {
    this.atLeastOneInternal(3, e);
  }
  AT_LEAST_ONE4(e) {
    this.atLeastOneInternal(4, e);
  }
  AT_LEAST_ONE5(e) {
    this.atLeastOneInternal(5, e);
  }
  AT_LEAST_ONE6(e) {
    this.atLeastOneInternal(6, e);
  }
  AT_LEAST_ONE7(e) {
    this.atLeastOneInternal(7, e);
  }
  AT_LEAST_ONE8(e) {
    this.atLeastOneInternal(8, e);
  }
  AT_LEAST_ONE9(e) {
    this.atLeastOneInternal(9, e);
  }
  AT_LEAST_ONE_SEP(e) {
    this.atLeastOneSepFirstInternal(0, e);
  }
  AT_LEAST_ONE_SEP1(e) {
    this.atLeastOneSepFirstInternal(1, e);
  }
  AT_LEAST_ONE_SEP2(e) {
    this.atLeastOneSepFirstInternal(2, e);
  }
  AT_LEAST_ONE_SEP3(e) {
    this.atLeastOneSepFirstInternal(3, e);
  }
  AT_LEAST_ONE_SEP4(e) {
    this.atLeastOneSepFirstInternal(4, e);
  }
  AT_LEAST_ONE_SEP5(e) {
    this.atLeastOneSepFirstInternal(5, e);
  }
  AT_LEAST_ONE_SEP6(e) {
    this.atLeastOneSepFirstInternal(6, e);
  }
  AT_LEAST_ONE_SEP7(e) {
    this.atLeastOneSepFirstInternal(7, e);
  }
  AT_LEAST_ONE_SEP8(e) {
    this.atLeastOneSepFirstInternal(8, e);
  }
  AT_LEAST_ONE_SEP9(e) {
    this.atLeastOneSepFirstInternal(9, e);
  }
  RULE(e, n, r = sn) {
    if (Q(this.definedRulesNames, e)) {
      const s = {
        message: Xe.buildDuplicateRuleNameError({
          topLevelRule: e,
          grammarName: this.className
        }),
        type: q.DUPLICATE_RULE_NAME,
        ruleName: e
      };
      this.definitionErrors.push(s);
    }
    this.definedRulesNames.push(e);
    const i = this.defineRule(e, n, r);
    return this[e] = i, i;
  }
  OVERRIDE_RULE(e, n, r = sn) {
    const i = _d(e, this.definedRulesNames, this.className);
    this.definitionErrors = this.definitionErrors.concat(i);
    const s = this.defineRule(e, n, r);
    return this[e] = s, s;
  }
  BACKTRACK(e, n) {
    return function() {
      this.isBackTrackingStack.push(1);
      const r = this.saveRecogState();
      try {
        return e.apply(this, n), !0;
      } catch (i) {
        if (tn(i))
          return !1;
        throw i;
      } finally {
        this.reloadRecogState(r), this.isBackTrackingStack.pop();
      }
    };
  }
  // GAST export APIs
  getGAstProductions() {
    return this.gastProductionsCache;
  }
  getSerializedGastProductions() {
    return ef(H(this.gastProductionsCache));
  }
}
class ep {
  initRecognizerEngine(e, n) {
    if (this.className = this.constructor.name, this.shortRuleNameToFull = {}, this.fullRuleNameToShort = {}, this.ruleShortNameIdx = 256, this.tokenMatcher = en, this.subruleIdx = 0, this.definedRulesNames = [], this.tokensMap = {}, this.isBackTrackingStack = [], this.RULE_STACK = [], this.RULE_OCCURRENCE_STACK = [], this.gastProductionsCache = {}, O(n, "serializedGrammar"))
      throw Error(`The Parser's configuration can no longer contain a <serializedGrammar> property.
	See: https://chevrotain.io/docs/changes/BREAKING_CHANGES.html#_6-0-0
	For Further details.`);
    if (x(e)) {
      if (D(e))
        throw Error(`A Token Vocabulary cannot be empty.
	Note that the first argument for the parser constructor
	is no longer a Token vector (since v4.0).`);
      if (typeof e[0].startOffset == "number")
        throw Error(`The Parser constructor no longer accepts a token vector as the first argument.
	See: https://chevrotain.io/docs/changes/BREAKING_CHANGES.html#_4-0-0
	For Further details.`);
    }
    if (x(e))
      this.tokensMap = re(e, (s, o) => (s[o.name] = o, s), {});
    else if (O(e, "modes") && me(pe(H(e.modes)), Xf)) {
      const s = pe(H(e.modes)), o = Ar(s);
      this.tokensMap = re(o, (a, c) => (a[c.name] = c, a), {});
    } else if (le(e))
      this.tokensMap = Y(e);
    else
      throw new Error("<tokensDictionary> argument must be An Array of Token constructors, A dictionary of Token constructors or an IMultiModeLexerDefinition");
    this.tokensMap.EOF = Ze;
    const r = O(e, "modes") ? pe(H(e.modes)) : H(e), i = me(r, (s) => D(s.categoryMatches));
    this.tokenMatcher = i ? en : bt, xt(H(this.tokensMap));
  }
  defineRule(e, n, r) {
    if (this.selfAnalysisDone)
      throw Error(`Grammar rule <${e}> may not be defined after the 'performSelfAnalysis' method has been called'
Make sure that all grammar rule definitions are done before 'performSelfAnalysis' is called.`);
    const i = O(r, "resyncEnabled") ? r.resyncEnabled : sn.resyncEnabled, s = O(r, "recoveryValueFunc") ? r.recoveryValueFunc : sn.recoveryValueFunc, o = this.ruleShortNameIdx << $d + Ke;
    this.ruleShortNameIdx++, this.shortRuleNameToFull[o] = e, this.fullRuleNameToShort[e] = o;
    let a;
    return this.outputCst === !0 ? a = function(...c) {
      try {
        this.ruleInvocationStateUpdate(o, e, this.subruleIdx), n.apply(this, c);
        const u = this.CST_STACK[this.CST_STACK.length - 1];
        return this.cstPostRule(u), u;
      } catch (u) {
        return this.invokeRuleCatch(u, i, s);
      } finally {
        this.ruleFinallyStateUpdate();
      }
    } : a = function(...c) {
      try {
        return this.ruleInvocationStateUpdate(o, e, this.subruleIdx), n.apply(this, c);
      } catch (u) {
        return this.invokeRuleCatch(u, i, s);
      } finally {
        this.ruleFinallyStateUpdate();
      }
    }, Object.assign(a, { ruleName: e, originalGrammarAction: n });
  }
  invokeRuleCatch(e, n, r) {
    const i = this.RULE_STACK.length === 1, s = n && !this.isBackTracking() && this.recoveryEnabled;
    if (tn(e)) {
      const o = e;
      if (s) {
        const a = this.findReSyncTokenType();
        if (this.isInCurrentRuleReSyncSet(a))
          if (o.resyncedTokens = this.reSyncTo(a), this.outputCst) {
            const c = this.CST_STACK[this.CST_STACK.length - 1];
            return c.recoveredNode = !0, c;
          } else
            return r(e);
        else {
          if (this.outputCst) {
            const c = this.CST_STACK[this.CST_STACK.length - 1];
            c.recoveredNode = !0, o.partialCstResult = c;
          }
          throw o;
        }
      } else {
        if (i)
          return this.moveToTerminatedState(), r(e);
        throw o;
      }
    } else
      throw e;
  }
  // Implementation of parsing DSL
  optionInternal(e, n) {
    const r = this.getKeyForAutomaticLookahead(Gs, n);
    return this.optionInternalLogic(e, n, r);
  }
  optionInternalLogic(e, n, r) {
    let i = this.getLaFuncFromCache(r), s;
    if (typeof e != "function") {
      s = e.DEF;
      const o = e.GATE;
      if (o !== void 0) {
        const a = i;
        i = () => o.call(this) && a.call(this);
      }
    } else
      s = e;
    if (i.call(this) === !0)
      return s.call(this);
  }
  atLeastOneInternal(e, n) {
    const r = this.getKeyForAutomaticLookahead(zn, e);
    return this.atLeastOneInternalLogic(e, n, r);
  }
  atLeastOneInternalLogic(e, n, r) {
    let i = this.getLaFuncFromCache(r), s;
    if (typeof n != "function") {
      s = n.DEF;
      const o = n.GATE;
      if (o !== void 0) {
        const a = i;
        i = () => o.call(this) && a.call(this);
      }
    } else
      s = n;
    if (i.call(this) === !0) {
      let o = this.doSingleRepetition(s);
      for (; i.call(this) === !0 && o === !0; )
        o = this.doSingleRepetition(s);
    } else
      throw this.raiseEarlyExitException(e, G.REPETITION_MANDATORY, n.ERR_MSG);
    this.attemptInRepetitionRecovery(this.atLeastOneInternal, [e, n], i, zn, e, od);
  }
  atLeastOneSepFirstInternal(e, n) {
    const r = this.getKeyForAutomaticLookahead(Wt, e);
    this.atLeastOneSepFirstInternalLogic(e, n, r);
  }
  atLeastOneSepFirstInternalLogic(e, n, r) {
    const i = n.DEF, s = n.SEP;
    if (this.getLaFuncFromCache(r).call(this) === !0) {
      i.call(this);
      const o = () => this.tokenMatcher(this.LA(1), s);
      for (; this.tokenMatcher(this.LA(1), s) === !0; )
        this.CONSUME(s), i.call(this);
      this.attemptInRepetitionRecovery(this.repetitionSepSecondInternal, [
        e,
        s,
        o,
        i,
        Ni
      ], o, Wt, e, Ni);
    } else
      throw this.raiseEarlyExitException(e, G.REPETITION_MANDATORY_WITH_SEPARATOR, n.ERR_MSG);
  }
  manyInternal(e, n) {
    const r = this.getKeyForAutomaticLookahead(Wn, e);
    return this.manyInternalLogic(e, n, r);
  }
  manyInternalLogic(e, n, r) {
    let i = this.getLaFuncFromCache(r), s;
    if (typeof n != "function") {
      s = n.DEF;
      const a = n.GATE;
      if (a !== void 0) {
        const c = i;
        i = () => a.call(this) && c.call(this);
      }
    } else
      s = n;
    let o = !0;
    for (; i.call(this) === !0 && o === !0; )
      o = this.doSingleRepetition(s);
    this.attemptInRepetitionRecovery(
      this.manyInternal,
      [e, n],
      i,
      Wn,
      e,
      sd,
      // The notStuck parameter is only relevant when "attemptInRepetitionRecovery"
      // is invoked from manyInternal, in the MANY_SEP case and AT_LEAST_ONE[_SEP]
      // An infinite loop cannot occur as:
      // - Either the lookahead is guaranteed to consume something (Single Token Separator)
      // - AT_LEAST_ONE by definition is guaranteed to consume something (or error out).
      o
    );
  }
  manySepFirstInternal(e, n) {
    const r = this.getKeyForAutomaticLookahead(Yn, e);
    this.manySepFirstInternalLogic(e, n, r);
  }
  manySepFirstInternalLogic(e, n, r) {
    const i = n.DEF, s = n.SEP;
    if (this.getLaFuncFromCache(r).call(this) === !0) {
      i.call(this);
      const o = () => this.tokenMatcher(this.LA(1), s);
      for (; this.tokenMatcher(this.LA(1), s) === !0; )
        this.CONSUME(s), i.call(this);
      this.attemptInRepetitionRecovery(this.repetitionSepSecondInternal, [
        e,
        s,
        o,
        i,
        Ii
      ], o, Yn, e, Ii);
    }
  }
  repetitionSepSecondInternal(e, n, r, i, s) {
    for (; r(); )
      this.CONSUME(n), i.call(this);
    this.attemptInRepetitionRecovery(this.repetitionSepSecondInternal, [
      e,
      n,
      r,
      i,
      s
    ], r, Wt, e, s);
  }
  doSingleRepetition(e) {
    const n = this.getLexerPosition();
    return e.call(this), this.getLexerPosition() > n;
  }
  orInternal(e, n) {
    const r = this.getKeyForAutomaticLookahead(js, n), i = x(e) ? e : e.DEF, s = this.getLaFuncFromCache(r).call(this, i);
    if (s !== void 0)
      return i[s].ALT.call(this);
    this.raiseNoAltException(n, e.ERR_MSG);
  }
  ruleFinallyStateUpdate() {
    if (this.RULE_STACK.pop(), this.RULE_OCCURRENCE_STACK.pop(), this.cstFinallyStateUpdate(), this.RULE_STACK.length === 0 && this.isAtEndOfInput() === !1) {
      const e = this.LA(1), n = this.errorMessageProvider.buildNotAllInputParsedMessage({
        firstRedundant: e,
        ruleName: this.getCurrRuleFullName()
      });
      this.SAVE_ERROR(new Md(n, e));
    }
  }
  subruleInternal(e, n, r) {
    let i;
    try {
      const s = r !== void 0 ? r.ARGS : void 0;
      return this.subruleIdx = n, i = e.apply(this, s), this.cstPostNonTerminal(i, r !== void 0 && r.LABEL !== void 0 ? r.LABEL : e.ruleName), i;
    } catch (s) {
      throw this.subruleInternalError(s, r, e.ruleName);
    }
  }
  subruleInternalError(e, n, r) {
    throw tn(e) && e.partialCstResult !== void 0 && (this.cstPostNonTerminal(e.partialCstResult, n !== void 0 && n.LABEL !== void 0 ? n.LABEL : r), delete e.partialCstResult), e;
  }
  consumeInternal(e, n, r) {
    let i;
    try {
      const s = this.LA(1);
      this.tokenMatcher(s, e) === !0 ? (this.consumeToken(), i = s) : this.consumeInternalError(e, s, r);
    } catch (s) {
      i = this.consumeInternalRecovery(e, n, s);
    }
    return this.cstPostTerminal(r !== void 0 && r.LABEL !== void 0 ? r.LABEL : e.name, i), i;
  }
  consumeInternalError(e, n, r) {
    let i;
    const s = this.LA(0);
    throw r !== void 0 && r.ERR_MSG ? i = r.ERR_MSG : i = this.errorMessageProvider.buildMismatchTokenMessage({
      expected: e,
      actual: n,
      previous: s,
      ruleName: this.getCurrRuleFullName()
    }), this.SAVE_ERROR(new $s(i, n, s));
  }
  consumeInternalRecovery(e, n, r) {
    if (this.recoveryEnabled && // TODO: more robust checking of the exception type. Perhaps Typescript extending expressions?
    r.name === "MismatchedTokenException" && !this.isBackTracking()) {
      const i = this.getFollowsForInRuleRecovery(e, n);
      try {
        return this.tryInRuleRecovery(e, i);
      } catch (s) {
        throw s.name === Fs ? r : s;
      }
    } else
      throw r;
  }
  saveRecogState() {
    const e = this.errors, n = Y(this.RULE_STACK);
    return {
      errors: e,
      lexerState: this.exportLexerState(),
      RULE_STACK: n,
      CST_STACK: this.CST_STACK
    };
  }
  reloadRecogState(e) {
    this.errors = e.errors, this.importLexerState(e.lexerState), this.RULE_STACK = e.RULE_STACK;
  }
  ruleInvocationStateUpdate(e, n, r) {
    this.RULE_OCCURRENCE_STACK.push(r), this.RULE_STACK.push(e), this.cstInvocationStateUpdate(n);
  }
  isBackTracking() {
    return this.isBackTrackingStack.length !== 0;
  }
  getCurrRuleFullName() {
    const e = this.getLastExplicitRuleShortName();
    return this.shortRuleNameToFull[e];
  }
  shortRuleNameToFullName(e) {
    return this.shortRuleNameToFull[e];
  }
  isAtEndOfInput() {
    return this.tokenMatcher(this.LA(1), Ze);
  }
  reset() {
    this.resetLexerState(), this.subruleIdx = 0, this.isBackTrackingStack = [], this.errors = [], this.RULE_STACK = [], this.CST_STACK = [], this.RULE_OCCURRENCE_STACK = [];
  }
}
class tp {
  initErrorHandler(e) {
    this._errors = [], this.errorMessageProvider = O(e, "errorMessageProvider") ? e.errorMessageProvider : Ue.errorMessageProvider;
  }
  SAVE_ERROR(e) {
    if (tn(e))
      return e.context = {
        ruleStack: this.getHumanReadableRuleStack(),
        ruleOccurrenceStack: Y(this.RULE_OCCURRENCE_STACK)
      }, this._errors.push(e), e;
    throw Error("Trying to save an Error which is not a RecognitionException");
  }
  get errors() {
    return Y(this._errors);
  }
  set errors(e) {
    this._errors = e;
  }
  // TODO: consider caching the error message computed information
  raiseEarlyExitException(e, n, r) {
    const i = this.getCurrRuleFullName(), s = this.getGAstProductions()[i], o = Sr(e, s, n, this.maxLookahead)[0], a = [];
    for (let u = 1; u <= this.maxLookahead; u++)
      a.push(this.LA(u));
    const c = this.errorMessageProvider.buildEarlyExitMessage({
      expectedIterationPaths: o,
      actual: a,
      previous: this.LA(0),
      customUserDescription: r,
      ruleName: i
    });
    throw this.SAVE_ERROR(new Pd(c, this.LA(1), this.LA(0)));
  }
  // TODO: consider caching the error message computed information
  raiseNoAltException(e, n) {
    const r = this.getCurrRuleFullName(), i = this.getGAstProductions()[r], s = Or(e, i, this.maxLookahead), o = [];
    for (let u = 1; u <= this.maxLookahead; u++)
      o.push(this.LA(u));
    const a = this.LA(0), c = this.errorMessageProvider.buildNoViableAltMessage({
      expectedPathsPerAlt: s,
      actual: o,
      previous: a,
      customUserDescription: n,
      ruleName: this.getCurrRuleFullName()
    });
    throw this.SAVE_ERROR(new xd(c, this.LA(1), a));
  }
}
class np {
  initContentAssist() {
  }
  computeContentAssist(e, n) {
    const r = this.gastProductionsCache[e];
    if (we(r))
      throw Error(`Rule ->${e}<- does not exist in this grammar.`);
    return Is([r], n, this.tokenMatcher, this.maxLookahead);
  }
  // TODO: should this be a member method or a utility? it does not have any state or usage of 'this'...
  // TODO: should this be more explicitly part of the public API?
  getNextPossibleTokenTypes(e) {
    const n = Ee(e.ruleStack), r = this.getGAstProductions()[n];
    return new id(r, e).startWalking();
  }
}
const vn = {
  description: "This Object indicates the Parser is during Recording Phase"
};
Object.freeze(vn);
const bi = !0, xi = Math.pow(2, Ke) - 1, Vs = M({ name: "RECORDING_PHASE_TOKEN", pattern: X.NA });
xt([Vs]);
const Ks = Rr(
  Vs,
  `This IToken indicates the Parser is in Recording Phase
	See: https://chevrotain.io/docs/guide/internals.html#grammar-recording for details`,
  // Using "-1" instead of NaN (as in EOF) because an actual number is less likely to
  // cause errors if the output of LA or CONSUME would be (incorrectly) used during the recording phase.
  -1,
  -1,
  -1,
  -1,
  -1,
  -1
);
Object.freeze(Ks);
const rp = {
  name: `This CSTNode indicates the Parser is in Recording Phase
	See: https://chevrotain.io/docs/guide/internals.html#grammar-recording for details`,
  children: {}
};
class ip {
  initGastRecorder(e) {
    this.recordingProdStack = [], this.RECORDING_PHASE = !1;
  }
  enableRecording() {
    this.RECORDING_PHASE = !0, this.TRACE_INIT("Enable Recording", () => {
      for (let e = 0; e < 10; e++) {
        const n = e > 0 ? e : "";
        this[`CONSUME${n}`] = function(r, i) {
          return this.consumeInternalRecord(r, e, i);
        }, this[`SUBRULE${n}`] = function(r, i) {
          return this.subruleInternalRecord(r, e, i);
        }, this[`OPTION${n}`] = function(r) {
          return this.optionInternalRecord(r, e);
        }, this[`OR${n}`] = function(r) {
          return this.orInternalRecord(r, e);
        }, this[`MANY${n}`] = function(r) {
          this.manyInternalRecord(e, r);
        }, this[`MANY_SEP${n}`] = function(r) {
          this.manySepFirstInternalRecord(e, r);
        }, this[`AT_LEAST_ONE${n}`] = function(r) {
          this.atLeastOneInternalRecord(e, r);
        }, this[`AT_LEAST_ONE_SEP${n}`] = function(r) {
          this.atLeastOneSepFirstInternalRecord(e, r);
        };
      }
      this.consume = function(e, n, r) {
        return this.consumeInternalRecord(n, e, r);
      }, this.subrule = function(e, n, r) {
        return this.subruleInternalRecord(n, e, r);
      }, this.option = function(e, n) {
        return this.optionInternalRecord(n, e);
      }, this.or = function(e, n) {
        return this.orInternalRecord(n, e);
      }, this.many = function(e, n) {
        this.manyInternalRecord(e, n);
      }, this.atLeastOne = function(e, n) {
        this.atLeastOneInternalRecord(e, n);
      }, this.ACTION = this.ACTION_RECORD, this.BACKTRACK = this.BACKTRACK_RECORD, this.LA = this.LA_RECORD;
    });
  }
  disableRecording() {
    this.RECORDING_PHASE = !1, this.TRACE_INIT("Deleting Recording methods", () => {
      const e = this;
      for (let n = 0; n < 10; n++) {
        const r = n > 0 ? n : "";
        delete e[`CONSUME${r}`], delete e[`SUBRULE${r}`], delete e[`OPTION${r}`], delete e[`OR${r}`], delete e[`MANY${r}`], delete e[`MANY_SEP${r}`], delete e[`AT_LEAST_ONE${r}`], delete e[`AT_LEAST_ONE_SEP${r}`];
      }
      delete e.consume, delete e.subrule, delete e.option, delete e.or, delete e.many, delete e.atLeastOne, delete e.ACTION, delete e.BACKTRACK, delete e.LA;
    });
  }
  //   Parser methods are called inside an ACTION?
  //   Maybe try/catch/finally on ACTIONS while disabling the recorders state changes?
  // @ts-expect-error -- noop place holder
  ACTION_RECORD(e) {
  }
  // Executing backtracking logic will break our recording logic assumptions
  BACKTRACK_RECORD(e, n) {
    return () => !0;
  }
  // LA is part of the official API and may be used for custom lookahead logic
  // by end users who may forget to wrap it in ACTION or inside a GATE
  LA_RECORD(e) {
    return rn;
  }
  topLevelRuleRecord(e, n) {
    try {
      const r = new ht({ definition: [], name: e });
      return r.name = e, this.recordingProdStack.push(r), n.call(this), this.recordingProdStack.pop(), r;
    } catch (r) {
      if (r.KNOWN_RECORDER_ERROR !== !0)
        try {
          r.message = r.message + `
	 This error was thrown during the "grammar recording phase" For more info see:
	https://chevrotain.io/docs/guide/internals.html#grammar-recording`;
        } catch {
          throw r;
        }
      throw r;
    }
  }
  // Implementation of parsing DSL
  optionInternalRecord(e, n) {
    return gt.call(this, J, e, n);
  }
  atLeastOneInternalRecord(e, n) {
    gt.call(this, Le, n, e);
  }
  atLeastOneSepFirstInternalRecord(e, n) {
    gt.call(this, ke, n, e, bi);
  }
  manyInternalRecord(e, n) {
    gt.call(this, W, n, e);
  }
  manySepFirstInternalRecord(e, n) {
    gt.call(this, ye, n, e, bi);
  }
  orInternalRecord(e, n) {
    return sp.call(this, e, n);
  }
  subruleInternalRecord(e, n, r) {
    if (nn(n), !e || O(e, "ruleName") === !1) {
      const a = new Error(`<SUBRULE${Mi(n)}> argument is invalid expecting a Parser method reference but got: <${JSON.stringify(e)}>
 inside top level rule: <${this.recordingProdStack[0].name}>`);
      throw a.KNOWN_RECORDER_ERROR = !0, a;
    }
    const i = ut(this.recordingProdStack), s = e.ruleName, o = new ie({
      idx: n,
      nonTerminalName: s,
      label: r?.LABEL,
      // The resolving of the `referencedRule` property will be done once all the Rule's GASTs have been created
      referencedRule: void 0
    });
    return i.definition.push(o), this.outputCst ? rp : vn;
  }
  consumeInternalRecord(e, n, r) {
    if (nn(n), !Rs(e)) {
      const o = new Error(`<CONSUME${Mi(n)}> argument is invalid expecting a TokenType reference but got: <${JSON.stringify(e)}>
 inside top level rule: <${this.recordingProdStack[0].name}>`);
      throw o.KNOWN_RECORDER_ERROR = !0, o;
    }
    const i = ut(this.recordingProdStack), s = new j({
      idx: n,
      terminalType: e,
      label: r?.LABEL
    });
    return i.definition.push(s), Ks;
  }
}
function gt(t, e, n, r = !1) {
  nn(n);
  const i = ut(this.recordingProdStack), s = De(e) ? e : e.DEF, o = new t({ definition: [], idx: n });
  return r && (o.separator = e.SEP), O(e, "MAX_LOOKAHEAD") && (o.maxLookahead = e.MAX_LOOKAHEAD), this.recordingProdStack.push(o), s.call(this), i.definition.push(o), this.recordingProdStack.pop(), vn;
}
function sp(t, e) {
  nn(e);
  const n = ut(this.recordingProdStack), r = x(t) === !1, i = r === !1 ? t : t.DEF, s = new Ae({
    definition: [],
    idx: e,
    ignoreAmbiguities: r && t.IGNORE_AMBIGUITIES === !0
  });
  O(t, "MAX_LOOKAHEAD") && (s.maxLookahead = t.MAX_LOOKAHEAD);
  const o = hs(i, (a) => De(a.GATE));
  return s.hasPredicates = o, n.definition.push(s), S(i, (a) => {
    const c = new te({ definition: [] });
    s.definition.push(c), O(a, "IGNORE_AMBIGUITIES") ? c.ignoreAmbiguities = a.IGNORE_AMBIGUITIES : O(a, "GATE") && (c.ignoreAmbiguities = !0), this.recordingProdStack.push(c), a.ALT.call(this), this.recordingProdStack.pop();
  }), vn;
}
function Mi(t) {
  return t === 0 ? "" : `${t}`;
}
function nn(t) {
  if (t < 0 || t > xi) {
    const e = new Error(
      // The stack trace will contain all the needed details
      `Invalid DSL Method idx value: <${t}>
	Idx value must be a none negative value smaller than ${xi + 1}`
    );
    throw e.KNOWN_RECORDER_ERROR = !0, e;
  }
}
class op {
  initPerformanceTracer(e) {
    if (O(e, "traceInitPerf")) {
      const n = e.traceInitPerf, r = typeof n == "number";
      this.traceInitMaxIdent = r ? n : 1 / 0, this.traceInitPerf = r ? n > 0 : n;
    } else
      this.traceInitMaxIdent = 0, this.traceInitPerf = Ue.traceInitPerf;
    this.traceInitIndent = -1;
  }
  TRACE_INIT(e, n) {
    if (this.traceInitPerf === !0) {
      this.traceInitIndent++;
      const r = new Array(this.traceInitIndent + 1).join("	");
      this.traceInitIndent < this.traceInitMaxIdent && console.log(`${r}--> <${e}>`);
      const { time: i, value: s } = ds(n), o = i > 10 ? console.warn : console.log;
      return this.traceInitIndent < this.traceInitMaxIdent && o(`${r}<-- <${e}> time: ${i}ms`), this.traceInitIndent--, s;
    } else
      return n();
  }
}
function ap(t, e) {
  e.forEach((n) => {
    const r = n.prototype;
    Object.getOwnPropertyNames(r).forEach((i) => {
      if (i === "constructor")
        return;
      const s = Object.getOwnPropertyDescriptor(r, i);
      s && (s.get || s.set) ? Object.defineProperty(t.prototype, i, s) : t.prototype[i] = n.prototype[i];
    });
  });
}
const rn = Rr(Ze, "", NaN, NaN, NaN, NaN, NaN, NaN);
Object.freeze(rn);
const Ue = Object.freeze({
  recoveryEnabled: !1,
  maxLookahead: 3,
  dynamicTokensEnabled: !1,
  outputCst: !0,
  errorMessageProvider: Ss,
  nodeLocationTracking: "none",
  traceInitPerf: !1,
  skipValidations: !1
}), sn = Object.freeze({
  recoveryValueFunc: () => {
  },
  resyncEnabled: !0
});
var q;
(function(t) {
  t[t.INVALID_RULE_NAME = 0] = "INVALID_RULE_NAME", t[t.DUPLICATE_RULE_NAME = 1] = "DUPLICATE_RULE_NAME", t[t.INVALID_RULE_OVERRIDE = 2] = "INVALID_RULE_OVERRIDE", t[t.DUPLICATE_PRODUCTIONS = 3] = "DUPLICATE_PRODUCTIONS", t[t.UNRESOLVED_SUBRULE_REF = 4] = "UNRESOLVED_SUBRULE_REF", t[t.LEFT_RECURSION = 5] = "LEFT_RECURSION", t[t.NONE_LAST_EMPTY_ALT = 6] = "NONE_LAST_EMPTY_ALT", t[t.AMBIGUOUS_ALTS = 7] = "AMBIGUOUS_ALTS", t[t.CONFLICT_TOKENS_RULES_NAMESPACE = 8] = "CONFLICT_TOKENS_RULES_NAMESPACE", t[t.INVALID_TOKEN_NAME = 9] = "INVALID_TOKEN_NAME", t[t.NO_NON_EMPTY_LOOKAHEAD = 10] = "NO_NON_EMPTY_LOOKAHEAD", t[t.AMBIGUOUS_PREFIX_ALTS = 11] = "AMBIGUOUS_PREFIX_ALTS", t[t.TOO_MANY_ALTS = 12] = "TOO_MANY_ALTS", t[t.CUSTOM_LOOKAHEAD_VALIDATION = 13] = "CUSTOM_LOOKAHEAD_VALIDATION";
})(q || (q = {}));
class Mt {
  /**
   *  @deprecated use the **instance** method with the same name instead
   */
  static performSelfAnalysis(e) {
    throw Error("The **static** `performSelfAnalysis` method has been deprecated.	\nUse the **instance** method with the same name instead.");
  }
  performSelfAnalysis() {
    this.TRACE_INIT("performSelfAnalysis", () => {
      let e;
      this.selfAnalysisDone = !0;
      const n = this.className;
      this.TRACE_INIT("toFastProps", () => {
        ps(this);
      }), this.TRACE_INIT("Grammar Recording", () => {
        try {
          this.enableRecording(), S(this.definedRulesNames, (i) => {
            const s = this[i].originalGrammarAction;
            let o;
            this.TRACE_INIT(`${i} Rule`, () => {
              o = this.topLevelRuleRecord(i, s);
            }), this.gastProductionsCache[i] = o;
          });
        } finally {
          this.disableRecording();
        }
      });
      let r = [];
      if (this.TRACE_INIT("Grammar Resolving", () => {
        r = Cd({
          rules: H(this.gastProductionsCache)
        }), this.definitionErrors = this.definitionErrors.concat(r);
      }), this.TRACE_INIT("Grammar Validations", () => {
        if (D(r) && this.skipValidations === !1) {
          const i = bd({
            rules: H(this.gastProductionsCache),
            tokenTypes: H(this.tokensMap),
            errMsgProvider: Xe,
            grammarName: n
          }), s = md({
            lookaheadStrategy: this.lookaheadStrategy,
            rules: H(this.gastProductionsCache),
            tokenTypes: H(this.tokensMap),
            grammarName: n
          });
          this.definitionErrors = this.definitionErrors.concat(i, s);
        }
      }), D(this.definitionErrors) && (this.recoveryEnabled && this.TRACE_INIT("computeAllProdsFollows", () => {
        const i = cf(H(this.gastProductionsCache));
        this.resyncFollows = i;
      }), this.TRACE_INIT("ComputeLookaheadFunctions", () => {
        var i, s;
        (s = (i = this.lookaheadStrategy).initialize) === null || s === void 0 || s.call(i, {
          rules: H(this.gastProductionsCache)
        }), this.preComputeLookaheadFunctions(H(this.gastProductionsCache));
      })), !Mt.DEFER_DEFINITION_ERRORS_HANDLING && !D(this.definitionErrors))
        throw e = v(this.definitionErrors, (i) => i.message), new Error(`Parser Definition Errors detected:
 ${e.join(`
-------------------------------
`)}`);
    });
  }
  constructor(e, n) {
    this.definitionErrors = [], this.selfAnalysisDone = !1;
    const r = this;
    if (r.initErrorHandler(n), r.initLexerAdapter(), r.initLooksAhead(n), r.initRecognizerEngine(e, n), r.initRecoverable(n), r.initTreeBuilder(n), r.initContentAssist(), r.initGastRecorder(n), r.initPerformanceTracer(n), O(n, "ignoredIssues"))
      throw new Error(`The <ignoredIssues> IParserConfig property has been deprecated.
	Please use the <IGNORE_AMBIGUITIES> flag on the relevant DSL method instead.
	See: https://chevrotain.io/docs/guide/resolving_grammar_errors.html#IGNORING_AMBIGUITIES
	For further details.`);
    this.skipValidations = O(n, "skipValidations") ? n.skipValidations : Ue.skipValidations;
  }
}
Mt.DEFER_DEFINITION_ERRORS_HANDLING = !1;
ap(Mt, [
  Ud,
  jd,
  Qd,
  Zd,
  ep,
  Jd,
  tp,
  np,
  ip,
  op
]);
class cp extends Mt {
  constructor(e, n = Ue) {
    const r = Y(n);
    r.outputCst = !0, super(e, r);
  }
}
const Hs = M({
  name: "HTML_COMMENT",
  pattern: /<!--[\s\S]*?-->/,
  line_breaks: !0
}), Ws = M({
  name: "HTML_CONDITIONAL_COMMENT",
  pattern: /<!\[[\s\S]*?\]>/,
  line_breaks: !0
}), up = M({
  name: "XML",
  pattern: /<\?xml(?:.|\s)*?\?>/
}), zs = M({
  name: "CDATA",
  pattern: /<!\[CDATA\[[\s\S]*?]]>/
}), Ys = M({
  name: "DTD",
  pattern: /<!.*?>/
}), Xs = M({
  name: "SCRIPTLET",
  pattern: /<%(.*?)%>|<\?(.*?)\?>/
}), lp = M({
  name: "SEA_WS",
  pattern: /[ \t]+/,
  group: X.SKIPPED
}), qs = M({
  name: "LINE_BREAK",
  pattern: /\r?\n/
}), Qs = M({
  name: "SCRIPT_OPEN",
  pattern: /<script\b[^>]*>/,
  push_mode: "SCRIPT"
}), Zs = M({
  name: "STYLE_OPEN",
  pattern: /<style\b[^>]*>/,
  push_mode: "STYLE"
}), qn = M({
  name: "TAG_OPEN",
  pattern: /</,
  push_mode: "TAG"
}), Js = M({
  name: "HTML_TEXT",
  pattern: /[^<@]+/,
  line_breaks: !0
}), Qn = M({
  name: "TAG_CLOSE",
  pattern: />/,
  pop_mode: !0
}), eo = M({
  name: "TAG_SLASH_CLOSE",
  pattern: /\/>/,
  pop_mode: !0
}), to = M({
  name: "TAG_SLASH",
  pattern: /\//
}), no = M({
  name: "TAG_EQUALS",
  pattern: /=/,
  push_mode: "ATTVALUE"
}), zt = M({
  name: "TAG_NAME",
  pattern: /[:@a-zA-Z\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:\.\-@\u00B7\u0300-\u036F\u203F-\u2040.0-9a-zA-Z]*/
}), hp = M({
  name: "TAG_WHITESPACE",
  pattern: /[ \t\r\n]+/,
  group: X.SKIPPED
}), ro = M({
  name: "SCRIPT_BODY",
  pattern: /[\s\S]*?<\/script>/,
  pop_mode: !0
}), io = M({
  name: "STYLE_BODY",
  pattern: /[\s\S]*?<\/style>/,
  pop_mode: !0
}), so = M({
  name: "ATTVALUE_VALUE",
  pattern: / *("[^<"]*"|'[^<']*'|[-_./+,?=:#;0-9a-zA-Z]+ ?|#[0-9a-fA-F]+|[0-9]+%?)/,
  pop_mode: !0
}), Zn = M({
  name: "EDGE_COMMENT",
  pattern: /{{--[\s\S]*?--}}/,
  line_breaks: !0
}), Jn = M({
  name: "EDGE_MUSTACHE",
  pattern: /{{[\s\S]*?}}\s*/
}), er = M({
  name: "EDGE_SAFE_MUSTACHE",
  pattern: /{{{[\s\S]*?}}}\s*/
}), oo = M({
  name: "EDGE_ESCAPED_MUSTACHE",
  pattern: /@{{[\s\S]*?}}\s*/
}), ao = M({
  name: "EDGE_TAG",
  pattern: /@(?:!?\w+(?:\.\w+)*)\s*(?:\((?:[^)(]+|\((?:[^)(]+|\([^)(]*\))*\))*\))?/
}), co = M({
  name: "EDGE_TAG_PROP",
  pattern: /@(if|elseif|else|each|flashMessage)\([^)]*\)\s*([\s\S]*?)@end/
}), tr = {
  defaultMode: "default",
  modes: {
    default: [
      Hs,
      Ws,
      up,
      zs,
      Ys,
      Xs,
      qs,
      lp,
      Qs,
      Zs,
      qn,
      Zn,
      oo,
      er,
      Jn,
      ao,
      Js
    ],
    TAG: [
      Qn,
      eo,
      to,
      no,
      Zn,
      er,
      Jn,
      co,
      zt,
      hp
    ],
    SCRIPT: [ro],
    STYLE: [io],
    ATTVALUE: [so]
  }
}, fp = () => {
  const t = [];
  for (const e in tr.modes)
    t.push(...tr.modes[e]);
  return t;
}, dp = new X(tr);
class uo extends cp {
  constructor() {
    super(fp());
    const e = this;
    e.RULE("document", () => {
      e.MANY(() => {
        e.SUBRULE(e.content);
      });
    }), e.RULE("content", () => {
      e.OR([
        { ALT: () => e.CONSUME(Js) },
        { ALT: () => e.CONSUME(qs) },
        { ALT: () => e.SUBRULE(e.element) },
        { ALT: () => e.SUBRULE(e.scriptlet) },
        { ALT: () => e.SUBRULE(e.htmlComment) },
        { ALT: () => e.SUBRULE(e.htmlConditionalComment) },
        { ALT: () => e.SUBRULE(e.cdata) },
        { ALT: () => e.SUBRULE(e.dtd) },
        { ALT: () => e.SUBRULE(e.scriptElement) },
        { ALT: () => e.SUBRULE(e.styleElement) },
        { ALT: () => e.SUBRULE(e.edgeComment) },
        { ALT: () => e.SUBRULE(e.edgeSafeMustache) },
        { ALT: () => e.SUBRULE(e.edgeMustache) },
        { ALT: () => e.SUBRULE(e.edgeEscapedMustache) },
        { ALT: () => e.SUBRULE(e.edgeTag) }
      ]);
    }), e.RULE("element", () => {
      e.OR([
        {
          ALT: () => e.SUBRULE(e.openingTag)
        },
        {
          ALT: () => e.SUBRULE(e.closingTag)
        }
      ]);
    }), e.RULE("openingTag", () => {
      e.CONSUME(qn), e.CONSUME(zt), e.MANY(() => {
        e.OR([
          { ALT: () => e.SUBRULE(e.edgeTagProp) },
          { ALT: () => e.SUBRULE(e.attribute) },
          { ALT: () => e.SUBRULE(e.edgeSafeMustache) },
          { ALT: () => e.SUBRULE(e.edgeMustache) },
          { ALT: () => e.SUBRULE(e.edgeComment) }
        ]);
      }), e.OR1([
        { ALT: () => e.CONSUME1(eo) },
        { ALT: () => e.CONSUME1(Qn) }
      ]);
    }), e.RULE("attribute", () => {
      e.CONSUME(zt), e.OPTION(() => {
        e.CONSUME(no), e.CONSUME(so);
      });
    }), e.RULE("closingTag", () => {
      e.CONSUME(qn), e.CONSUME(to), e.CONSUME(zt), e.CONSUME(Qn);
    }), e.RULE("scriptlet", () => {
      e.CONSUME(Xs);
    }), e.RULE("htmlComment", () => {
      e.CONSUME(Hs);
    }), e.RULE("htmlConditionalComment", () => {
      e.CONSUME(Ws);
    }), e.RULE("cdata", () => {
      e.CONSUME(zs);
    }), e.RULE("dtd", () => {
      e.CONSUME(Ys);
    }), e.RULE("scriptElement", () => {
      e.CONSUME(Qs), e.CONSUME(ro);
    }), e.RULE("styleElement", () => {
      e.CONSUME(Zs), e.CONSUME(io);
    }), e.RULE("edgeComment", () => {
      e.CONSUME(Zn);
    }), e.RULE("edgeMustache", () => {
      e.CONSUME(Jn);
    }), e.RULE("edgeSafeMustache", () => {
      e.CONSUME(er);
    }), e.RULE("edgeEscapedMustache", () => {
      e.CONSUME(oo);
    }), e.RULE("edgeTag", () => {
      e.CONSUME(ao);
    }), e.RULE("edgeTagProp", () => {
      e.CONSUME(co);
    }), this.performSelfAnalysis();
  }
}
const pp = new uo(), mp = pp.getBaseCstVisitorConstructor();
class gp extends mp {
  #t = /* @__PURE__ */ new Set([
    "area",
    "base",
    "br",
    "col",
    "embed",
    "hr",
    "img",
    "input",
    "link",
    "meta",
    "param",
    "source",
    "track",
    "wbr",
    // Below are for SVGs
    "animateMotion",
    "animateTransform",
    "animate",
    "circle",
    "ellipse",
    "feGaussianBlur",
    "feDropShadow",
    "feOffset",
    "feBlend",
    "feColorMatrix",
    "feComposite",
    "feDisplacementMap",
    "feFlood",
    "feImage",
    "feMergeNode",
    "feMorphology",
    "fePointLight",
    "feSpotLight",
    "feTile",
    "feTurbulence",
    "image",
    "line",
    "mpath",
    "path",
    "polygon",
    "polyline",
    "rect",
    "set",
    "stop",
    "use",
    "view"
  ]);
  constructor() {
    super(), this.validateVisitor();
  }
  #e(e) {
    return this.#t.has(e);
  }
  document(e) {
    const n = e.content[0].startOffset, r = e.content[e.content.length - 1].endOffset;
    return {
      type: "document",
      children: e.content.map((i) => this.visit(i)),
      start: n,
      end: r
    };
  }
  content(e) {
    if (e.HTML_TEXT)
      return {
        type: "htmlText",
        value: e.HTML_TEXT[0].image,
        start: e.HTML_TEXT[0].startOffset,
        end: e.HTML_TEXT[0].endOffset
      };
    if (e.LINE_BREAK)
      return {
        type: "linebreak",
        value: e.LINE_BREAK[0].image,
        start: e.LINE_BREAK[0].startOffset,
        end: e.LINE_BREAK[0].endOffset
      };
    const n = e.element || e.scriptlet || e.htmlComment || e.htmlConditionalComment || e.cdata || e.dtd || e.scriptElement || e.styleElement || e.edgeComment || e.edgeMustache || e.edgeSafeMustache || e.edgeEscapedMustache || e.edgeTag || e.htmlText;
    return this.visit(n);
  }
  element(e) {
    return e.openingTag ? this.visit(e.openingTag) : e.selfClosingTag ? this.visit(e.selfClosingTag) : this.visit(e.closingTag);
  }
  openingTag(e) {
    const n = e.TAG_NAME[0].image, r = e.edgeSafeMustache ? e.edgeSafeMustache.map((l) => this.visit(l)) : [], i = e.edgeMustache ? e.edgeMustache.map((l) => this.visit(l)) : [], s = e.edgeTagProp ? e.edgeTagProp.map((l) => this.visit(l)) : [], o = e.attribute ? e.attribute.map((l) => this.visit(l)) : [], a = e.edgeComment ? e.edgeComment.map((l) => this.visit(l)) : [], c = e.TAG_NAME[0].startOffset, u = e.TAG_NAME[0].endOffset;
    return this.#e(n) || e.TAG_SLASH_CLOSE ? {
      type: "voidTag",
      tagName: n,
      edgeSafeMustaches: r,
      edgeMustaches: i,
      edgeTagProps: s,
      attributes: o,
      comments: a,
      start: c,
      end: u
    } : {
      type: "openingTag",
      tagName: n,
      edgeSafeMustaches: r,
      edgeMustaches: i,
      edgeTagProps: s,
      attributes: o,
      comments: a,
      start: c,
      end: u
    };
  }
  attribute(e) {
    const n = e.TAG_NAME[0].image, r = e.ATTVALUE_VALUE?.[0]?.image?.trim(), i = e.TAG_NAME[0].startOffset, s = e.ATTVALUE_VALUE ? e.ATTVALUE_VALUE[0].endOffset : e.TAG_NAME[0].endOffset;
    return { type: "attribute", attributeName: n, attributeValue: r, start: i, end: s };
  }
  closingTag(e) {
    const n = e.TAG_NAME[0].image, r = e.TAG_OPEN[0].startOffset, i = e.TAG_CLOSE[0].endOffset;
    return this.#e(n) ? {
      type: "doNotPrint"
    } : { type: "closingTag", tagName: n, start: r, end: i };
  }
  scriptlet(e) {
    const n = e.SCRIPTLET[0].image, r = e.SCRIPTLET[0].startOffset, i = e.SCRIPTLET[0].endOffset;
    return { type: "scriptlet", value: n, start: r, end: i };
  }
  htmlComment(e) {
    const n = e.HTML_COMMENT[0].image, r = e.HTML_COMMENT[0].startOffset, i = e.HTML_COMMENT[0].endOffset;
    return { type: "htmlComment", value: n, start: r, end: i };
  }
  htmlConditionalComment(e) {
    const n = e.HTML_CONDITIONAL_COMMENT[0].image, r = e.HTML_CONDITIONAL_COMMENT[0].startOffset, i = e.HTML_CONDITIONAL_COMMENT[0].endOffset;
    return { type: "htmlConditionalComment", value: n, start: r, end: i };
  }
  cdata(e) {
    const n = e.CDATA[0].image, r = e.CDATA[0].startOffset, i = e.CDATA[0].endOffset;
    return { type: "cdata", value: n, start: r, end: i };
  }
  dtd(e) {
    const n = e.DTD[0].image, r = e.DTD[0].startOffset, i = e.DTD[0].endOffset;
    return { type: "dtd", value: n, start: r, end: i };
  }
  scriptElement(e) {
    const n = e.SCRIPT_OPEN[0].image + e.SCRIPT_BODY[0].image, r = e.SCRIPT_OPEN[0].startOffset, i = e.SCRIPT_BODY[0].endOffset;
    return { type: "scriptElement", value: n, start: r, end: i };
  }
  styleElement(e) {
    const n = e.STYLE_OPEN[0].image + e.STYLE_BODY[0].image, r = e.STYLE_OPEN[0].startOffset, i = e.STYLE_BODY[0].endOffset;
    return { type: "styleElement", value: n, start: r, end: i };
  }
  edgeComment(e) {
    const n = e.EDGE_COMMENT[0].image, r = e.EDGE_COMMENT[0].startOffset, i = e.EDGE_COMMENT[0].endOffset;
    return { type: "edgeComment", value: n, start: r, end: i };
  }
  edgeMustache(e) {
    const n = e.EDGE_MUSTACHE[0].image, r = e.EDGE_MUSTACHE[0].startOffset, i = e.EDGE_MUSTACHE[0].endOffset;
    return { type: "edgeMustache", value: n, start: r, end: i };
  }
  edgeSafeMustache(e) {
    const n = e.EDGE_SAFE_MUSTACHE[0].image, r = e.EDGE_SAFE_MUSTACHE[0].startOffset, i = e.EDGE_SAFE_MUSTACHE[0].endOffset;
    return { type: "edgeSafeMustache", value: n, start: r, end: i };
  }
  edgeEscapedMustache(e) {
    const n = e.EDGE_ESCAPED_MUSTACHE[0].image, r = e.EDGE_ESCAPED_MUSTACHE[0].startOffset, i = e.EDGE_ESCAPED_MUSTACHE[0].endOffset;
    return { type: "edgeEscapedMustache", value: n, start: r, end: i };
  }
  edgeTag(e) {
    const n = e.EDGE_TAG[0].image, r = e.EDGE_TAG[0].startOffset, i = e.EDGE_TAG[0].endOffset;
    return { type: "edgeTag", value: n, start: r, end: i };
  }
  edgeTagProp(e) {
    const n = e.EDGE_TAG_PROP[0].image, r = e.EDGE_TAG_PROP[0].startOffset, i = e.EDGE_TAG_PROP[0].endOffset;
    return { type: "edgeTagProp", value: n, start: r, end: i };
  }
}
const jt = new uo(), Ep = new gp();
function Nr(t) {
  const e = dp.tokenize(t);
  jt.input = e.tokens;
  const n = jt.document();
  if (jt.errors.length > 0)
    throw new Error(
      `Parsing Errors Detected: ${JSON.stringify(jt.errors)}`
    );
  return Ep.visit(n);
}
function lo(t) {
  Object.defineProperty(t, "__esModule", { value: !0, configurable: !0 });
}
function Lr(t, e, n, r) {
  Object.defineProperty(t, e, { get: n, set: r, enumerable: !0, configurable: !0 });
}
var ho = {};
lo(ho);
Lr(ho, "default", () => fo);
class fo extends Error {
  constructor(e, n, r, i, s) {
    super(e + ":" + r + ":" + i + ": " + n), this.reason = n, this.filename = e, this.line = r, this.column = i, this.source = s;
  }
}
var po = {};
lo(po);
Lr(po, "default", () => mo);
class mo {
  constructor(e, n, r) {
    this.start = e, this.end = n, this.source = r;
  }
}
var Tp = {};
Lr(Tp, "CssTypes", () => b);
var b = /* @__PURE__ */ function(t) {
  return t.stylesheet = "stylesheet", t.rule = "rule", t.declaration = "declaration", t.comment = "comment", t.container = "container", t.charset = "charset", t.document = "document", t.customMedia = "custom-media", t.fontFace = "font-face", t.host = "host", t.import = "import", t.keyframes = "keyframes", t.keyframe = "keyframe", t.layer = "layer", t.media = "media", t.namespace = "namespace", t.page = "page", t.startingStyle = "starting-style", t.supports = "supports", t;
}({});
const go = 1e4, Pi = (t, e, n) => {
  let r = n, i = go;
  do {
    const s = e.map((c) => t.indexOf(c, r));
    s.push(t.indexOf("\\", r));
    const o = s.filter((c) => c !== -1);
    if (o.length === 0) return -1;
    const a = Math.min(...o);
    if (t[a] === "\\")
      r = a + 2, i--;
    else return a;
  } while (i > 0);
  throw new Error("Too many escaping");
}, kr = (t, e, n) => {
  let r = n, i = go;
  do {
    const s = e.map((u) => t.indexOf(u, r));
    s.push(t.indexOf("(", r)), s.push(t.indexOf('"', r)), s.push(t.indexOf("'", r)), s.push(t.indexOf("\\", r));
    const o = s.filter((u) => u !== -1);
    if (o.length === 0) return -1;
    const a = Math.min(...o);
    switch (t[a]) {
      case "\\":
        r = a + 2;
        break;
      case "(":
        {
          const u = kr(t, [
            ")"
          ], a + 1);
          if (u === -1) return -1;
          r = u + 1;
        }
        break;
      case '"':
        {
          const u = Pi(t, [
            '"'
          ], a + 1);
          if (u === -1) return -1;
          r = u + 1;
        }
        break;
      case "'":
        {
          const u = Pi(t, [
            "'"
          ], a + 1);
          if (u === -1) return -1;
          r = u + 1;
        }
        break;
      default:
        return a;
    }
    i--;
  } while (i > 0);
  throw new Error("Too many escaping");
}, yp = (t, e) => {
  const n = [];
  let r = 0;
  for (; r < t.length; ) {
    const i = kr(t, e, r);
    if (i === -1)
      return n.push(t.substring(r)), n;
    n.push(t.substring(r, i)), r = i + 1;
  }
  return n;
}, Mn = /\/\*[^]*?(?:\*\/|$)/g, Ap = (t, e) => {
  e = e || {};
  let n = 1, r = 1;
  function i(g) {
    const E = g.match(/\n/g);
    E && (n += E.length);
    const k = g.lastIndexOf(`
`);
    r = ~k ? g.length - k : r + g.length;
  }
  function s() {
    const g = {
      line: n,
      column: r
    };
    return function(E) {
      return E.position = new mo(g, {
        line: n,
        column: r
      }, e?.source || ""), A(), E;
    };
  }
  const o = [];
  function a(g) {
    const E = new fo(e?.source || "", g, n, r, t);
    if (e?.silent) o.push(E);
    else throw E;
  }
  function c() {
    const g = h();
    return {
      type: b.stylesheet,
      stylesheet: {
        source: e?.source,
        rules: g,
        parsingErrors: o
      }
    };
  }
  function u() {
    const g = /^{\s*/.exec(t);
    return g ? (f(g), !0) : !1;
  }
  function l() {
    const g = /^}/.exec(t);
    return g ? (f(g), !0) : !1;
  }
  function h() {
    let g;
    const E = [];
    for (A(), d(E); t.length && t.charAt(0) !== "}" && (g = ze() || Pt()); ) g && (E.push(g), d(E));
    return E;
  }
  function f(g) {
    const E = g[0];
    return i(E), t = t.slice(E.length), g;
  }
  function A() {
    const g = /^\s*/.exec(t);
    g && f(g);
  }
  function d(g) {
    let E;
    for (g = g || []; E = _(); ) E && g.push(E);
    return g;
  }
  function _() {
    const g = s();
    if (t.charAt(0) !== "/" || t.charAt(1) !== "*") return;
    const E = /^\/\*[^]*?\*\//.exec(t);
    return E ? (f(E), g({
      type: b.comment,
      comment: E[0].slice(2, -2)
    })) : a("End of comment missing");
  }
  function R() {
    const g = /^([^{]+)/.exec(t);
    if (!g) return;
    f(g);
    const E = oe(g[0]).replace(Mn, "");
    return yp(E, [
      ","
    ]).map((k) => oe(k));
  }
  function m() {
    const g = s(), E = /^(\*?[-#/*\\\w]+(\[[0-9a-z_-]+\])?)\s*/.exec(t);
    if (!E) return;
    f(E);
    const k = oe(E[0]), C = /^:\s*/.exec(t);
    if (!C) return a("property missing ':'");
    f(C);
    let F = "";
    const w = kr(t, [
      ";",
      "}"
    ]);
    w !== -1 && (F = t.substring(0, w), f([
      F
    ]), F = oe(F).replace(Mn, ""));
    const V = g({
      type: b.declaration,
      property: k.replace(Mn, ""),
      value: F
    }), be = /^[;\s]*/.exec(t);
    return be && f(be), V;
  }
  function T() {
    const g = [];
    if (!u()) return a("missing '{'");
    d(g);
    let E;
    for (; E = m(); ) E && (g.push(E), d(g));
    return l() ? g : a("missing '}'");
  }
  function p() {
    let g;
    const E = [], k = s();
    for (; g = /^((\d+\.\d+|\.\d+|\d+)%?|[a-z]+)\s*/.exec(t); ) {
      const C = f(g);
      E.push(C[1]);
      const F = /^,\s*/.exec(t);
      F && f(F);
    }
    if (E.length)
      return k({
        type: b.keyframe,
        values: E,
        declarations: T() || []
      });
  }
  function y() {
    const g = s(), E = /^@([-\w]+)?keyframes\s*/.exec(t);
    if (!E) return;
    const k = f(E)[1], C = /^([-\w]+)\s*/.exec(t);
    if (!C) return a("@keyframes missing name");
    const F = f(C)[1];
    if (!u()) return a("@keyframes missing '{'");
    let w, V = d();
    for (; w = p(); )
      V.push(w), V = V.concat(d());
    return l() ? g({
      type: b.keyframes,
      name: F,
      vendor: k,
      keyframes: V
    }) : a("@keyframes missing '}'");
  }
  function N() {
    const g = s(), E = /^@supports *([^{]+)/.exec(t);
    if (!E) return;
    const k = oe(f(E)[1]);
    if (!u()) return a("@supports missing '{'");
    const C = d().concat(h());
    return l() ? g({
      type: b.supports,
      supports: k,
      rules: C
    }) : a("@supports missing '}'");
  }
  function L() {
    const g = s(), E = /^@host\s*/.exec(t);
    if (!E) return;
    if (f(E), !u()) return a("@host missing '{'");
    const k = d().concat(h());
    return l() ? g({
      type: b.host,
      rules: k
    }) : a("@host missing '}'");
  }
  function P() {
    const g = s(), E = /^@container *([^{]+)/.exec(t);
    if (!E) return;
    const k = oe(f(E)[1]);
    if (!u()) return a("@container missing '{'");
    const C = d().concat(h());
    return l() ? g({
      type: b.container,
      container: k,
      rules: C
    }) : a("@container missing '}'");
  }
  function se() {
    const g = s(), E = /^@layer *([^{;@]+)/.exec(t);
    if (!E) return;
    const k = oe(f(E)[1]);
    if (!u()) {
      const F = /^[;\s]*/.exec(t);
      return F && f(F), g({
        type: b.layer,
        layer: k
      });
    }
    const C = d().concat(h());
    return l() ? g({
      type: b.layer,
      layer: k,
      rules: C
    }) : a("@layer missing '}'");
  }
  function Ce() {
    const g = s(), E = /^@media *([^{]+)/.exec(t);
    if (!E) return;
    const k = oe(f(E)[1]);
    if (!u()) return a("@media missing '{'");
    const C = d().concat(h());
    return l() ? g({
      type: b.media,
      media: k,
      rules: C
    }) : a("@media missing '}'");
  }
  function de() {
    const g = s(), E = /^@custom-media\s+(--\S+)\s+([^{;\s][^{;]*);/.exec(t);
    if (!E) return;
    const k = f(E);
    return g({
      type: b.customMedia,
      name: oe(k[1]),
      media: oe(k[2])
    });
  }
  function He() {
    const g = s(), E = /^@page */.exec(t);
    if (!E) return;
    f(E);
    const k = R() || [];
    if (!u()) return a("@page missing '{'");
    let C = d(), F;
    for (; F = m(); )
      C.push(F), C = C.concat(d());
    return l() ? g({
      type: b.page,
      selectors: k,
      declarations: C
    }) : a("@page missing '}'");
  }
  function Rn() {
    const g = s(), E = /^@([-\w]+)?document *([^{]+)/.exec(t);
    if (!E) return;
    const k = f(E), C = oe(k[1]), F = oe(k[2]);
    if (!u()) return a("@document missing '{'");
    const w = d().concat(h());
    return l() ? g({
      type: b.document,
      document: F,
      vendor: C,
      rules: w
    }) : a("@document missing '}'");
  }
  function dt() {
    const g = s(), E = /^@font-face\s*/.exec(t);
    if (!E) return;
    if (f(E), !u()) return a("@font-face missing '{'");
    let k = d(), C;
    for (; C = m(); )
      k.push(C), k = k.concat(d());
    return l() ? g({
      type: b.fontFace,
      declarations: k
    }) : a("@font-face missing '}'");
  }
  function nt() {
    const g = s(), E = /^@starting-style\s*/.exec(t);
    if (!E) return;
    if (f(E), !u()) return a("@starting-style missing '{'");
    const k = d().concat(h());
    return l() ? g({
      type: b.startingStyle,
      rules: k
    }) : a("@starting-style missing '}'");
  }
  const _e = it("import"), We = it("charset"), rt = it("namespace");
  function it(g) {
    const E = new RegExp("^@" + g + `\\s*((?::?[^;'"]|"(?:\\\\"|[^"])*?"|'(?:\\\\'|[^'])*?')+)(?:;|$)`);
    return function() {
      const k = s(), C = E.exec(t);
      if (!C) return;
      const F = f(C), w = {
        type: g
      };
      return w[g] = F[1].trim(), k(w);
    };
  }
  function ze() {
    if (t[0] === "@")
      return y() || Ce() || de() || N() || _e() || We() || rt() || Rn() || He() || L() || dt() || P() || nt() || se();
  }
  function Pt() {
    const g = s(), E = R();
    return E ? (d(), g({
      type: b.rule,
      selectors: E,
      declarations: T() || []
    })) : a("selector missing");
  }
  return nr(c());
};
function oe(t) {
  return t ? t.trim() : "";
}
function nr(t, e) {
  const n = t && typeof t.type == "string", r = n ? t : e;
  for (const i in t) {
    const s = t[i];
    Array.isArray(s) ? s.forEach((o) => {
      nr(o, r);
    }) : s && typeof s == "object" && nr(s, r);
  }
  return n && Object.defineProperty(t, "parent", {
    configurable: !0,
    writable: !0,
    enumerable: !1,
    value: e || null
  }), t;
}
var _p = Ap;
class vp {
  constructor(e) {
    this.level = 0, this.indentation = "  ", this.compress = !1, typeof e?.indent == "string" && (this.indentation = e?.indent), e?.compress && (this.compress = !0);
  }
  // We disable no-unused-vars for _position. We keep position for potential reintroduction of source-map
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  emit(e, n) {
    return e;
  }
  /**
  * Increase, decrease or return current indentation.
  */
  indent(e) {
    return this.level = this.level || 1, e ? (this.level += e, "") : Array(this.level).join(this.indentation);
  }
  visit(e) {
    switch (e.type) {
      case b.stylesheet:
        return this.stylesheet(e);
      case b.rule:
        return this.rule(e);
      case b.declaration:
        return this.declaration(e);
      case b.comment:
        return this.comment(e);
      case b.container:
        return this.container(e);
      case b.charset:
        return this.charset(e);
      case b.document:
        return this.document(e);
      case b.customMedia:
        return this.customMedia(e);
      case b.fontFace:
        return this.fontFace(e);
      case b.host:
        return this.host(e);
      case b.import:
        return this.import(e);
      case b.keyframes:
        return this.keyframes(e);
      case b.keyframe:
        return this.keyframe(e);
      case b.layer:
        return this.layer(e);
      case b.media:
        return this.media(e);
      case b.namespace:
        return this.namespace(e);
      case b.page:
        return this.page(e);
      case b.startingStyle:
        return this.startingStyle(e);
      case b.supports:
        return this.supports(e);
    }
  }
  mapVisit(e, n) {
    let r = "";
    n = n || "";
    for (let i = 0, s = e.length; i < s; i++)
      r += this.visit(e[i]), n && i < s - 1 && (r += this.emit(n));
    return r;
  }
  compile(e) {
    return this.compress ? e.stylesheet.rules.map(this.visit, this).join("") : this.stylesheet(e);
  }
  /**
  * Visit stylesheet node.
  */
  stylesheet(e) {
    return this.mapVisit(e.stylesheet.rules, `

`);
  }
  /**
  * Visit comment node.
  */
  comment(e) {
    return this.compress ? this.emit("", e.position) : this.emit(this.indent() + "/*" + e.comment + "*/", e.position);
  }
  /**
  * Visit container node.
  */
  container(e) {
    return this.compress ? this.emit("@container " + e.container, e.position) + this.emit("{") + this.mapVisit(e.rules) + this.emit("}") : this.emit(this.indent() + "@container " + e.container, e.position) + this.emit(` {
` + this.indent(1)) + this.mapVisit(e.rules, `

`) + this.emit(`
` + this.indent(-1) + this.indent() + "}");
  }
  /**
  * Visit container node.
  */
  layer(e) {
    return this.compress ? this.emit("@layer " + e.layer, e.position) + (e.rules ? this.emit("{") + this.mapVisit(e.rules) + this.emit("}") : ";") : this.emit(this.indent() + "@layer " + e.layer, e.position) + (e.rules ? this.emit(` {
` + this.indent(1)) + this.mapVisit(e.rules, `

`) + this.emit(`
` + this.indent(-1) + this.indent() + "}") : ";");
  }
  /**
  * Visit import node.
  */
  import(e) {
    return this.emit("@import " + e.import + ";", e.position);
  }
  /**
  * Visit media node.
  */
  media(e) {
    return this.compress ? this.emit("@media " + e.media, e.position) + this.emit("{") + this.mapVisit(e.rules) + this.emit("}") : this.emit(this.indent() + "@media " + e.media, e.position) + this.emit(` {
` + this.indent(1)) + this.mapVisit(e.rules, `

`) + this.emit(`
` + this.indent(-1) + this.indent() + "}");
  }
  /**
  * Visit document node.
  */
  document(e) {
    const n = "@" + (e.vendor || "") + "document " + e.document;
    return this.compress ? this.emit(n, e.position) + this.emit("{") + this.mapVisit(e.rules) + this.emit("}") : this.emit(n, e.position) + this.emit(`  {
` + this.indent(1)) + this.mapVisit(e.rules, `

`) + this.emit(this.indent(-1) + `
}`);
  }
  /**
  * Visit charset node.
  */
  charset(e) {
    return this.emit("@charset " + e.charset + ";", e.position);
  }
  /**
  * Visit namespace node.
  */
  namespace(e) {
    return this.emit("@namespace " + e.namespace + ";", e.position);
  }
  /**
  * Visit container node.
  */
  startingStyle(e) {
    return this.compress ? this.emit("@starting-style", e.position) + this.emit("{") + this.mapVisit(e.rules) + this.emit("}") : this.emit(this.indent() + "@starting-style", e.position) + this.emit(` {
` + this.indent(1)) + this.mapVisit(e.rules, `

`) + this.emit(`
` + this.indent(-1) + this.indent() + "}");
  }
  /**
  * Visit supports node.
  */
  supports(e) {
    return this.compress ? this.emit("@supports " + e.supports, e.position) + this.emit("{") + this.mapVisit(e.rules) + this.emit("}") : this.emit(this.indent() + "@supports " + e.supports, e.position) + this.emit(` {
` + this.indent(1)) + this.mapVisit(e.rules, `

`) + this.emit(`
` + this.indent(-1) + this.indent() + "}");
  }
  /**
  * Visit keyframes node.
  */
  keyframes(e) {
    return this.compress ? this.emit("@" + (e.vendor || "") + "keyframes " + e.name, e.position) + this.emit("{") + this.mapVisit(e.keyframes) + this.emit("}") : this.emit("@" + (e.vendor || "") + "keyframes " + e.name, e.position) + this.emit(` {
` + this.indent(1)) + this.mapVisit(e.keyframes, `
`) + this.emit(this.indent(-1) + "}");
  }
  /**
  * Visit keyframe node.
  */
  keyframe(e) {
    const n = e.declarations;
    return this.compress ? this.emit(e.values.join(","), e.position) + this.emit("{") + this.mapVisit(n) + this.emit("}") : this.emit(this.indent()) + this.emit(e.values.join(", "), e.position) + this.emit(` {
` + this.indent(1)) + this.mapVisit(n, `
`) + this.emit(this.indent(-1) + `
` + this.indent() + `}
`);
  }
  /**
  * Visit page node.
  */
  page(e) {
    if (this.compress) {
      const r = e.selectors.length ? e.selectors.join(", ") : "";
      return this.emit("@page " + r, e.position) + this.emit("{") + this.mapVisit(e.declarations) + this.emit("}");
    }
    const n = e.selectors.length ? e.selectors.join(", ") + " " : "";
    return this.emit("@page " + n, e.position) + this.emit(`{
`) + this.emit(this.indent(1)) + this.mapVisit(e.declarations, `
`) + this.emit(this.indent(-1)) + this.emit(`
}`);
  }
  /**
  * Visit font-face node.
  */
  fontFace(e) {
    return this.compress ? this.emit("@font-face", e.position) + this.emit("{") + this.mapVisit(e.declarations) + this.emit("}") : this.emit("@font-face ", e.position) + this.emit(`{
`) + this.emit(this.indent(1)) + this.mapVisit(e.declarations, `
`) + this.emit(this.indent(-1)) + this.emit(`
}`);
  }
  /**
  * Visit host node.
  */
  host(e) {
    return this.compress ? this.emit("@host", e.position) + this.emit("{") + this.mapVisit(e.rules) + this.emit("}") : this.emit("@host", e.position) + this.emit(` {
` + this.indent(1)) + this.mapVisit(e.rules, `

`) + this.emit(this.indent(-1) + `
}`);
  }
  /**
  * Visit custom-media node.
  */
  customMedia(e) {
    return this.emit("@custom-media " + e.name + " " + e.media + ";", e.position);
  }
  /**
  * Visit rule node.
  */
  rule(e) {
    const n = e.declarations;
    if (!n.length) return "";
    if (this.compress) return this.emit(e.selectors.join(","), e.position) + this.emit("{") + this.mapVisit(n) + this.emit("}");
    const r = this.indent();
    return this.emit(e.selectors.map((i) => r + i).join(`,
`), e.position) + this.emit(` {
`) + this.emit(this.indent(1)) + this.mapVisit(n, `
`) + this.emit(this.indent(-1)) + this.emit(`
` + this.indent() + "}");
  }
  /**
  * Visit declaration node.
  */
  declaration(e) {
    return this.compress ? this.emit(e.property + ":" + e.value, e.position) + this.emit(";") : e.property === "grid-template-areas" ? this.emit(this.indent()) + this.emit(e.property + ": " + e.value.split(`
`).join(`
`.padEnd(22) + this.indent()), e.position) + this.emit(";") : this.emit(this.indent()) + this.emit(e.property + ": " + e.value, e.position) + this.emit(";");
  }
}
var Rp = vp, Op = (t, e) => new Rp(e || {}).compile(t);
const Sp = _p, Ip = Op;
var wi = {
  parse: Sp,
  stringify: Ip
};
const Np = 2;
let Pn = 0;
const Eo = [
  "assign",
  "!component",
  "debugger",
  "eval",
  "include",
  "includeIf",
  "inject",
  "stack",
  "svg",
  "let",
  "newError",
  "vite",
  "inertia",
  "dd",
  "dump",
  "markdown"
];
function To(t) {
  return t.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function Lp(t) {
  return t.type !== "linebreak" ? (Pn = 0, !0) : (Pn++, Pn <= Np);
}
function kp(t) {
  return t.replace(/{{--(?![\s\n\r\t])/g, "{{-- ").replace(new RegExp("(?<![\\s\\n\\r\\t])--}}", "g"), " --}}");
}
function wn(t) {
  const e = /{{{[^{}]*}}}/g, n = "__TRIPLE_CURLY__", r = [];
  return t = t.replace(e, (i) => {
    const s = `${n}${r.length}`;
    return r.push(i), s;
  }), t = t.replace(/{{\s*/g, "{{ ").replace(/\s*}}/g, " }}"), r.forEach((i, s) => {
    t = t.replace(`${n}${s}`, i);
  }), t;
}
function Un(t) {
  return t.replace(/{{{\s*/g, "{{{ ").replace(/\s*}}}/g, " }}}");
}
function Cp(t, e, n, r, i, s) {
  const o = /<style\b[^>]*>([\s\S]*?)<\/style>/gi, a = /{{.*?}}/g, c = /{{{.*?}}}/g, u = /@(?!media|keyframes|supports|font-face|viewport|counter-style|page|document|font-feature-values)(?:!?\w+(?:\.\w+)*)\s*(?:\((?:[^)(]+|\((?:[^)(]+|\([^)(]*\))*\))*\))?[\s\S]*?@end/g, l = (i.customSingleLineEdgeTags ?? []).map((d) => d.startsWith("@") ? d.slice(1) : d), h = [...Eo, ...l], f = new RegExp(
    `@(${h.map(To).join("|")})\\s*(?:\\((?:[^)(]+|\\((?:[^)(]+|\\([^)(]*\\))*\\))*\\))?`,
    "g"
  );
  return t.value.replace(o, (d, _) => {
    let R = [], m = 0;
    _ = _.replace(
      c,
      (N) => {
        const L = `__SAFE_MUSTACHE_TAG_${m++}__;`;
        return R.push(N), L;
      }
    ), _ = _.replace(
      a,
      (N) => {
        const L = `__MUSTACHE_TAG_${m++}__;`;
        return R.push(N), L;
      }
    ), _ = _.replace(
      u,
      (N) => {
        const L = `/*__EDGE_TAG_BLOCK_${m++}__*/`;
        return R.push(N), L;
      }
    ), _ = _.replace(
      f,
      (N) => {
        const L = `/*__SINGLE_EDGE_TAG_${m++}__*/`;
        return R.push(N), L;
      }
    );
    const T = wi.parse(_), p = wi.stringify(T, { indent: r });
    return `${e}<style>
${p.split(`
`).map((N) => `${n}${N}`).join(`
`)}
${e}</style>`.replace(/\/\*__EDGE_TAG_BLOCK_\d+__\*\//g, (N) => {
      const L = R[parseInt(N.match(/\d+/)[0], 10)], P = new Nr(L);
      return new Cr(
        {
          ...i
        },
        s + 2
      ).handlePrint(P).trim();
    }).replace(/__MUSTACHE_TAG_\d+__;/g, (N) => R[parseInt(N.match(/\d+/)[0], 10)]).replace(/__SAFE_MUSTACHE_TAG_\d+__;/g, (N) => R[parseInt(N.match(/\d+/)[0], 10)]).replace(/\/\*__SINGLE_EDGE_TAG_\d+__\*\//g, (N) => R[parseInt(N.match(/\d+/)[0], 10)]);
  });
}
function bp(t, e, n, r, i, s) {
  const o = /<script\b([^>]*)>([\s\S]*?)<\/script>/i, a = /{{.*?}}/g, c = /{{{.*?}}}/g, u = /@(!?\w+(?:\.\w+)*)\s*(?:\((?:[^)(]+|\((?:[^)(]+|\([^)(]*\))*\))*\))?[\s\S]*?@end/g, l = (i.customSingleLineEdgeTags ?? []).map((L) => L.startsWith("@") ? L.slice(1) : L), h = [...Eo, ...l], f = new RegExp(
    `@(${h.map(To).join("|")})\\s*(?:\\((?:[^)(]+|\\((?:[^)(]+|\\([^)(]*\\))*\\))*\\))?`,
    "g"
  ), A = t.value.match(o);
  if (!A)
    throw new Error("Invalid <script> tag format");
  const [d, _, R] = A;
  let m = [], T = 0, p = R.replace(c, (L) => {
    const P = `__SAFE_MUSTACHE_TAG_${T++}__`;
    return m.push(L), P;
  }).replace(a, (L) => {
    const P = `__MUSTACHE_TAG_${T++}__`;
    return m.push(L), P;
  }).replace(u, (L) => {
    const P = `/*__EDGE_TAG_BLOCK_${T++}__*/`;
    return m.push(L), P;
  }).replace(f, (L) => {
    const P = `/*__SINGLE_EDGE_TAG_${T++}__*/`;
    return m.push(L), P;
  });
  const y = Ao.minify(
    {
      "file1.js": p
    },
    {
      compress: !1,
      keep_fnames: !0,
      mangle: !1,
      output: {
        beautify: !0,
        comments: "all",
        indent_level: e
      }
    }
  );
  if (y.error)
    throw new Error(JSON.stringify(y.error));
  const N = y.code.replace(/\/\*__EDGE_TAG_BLOCK_\d+__\*\//g, (L) => {
    const P = parseInt(L.match(/\d+/)[0], 10), se = m[P], Ce = new Nr(se);
    return new Cr(
      {
        ...i
      },
      0
    ).handlePrint(Ce);
  }).replace(/\/\*__SINGLE_EDGE_TAG_\d+__\*\//g, (L) => {
    const P = parseInt(L.match(/\d+/)[0], 10);
    return m[P];
  }).replace(/__SAFE_MUSTACHE_TAG_\d+__/g, (L) => {
    const P = parseInt(L.match(/\d+/)[0], 10);
    return m[P];
  }).replace(/__MUSTACHE_TAG_\d+__/g, (L) => {
    const P = parseInt(L.match(/\d+/)[0], 10);
    return m[P];
  });
  return `${n}<script${_}>
${N.split(`
`).map(
    (L) => L.trim().length === 0 ? "" : `${r}${L}`
  ).join(`
`)}
${n}<\/script>`;
}
function yo(t) {
  const e = t.match(/^\s*/);
  return e ? e[0].length : 0;
}
function xp(t, e, n) {
  return `${e}${t.value.split(`
`).map((r, i) => {
    if (i === 0)
      return `${r.trim()}`;
    if (i === t.value.split(`
`).length - 1)
      return `${e}${r.trim()}`;
    const s = yo(r);
    return `${" ".repeat(Math.max(e.length, s))}${r.trim()}`;
  }).join(`
`).replace(/[^\S\r\n]+$/g, "")}${n ? `
` : ""}`;
}
class Cr {
  options;
  level;
  skipLevelOverride;
  useTabs;
  printWidth;
  tabWidth;
  singleAttributePerLine;
  inlineTags = /* @__PURE__ */ new Set([
    "a",
    "abbr",
    "acronym",
    "b",
    "bdi",
    "bdo",
    "big",
    "br",
    "button",
    "cite",
    "code",
    "data",
    "dfn",
    "em",
    "i",
    "img",
    "input",
    "kbd",
    "label",
    "mark",
    "meter",
    "q",
    "ruby",
    "rp",
    "rt",
    "s",
    "samp",
    "select",
    "small",
    "span",
    "strong",
    "sub",
    "sup",
    "textarea",
    "time",
    "u",
    "var",
    "wbr",
    "feFuncR",
    "feFuncG",
    "feFuncB",
    "feFuncA"
  ]);
  constructor(e, n = void 0) {
    this.options = e, this.level = n ?? 0, this.skipLevelOverride = !!n, this.useTabs = e.useTabs ?? !1, this.printWidth = e.printWidth ?? 80, this.tabWidth = e.tabWidth ?? 4, this.singleAttributePerLine = e.singleAttributePerLine ?? !1;
  }
  isInlineTag(e) {
    return this.inlineTags.has(e);
  }
  getIndent(e, n = "none") {
    const r = e !== void 0 ? Math.max(e, 0) : this.level;
    return this.adjustIndentLevel(n), `${this.useTabs ? "	" : " ".repeat(this.tabWidth * r)}`;
  }
  adjustIndentLevel(e) {
    e === "increase" ? this.level++ : e === "decrease" && this.level--;
  }
  formatMultilineValue(e, n, r = !0) {
    return e.split(`
`).map((i, s, o) => {
      if (s === 0)
        return `${r ? n : ""}${i.trim()}`;
      if (s === o.length - 1) return `${n}${i.trim()}`;
      const a = yo(i);
      return `${" ".repeat(Math.max(n.length, a))}${i.trim()}`;
    }).join(`
`);
  }
  formatAttributes(e, n = "") {
    return e.map(
      (r) => r.attributeValue ? `${n}${r.attributeName}=${Un(wn(r.attributeValue)).trim()}` : `${n}${r.attributeName.trim()}`
    ).join(n ? `
` : " ");
  }
  formatEdgeSafeMustacheProps(e, n = "") {
    return e.map(
      (r) => `${n}${Un(r.value).trim()}`
    ).join(n ? `
` : " ");
  }
  formatEdgeMustacheProps(e, n = "") {
    return e.map((r) => `${n}${wn(r.value).trim()}`).join(n ? `
` : " ");
  }
  formatEdgeTagProps(e, n = "") {
    return e.map((r) => `${n}${r.value.trim()}`).join(n ? `
` : " ");
  }
  formatComments(e, n = "") {
    return e.map((r) => `${n}${r.value}`).join(n ? `
` : " ");
  }
  printDocumentNode(e) {
    return this.skipLevelOverride || (this.level = 0), e.children.filter(Lp).map(
      (n, r, i) => this.handlePrint(n, i[r - 1], i[r + 1])
    ).join("");
  }
  printDTDNode(e) {
    return `${this.getIndent()}${e.value}`;
  }
  printStandardNode(e) {
    const n = e.type === "scriptlet";
    return this.formatMultilineValue(
      e.value,
      n ? "" : this.getIndent()
    );
  }
  printScriptElementNode(e) {
    return bp(
      e,
      this.tabWidth,
      this.getIndent(),
      this.getIndent(this.level + 1),
      this.options,
      this.level
    );
  }
  printStyleElementNode(e) {
    return Cp(
      e,
      this.getIndent(),
      this.getIndent(this.level + 1),
      this.getIndent(1),
      this.options,
      this.level
    );
  }
  printEdgeComment(e) {
    return this.formatMultilineValue(
      kp(e.value.trim()),
      this.getIndent()
    );
  }
  printEdgeMustacheNode(e, n, r) {
    const i = !(n?.type === "htmlText" || n?.type === "edgeMustache" || n?.type === "edgeEscapedMustache" || n?.type === "edgeSafeMustache" || (n?.type === "openingTag" || n?.type === "voidTag" || n?.type === "closingTag") && this.isInlineTag(n.tagName)), s = !(r?.type === "htmlText" || r?.type === "edgeMustache" || r?.type === "edgeEscapedMustache" || r?.type === "edgeSafeMustache" || (r?.type === "openingTag" || r?.type === "voidTag" || r?.type === "closingTag") && this.isInlineTag(r.tagName));
    let o = `${i ? this.getIndent() : ""}`;
    const a = e.type === "edgeSafeMustache" ? Un(e.value) : wn(e.value);
    return o += s ? a.replace(/[\r\n]+/g, "").trimEnd() + `
` : a, o;
  }
  printOpeningNode(e, n, r) {
    let i = this.formatAttributes(e.attributes), s = this.formatEdgeTagProps(e.edgeTagProps), o = this.formatEdgeSafeMustacheProps(
      e.edgeSafeMustaches
    ), a = this.formatEdgeMustacheProps(e.edgeMustaches), c = this.formatComments(e.comments);
    const u = `${i} ${o} ${a} ${s} ${c}`.length, l = this.getIndent(this.level + 1), h = this.getIndent(
      void 0,
      e.type === "openingTag" ? "increase" : "none"
    ), f = this.getIndent(
      e.type === "openingTag" ? this.level - 1 : this.level
    ), A = !this.isInlineTag(e.tagName) && r?.type !== "linebreak", d = !((n?.type === "htmlText" || n?.type === "edgeMustache" || n?.type === "edgeEscapedMustache" || n?.type === "edgeSafeMustache") && this.isInlineTag(e.tagName));
    if (u > this.printWidth || this.singleAttributePerLine) {
      const R = e.type == "voidTag" ? "/>" : ">";
      i = this.formatAttributes(e.attributes, l), s = this.formatEdgeTagProps(e.edgeTagProps, l), o = this.formatEdgeSafeMustacheProps(
        e.edgeSafeMustaches,
        l
      ), a = this.formatEdgeMustacheProps(
        e.edgeMustaches,
        l
      ), c = this.formatComments(e.comments, l);
      const m = u - 2 > 0 ? `
${f}` : "";
      return `${d ? h : ""}<${e.tagName}${i ? `
${i}` : ""}${a ? `
${a}` : ""}${o ? `
${o}` : ""}${s ? `
${this.formatMultilineValue(s, l)}` : ""}${c ? `
${this.formatMultilineValue(c, l)}` : ""}${m}${R}${A ? `
` : ""}`;
    }
    const _ = e.type == "voidTag" ? " />" : ">";
    return `${d ? h : ""}<${e.tagName}${i ? ` ${i}` : ""}${a ? ` ${a}` : ""}${o ? ` ${o}` : ""}${s ? ` ${this.formatMultilineValue(s, "")}` : ""}${c ? ` ${this.formatMultilineValue(c, "")}` : ""}${_}${A ? `
` : ""}`;
  }
  printClosingNode(e, n, r) {
    const i = !this.isInlineTag(e.tagName) || n?.type === "linebreak" || n?.type === "edgeTag", s = n?.type !== "linebreak" && n?.type === "closingTag" && this.isInlineTag(n.tagName) && !this.isInlineTag(e.tagName), o = !this.isInlineTag(e.tagName) && r?.type !== "linebreak" && !((r?.type === "openingTag" || r?.type === "voidTag" || r?.type === "closingTag") && this.isInlineTag(r.tagName));
    return `${s ? `
` : ""}${i ? this.getIndent(this.level - 1, "decrease") : this.getIndent(0, "decrease")}</${e.tagName}>${o ? `
` : ""}`;
  }
  printEdgeTagNode(e, n) {
    let r = "none", i = this.level;
    e.value.includes("@end") ? (r = "decrease", i--) : e.value.includes("@else") ? i-- : e.value.includes("@!") || e.value.includes("@let") || e.value.includes("@svg") || e.value.includes("@assign") || e.value.includes("@inject") || e.value.includes("@eval") || e.value.includes("@debugger") || e.value.includes("@newError") || e.value.includes("@vite") || e.value.includes("@inertia") || e.value.includes("@stack") || e.value.includes("@dd") || e.value.includes("@dump") || e.value.includes("@markdown") || (this.options.customSingleLineEdgeTags || []).includes(
      e.value
    ) || e.value.match(/^@include\(.*/)?.length || e.value.match(/^@includeIf\(.*/)?.length || !e.value.includes("(") ? r = "none" : r = "increase";
    const s = n?.type !== "linebreak" && !e.value.includes(`
`);
    return xp(
      e,
      this.getIndent(i, r),
      s
    );
  }
  printHtmlTextNode(e, n, r) {
    const i = !(n?.type === "scriptlet" || n?.type === "edgeMustache" || n?.type === "edgeSafeMustache" || n?.type === "edgeEscapedMustache" || (n?.type === "openingTag" || n?.type === "voidTag" || n?.type === "closingTag") && this.isInlineTag(n.tagName)), s = !(r?.type === "edgeMustache" || r?.type === "edgeSafeMustache" || r?.type === "edgeEscapedMustache" || r?.type === "htmlText" || (r?.type === "openingTag" || r?.type === "voidTag" || r?.type === "closingTag") && this.isInlineTag(r.tagName) || r?.type === "scriptlet"), o = i ? this.getIndent() + e.value : e.value;
    return s ? `${o.trimEnd()}
` : o;
  }
  printLineBreak(e) {
    return e.value;
  }
  handlePrint(e, n = void 0, r = void 0) {
    switch (e.type) {
      case "document":
        return this.printDocumentNode(e);
      case "dtd":
        return this.printDTDNode(e);
      case "htmlComment":
      case "htmlConditionalComment":
      case "cdata":
      case "scriptlet":
        return this.printStandardNode(e);
      case "scriptElement":
        return this.printScriptElementNode(e);
      case "styleElement":
        return this.printStyleElementNode(e);
      case "edgeComment":
        return this.printEdgeComment(e);
      case "edgeMustache":
      case "edgeEscapedMustache":
      case "edgeSafeMustache":
        return this.printEdgeMustacheNode(e, n, r);
      case "openingTag":
      case "voidTag":
        return this.printOpeningNode(e, n, r);
      case "closingTag":
        return this.printClosingNode(e, n, r);
      case "edgeTag":
        return this.printEdgeTagNode(e, r);
      case "htmlText":
        return this.printHtmlTextNode(e, n, r);
      case "linebreak":
        return this.printLineBreak(e);
      default:
        return "";
    }
  }
}
function Mp(t, e) {
  const n = t.getNode();
  return new Cr(e).handlePrint(n);
}
const wp = [
  {
    name: "EdgeJS",
    parsers: ["edgejs"],
    extensions: [".edge"],
    tmScope: "text.html.edge",
    aceMode: "html",
    linguistLanguageId: 460509620,
    vscodeLanguageIds: ["edge"]
  }
], Up = {
  edgejs: {
    parse(t) {
      return Nr(t);
    },
    astFormat: "edgejs",
    locStart(t) {
      return t.start;
    },
    locEnd(t) {
      return t.end;
    }
  }
}, Dp = {
  edgejs: {
    print: Mp
  }
}, $p = {
  useTabs: !1,
  tabWidth: 4,
  printWidth: 80,
  singleAttributePerLine: !1,
  customSingleLineEdgeTags: []
}, Fp = {};
export {
  $p as defaultOptions,
  wp as languages,
  Fp as options,
  Up as parsers,
  Dp as printers
};
